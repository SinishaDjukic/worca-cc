{
  "name": "worca-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "docs.worca.dev — static site built from the repo's docs/ folder",
  "scripts": {
    "build": "node build.mjs",
    "preview": "npm run build && npx wrangler dev",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
