# Agent F2 — Plugins API · agent-gen v2 · Agents-view port editor · CLI (adjudication, 2026-08-26)

## 0. Ground truth on dev
- In-tree plugins/ conversion list is EMPTY: all 5 bundled plugins (github-source `>=1 <2`, telegram/slack/discord/teams-chat `>=2 <3`) ship only connector/ or channel/ — no agents/, no workflows/.
- smoke:plugin fixture is v1 and must convert: test/fixtures/plugins/mock-source/ — agents/mockHelper.meta.json (consumes/produces), workflows/mock-flow.json (version:1, steps), manifest `>=1 <2`. scripts/smoke-plugin.mjs itself needs no change.
- API 2 (commit 025d07f3) = chatChannels + persistent channel-worker protocol; connector contract unchanged. apiSatisfies is SET semantics (plugin-manifest.mjs:47-76, "any member satisfies") and negotiatedApi hands children the HIGHEST satisfying member — plugin-shim.mjs:111, chat/channel-host.mjs:102. Old branch collapsed the set to one integer and bricked every `>=1 <2` manifest.
- MOCK_WRITER_ROLES does NOT exist on dev — only non-exported MOCK_FANOUT_ROLES (claude-runner.mjs:850). Mock switch arms at :1066-1105 (14 roles). Must be ADDED as export.
- --max-refine/--max-review do not exist in the CLI (parseArgs worca-cc.mjs:61-135); skills/worca/SKILL.md:28 already stale. CLI subscribes to the orchestrator directly (orch.on('phase') :375).
- 88851499's "agents accordion" is the run-setup (New pipeline) view — #agents-config/#agents-rows (index.html:299-310, app.js:3302 renderAgentRows). NOT the Agents management view (data-view="agents" index.html:800-882, app.js:6595-6893).
- Builtins today: Edit/Delete hidden, Duplicate shown (buildAgentCard app.js:6640-6643); store 409 BUILTIN "duplicate it under a new name instead" (agent-store.mjs:66/81/123); plugin-origin agents get code PLUGIN on update/delete (:84-89).

## 1. Plugin API — DECISION: bump to API 3, keep the API SET, gate data contributions by CONTENT
plugin-api.mjs: WORCA_PLUGIN_API = 3, WORCA_PLUGIN_APIS = [1, 2, 3]. Comment: API 3 = agent sidecars are meta v2 (typed ports) and workflows/*.json are v2 graphs; connector and channel-worker protocols unchanged 1→2→3. Connectors/channels unaffected: negotiatedApi('>=1 <2') → 1, '>=2 <3' → 2; plugin-shim*.mjs, channel-host.mjs, plugin-repo.mjs, marketplaces.mjs, plugin-inventory.mjs, plugins-lock.mjs untouched.
Why not "keep API 2 and accept both": silently changes what `>=2 <3` means for a plugin shipping agents — breaking change without version signal. Why not single integer: bricks connector-only API-1 plugins.
Rules:
1. plugin-manifest.mjs gains pure helpers: declaredApi(range) (lowest integer accepted; null if unconstrained), dataContractIssues(absDir) → { agentsV1: string[], workflowsV1: string[] } (sidecars lacking metaVersion === 2, templates lacking version === 2), apiMismatch(range, issues) → null | { builtFor, host: 3, agents: n, workflows: m }.
2. validatePluginDir (:402-447, used by install/update/link/`worca plugin validate`): v2 sidecars → SHARED validateMetaV2; every failed rule ERROR `agents/<f>: <rule text verbatim>`; key===stem, sibling .md, KEY_RE unchanged. v1-shaped sidecar/template: if negotiatedApi(range) === 3 (range admits 3, or no engines) → ERROR `agents/<f>: not a meta v2 sidecar (declare "metaVersion": 2 with typed inputs/outputs) — plugin API 3 no longer reads channel sidecars` / `workflows/<f>: not a version-2 graph template (nodes/wires) — port the "steps" pipeline`; if range excludes 3 → WARN with the shared message. Plugin still installs; connector/channel work. v2 templates → SHARED validateGraph(graph, portsFn) with portsFn from THIS plugin's own sidecars + engine flow ports, full kind set, V1–V21. Own-keys rule: `workflows/<f>: references agent key "<k>" which this plugin does not ship`. Errors `workflows/<f>: V<n>: <msg>`; warnings logged.
3. Load time: registry scanLayer (all layers) skips non-v2 sidecars with `[agent-registry] <layer>/<f>: built for plugin API N … ignored`; importPluginWorkflows (plugin-workflows.mjs:44) skips non-v2 with same message, validates v2, upserts version = 2, graph, steps='[]', feedbacks='[]', archived_at = NULL (successful re-import un-archives). wfp_<plugin>_<slug> minting + origin unchanged. referencedPluginAgents walks graph.nodes where kind === 'agent'. removePluginWorkflows guard unchanged.
4. Cut-over migration archives ALL v1 rows including wfp_* (decision #8); plugins never deleted.
5. listInstalledPlugins (plugin-store.mjs:428) adds apiMismatch; broken stays "manifest unparseable". dirChecks (:145) adds c('agents-api', ok, message) so `worca plugin doctor` + Doctor modal say it; CLI `worca plugin list` prints the message.
6. Plugins view ui/public/plugins-view.mjs: after linked badge (:59) → `if (p.apiMismatch) head.appendChild(h(doc,'span','badge amber pl-api-mismatch','needs update'))`; after .pl-contrib (:73) → `card.appendChild(h(doc,'small','pl-api-note hint err', apiMismatchMessage(p.apiMismatch)))`; export apiMismatchMessage. Amber.
7. FINAL WORDING (one function): `built for plugin API ${builtFor ?? 'an older version'}; this version of worca requires plugin API 3 for agents and pipeline templates — update or reinstall the plugin (${n} agent(s), ${m} template(s) ignored)`.
8. `worca plugin init` (worca-cc.mjs:971): engines `>=3 <4`; example sidecar v2 (task:md in, notes:md out filename 'notes.md'); workflows/example-flow.json = old branch block (old:src/cli/worca-cc.mjs:1013-1030, task → helper → end). cli-plugin.test.mjs:52 passes by construction.
9. Fixture conversion: mock-source manifest `>=3 <4`; mockHelper.meta.json → { metaVersion:2, key, displayName, description, color, agentFile, runnerType:'producer', requiresSkills:['mock-skill'], order:900, inputs:[{id:'task',type:'md'}], outputs:[{id:'notes',type:'md',filename:'notes.md'}] }; mock-flow.json → v2 graph n_task → n_helper(mockHelper) → n_end.
10. Tests: plugin-manifest.test.mjs:31-33,75 (3, [1,2,3]), new pins for declaredApi/apiMismatch/warn-vs-error; plugin-workflows.test.mjs 8 tests rewritten to v2 (borrow old branch's 12 names incl. "v1 steps rejected", "no End fails V21"); plugins-view.test.mjs:157 + badge/note; plugin-agent-registry.test.mjs:28 fixture sidecar → v2; api-plugins.test.mjs:125 uses converted fixture.

## 2. Agent-gen v2 (src/core/agent-gen.mjs)
Dies: constructor this.channels (:39); _neighborBlock :113-125 (produces/consumes/optionalConsumes JSON + `## Channel vocabulary` paragraph :122-123); _metaSchemaBlock :129-134 (the EXACT shape line with consumes/produces/connectsTo/loopSource); server.mjs:3897-3905 collectChannelIds + uses at :3926/:3949/:3993; channels: in startAgentGen. SYSTEM_PROMPT/task headers say "worca".
New _metaSchemaBlock text:
> Write the metadata JSON to: `<metaPath>`
> One JSON object, sidecar meta v2 — typed PORTS, no channel vocabulary. REQUIRED: { "metaVersion": 2, "key": "<lowerCamel>", "displayName", "description", "runnerType": "producer"|"verifier"|"clarifier", "inputs": [..], "outputs": [..] } — at least one output port, at most 8 ports per side.
> An INPUT port: { "id", "type": "md"|"json"|"void", "label", "required" (default true — a required input is a barrier the agent waits on), "loop" (true = loop receiver; forces required:false; a fresh token re-fires the agent), "expands" (json only — run once per element of the array it carries), "as": "file"|"answers"|"fix-review"|"worktree" (how the payload renders into the prompt; default "file"; "worktree" is the only renderer a void input takes), "directive" (markdown appended to the task prompt only when this port fires; {path} is substituted) }.
> An OUTPUT port: { "id", "type", "when": "always"|"blocking"|"clean" (default always; anything else requires "verdict"), "filename" (plain basename, required on md/json, may use {cycle} {vsuffix} {base}), "store": "run"|"project" (default run), "artifactKind" (defaults to the id) }. A void port carries no payload — no filename, no store.
> Port ids are lowerCamel, ≤32 chars, unique per side. The id "await" is RESERVED — the engine synthesizes an await gate on every agent node; never declare it.
> Runner obligations: "verifier" MUST declare "verdict": { "filename": "<basename>" }; "clarifier" MUST declare ≥1 json output; "producer" just writes its outputs.
> Optional agent fields: "color" (green|peach|red|blue|violet|amber), "icon" (inline SVG path), "sideEffect": "code", "scope": "project"|"workspace-only", "domain", "order", "fanOut", "asksQuestions"/"questionsLocked"/"questionsDefault", "requiresSkills": [..], "promptHints", "wantsRequest", "workspaceFanOut", "workspaceStrategy": "explore"|"task"|"review", "workspaceVariantOf": "<agentKey>" (requires scope workspace-only), "placeable": false, "mockRole" (omit unless mimicking a built-in writer; unknown values dropped).
> [description palette-blurb paragraph + questions-flags paragraph unchanged from :135-142]
Neighbor block: old-branch text (old:agent-gen.mjs:111-126) — neighbors as {key, displayName, inputs:[{id,type}], outputs:[{id,type,when}]} from registry metas (pick() :3987), plus "wires only require matching port TYPES, so port ids are yours to choose". _fullPrompt item 1 adds: "Document every port under a `## Ports` heading (one bullet per port id, what it carries); never hardcode filenames — the engine binds every port to an absolute path in the task prompt."
Validation: after read-back run validateMetaV2(rawMeta); errors → agentgen-error `the generator produced invalid metadata: <rules joined '; '>`; then normalizeMeta (v2). Mock: mockAgentGen (claude-runner.mjs:~1150) writes old branch's v2 meta (metaVersion:2, inputs:[{id:'plan',type:'md'}], outputs:[{id:'review',type:'md',filename:'review-{cycle}.md'}]) + body with `## Ports`. agent-gen.test.mjs:61-66 pins stay; add pins for metaVersion, reserved await, verdict, absence of consumes.

## 3. Agents view v2 port editor
Payload: GET /api/agents → { agents, mockWriterRoles: [...MOCK_WRITER_ROLES] } (channels gone); state.mockWriterRoles replaces state.channelIds (app.js:6600). ADD `export const MOCK_WRITER_ROLES = new Set(['clarify','planner-plan','refiner','decomposer','implementer','reviewer','plan-review','workspace-scan','agent-gen','workspace-reviewer','manual-tests-checklist','manual-web-ui-testing','generic-producer','generic-verifier'])` to claude-runner.mjs beside MOCK_FANOUT_ROLES (:850), lockstep with switch :1066-1105.
Card (index.html:814-838, buildAgentCard app.js:6634): .agent-origin badge, then NEW `<span class="agent-not-placeable badge" hidden>not placeable</span>` (amber, shown when placeable === false); .agent-sub = `${key} · ${runnerType} — ${description || portSummary}`; Input/Output chip rows (.agent-chips-in/-out, CSS style.css:1385-1393) show port pills `id · type` (void pills .void dashed border); loop inputs get ↺. portSummary = "Reads <non-void input ids>; produces <non-void output ids>." derived in registry (old derivePortSummary).
Editor (.agent-edit-pane, index.html:841-881): lines 842-876 replaced by `<div class="agent-form"></div>`, built by agentFormRender (also mounted in wizard Step 3 #agw-step-3, replacing :1025-1047). Mockup:
```
Display name [Docs Writer        ]   Description [Writes the docs…              ]
Color [green ▾]  Runner type [verifier ▾]   Order [8]   Verdict filename [docs-review-cycle{cycle}.json]
Domain [coding]  Scope [project ▾]         Icon (SVG path) [<path …>]
INPUTS                                                        [+ input]  (3/8)
┌ id [plan   ] type [md ▾] as [file ▾]             [▲][▼][×] ┐
│ ☑ required ☐ loop ☐ expands        [directive]              │
├ id [fix    ] type [md ▾] as [fix-review ▾]       [▲][▼][×] ┤
│ ☐ required(disabled) ☑ loop ☐ expands  [directive ✓]        │
│  ▸ textarea: "## Fix it\n\n{path}"                           │
│  hint: loop inputs are optional; a fresh token re-fires…    │
└─────────────────────────────────────────────────────────────┘
OUTPUTS                                                       [+ output] (2/8)
┌ id [review ] type [md ▾] when [blocking ▾] filename [{base}-docs-review.md] store [project ▾] [▲][▼][×]
│  hint: "blocking"/"clean" branch on the verdict file above
├ id [pass   ] type [void ▾] when [clean ▾]  (filename/store hidden)  [▲][▼][×]
Capabilities   ☑ Research fan-out  ☑ Asks questions  ☑ Questions locked  ☑ Questions on by default
               ☑ Writes code (sideEffect)  ☑ Carries the original request  ☑ Placeable on a canvas
Mock role [generic-verifier ▾ (auto|14 roles)]   Requires skills [mock-skill, …]   Prompt hints [textarea]
Workspace runs ☑ Force fan-out   Strategy [review ▾ (—|explore|task|review)]  Variant of [reviewer] (datalist = registry keys)
System prompt (markdown) [textarea rows=16]
<p class="agent-edit-msg form-msg">                      [Cancel] [Save]
```
Conventions: .field > label + .input/.select/.textarea, .row-2, .fanout-toggle checkboxes, .badge, .btn-ghost.btn-mini (style.css:1170,1383-1401,3307-3311); new classes .agent-form, .agent-ports(-in/-out), .agent-ports-head, .agent-ports-list, .port-row, .port-row-top, .port-row-flags, .pf-id/.pf-type/.pf-as/.pf-when/.pf-filename/.pf-store/.pf-required/.pf-loop/.pf-expands/.pf-directive(-wrap/-toggle)/.pf-remove/.pf-up/.pf-down/.pf-add-in/.pf-add-out/.pf-hint, .agent-caps, .agent-workspace, .agent-not-placeable (borrow old style.css:1049-1064). One delegated click/change listener per host (bindAgentForm). Live rule text (.pf-hint, non-blocking): id regex, await reserved, ≤8 per side (add disabled at 8), void ⇒ no filename/store (hidden), loop ⇒ required off, expands ⇒ json only, as type requirements, when ≠ always ⇒ verdict required, verifier ⇒ verdict, clarifier ⇒ ≥1 json output. Client never pre-empts store: PUT sent, 400 renders data.error VERBATIM in .agent-edit-msg (app.js:6863), pane stays open. agentFormRead emits v2 sidecar only; optional capabilities ABSENT when off; unsurfaced keys (artifactKind, label, description per port) ride via dataset.extra.
Store 400 rule texts (verbatim from validateMetaV2): `sidecar requires metaVersion 2` · `key "<k>" is not a valid agent key` · `runnerType must be one of producer, verifier, clarifier` · `at least one output port is required` · `inputs: at most 8 ports per side (got N)` · `inputs: port id "await" is reserved — the engine synthesizes the await gate port on every agent node` · `inputs: bad port id "<id>"` · `inputs: duplicate port id "<id>"` · `outputs.<id>: type must be one of md, json, void` · `outputs.<id>: void ports carry no filename or store` · `outputs.<id>: md outputs require a filename template` · `outputs.<id>: filename "<f>" must be a plain basename` · `outputs.<id>: filename "<f>" uses unknown token(s) {x}` · `outputs.<id>: when "blocking" requires the agent to declare verdict: { filename }` · `outputs: filename template "<f>" is shared by ports of different types` · `inputs.<id>: expands is only legal on json inputs` · `inputs.<id>: as "<as>" requires a <type> port (got <t>)` · `runnerType "verifier" requires verdict: { filename }` · `runnerType "clarifier" requires at least one json output port` · `sideEffect must be "code" when present` · `workspaceStrategy must be one of explore, task, review` · `workspaceVariantOf must be an agent key` / `…must not reference the agent itself` / `…requires scope "workspace-only"`. Unknown mockRole = warning + dropped. agent-store.mjs calls assertMetaV2(raw) before normalizeMeta in create/update (:62,:103); deleteAgent reference scan walks wf.nodes and matches workspaceVariantOf targets (old :145-155).
Builtins/plugin agents: read-only, editable-as-copy — today's affordance (Edit/Delete hidden, Duplicate → POST copy → editable; 409 texts unchanged). Header chips show their ports.

## 4. CLI parity (src/cli/worca-cc.mjs)
- --workflow keeps flag; before createOrchestrator (:1532) call shared assertRunnableWorkflow(id) (workflows.mjs; also POST /api/run server.mjs:1062): NOT_FOUND → existing `unknown workflowId "<id>"`; ARCHIVED → `workflow "<id>" is archived — a v1 template that was not migrated to a node graph; run a saved v2 pipeline or rebuild it in the Composer`. CLI fail() exit 2.
- Exec rendering replaces orch.on('phase') :375-377, phaseLabel :245-250, statusMark :252-256 with orch.on('exec'). Formatter in new pure src/cli/render.mjs (formatExecLine, fmtDur). Formats: start `▶ planner #2 (revise) via w5` — label = agentKey (flow: `· and`, `· or`, `■ end`, `▶ task`); (port) only when a loop port among trigger.freshPorts; `via <wireIds>` only for ordinal > 1. done `✓ planner #2 1m03s $0.12` (duration local; cost from costUsd); verifiers append ` — blocking`/` — clean` when payload carries verdict.hasBlocking. error `✗ implementer #1 12s — <error>`. kind:'task' slices indented `  ▶ implementer #1 · <phase>/<taskId> "<title>"`. End `■ end — <type> <path|value>`; after `Pipeline complete.` print `Result: <type> <path|value>`; endReached === false → yellow `Pipeline went quiet without reaching End.` token events not rendered.
[Main-loop note: Agent D proposed human labels from the manifest ("Implementer #2 · fix ← Reviewer", "⏹ End ← Reviewer.pass → plan-review.md"); reconcile in the spec: use manifest labels (D) with F2's status glyphs — ids never shown.]
- Gates: --yes unchanged — _ask auto-resolves kind:'gate' to continue (A4, orchestrator.mjs:2856-2897). Interactive askGate (:318-338) header `Loop gate on wire ${wireId} — maximum cycles reached.` (question payload gains wireId).
- --max-refine/--max-review: never existed; do NOT add wire-overlay flags. parseArgs untouched.
- --mock parity by construction; npm run smoke unchanged.
- HELP :182-220: headline → `worca — node-graph multi-agent pipelines`; `--workflow <id>  Saved pipeline template to run (default: wf_default)`.
- Tests: cli-resume.test.mjs has NO phase/output pins; its injected runners:{producer,verifier} (:96-121) and cli-budget-gates.test.mjs:91's seeded resume_point{workflowId:'wf_default'} depend on engine runner-injection/resume-v2 contracts. New test/cli-exec-render.test.mjs pins formatter + archived refusal.

## 5. Docs / skills — stale list
- skills/worca/SKILL.md: :3 description and :8 "Preflight -> Plan -> Refine (loop) -> Implement -> Review (loop) -> Done" → "runs the selected pipeline template (default wf_default: Plan → Refine ↺ → Implement → Review ↺ → End)"; :22 "streams phase changes" → "streams every agent execution (start/done, cycle, cost) and live logs"; DELETE :28 (--max-refine/--max-review); add `--workflow <id>`; :42 "step tracker" → "live pipeline graph".
- .claude/skills/orchestrate/SKILL.md: insert after frontmatter (:5): `> **Frozen (2026-08-26).** This skill is a prose clone of the v1 fixed pipeline (Plan → Refine → Implement → Review). worca now runs node-graph pipelines (typed ports, wires, loop budgets, AND/OR/Combine/End cards); this document is deliberately NOT updated and may drift from the engine. For the real engine use /worca. Follow-up: rewrite or retire.`
- .claude/skills/creating-worca-cc-plugins/SKILL.md: :14 agents row (meta v2 sidecar with typed ports); :16 workflows row (v2 graph JSON — one Task, one End); :51 `">=1 <2"` → `">=3 <4"` + set-semantics sentence; :91 heading "(plugin API v1)" → "(unchanged through API 3)"; :110 ctx.apiVersion = negotiated highest; :136-137 rows "v1 sidecar / v1 steps template → ignored with a Plugins-view note"; new section "Agents & templates (API 3)" with sidecar mini-schema + scaffold graph JSON.
- README.md:83-91 "Workflow Composer": rewrite to typed-port cards, wires, loop wires with cycle caps, Task/End/AND/OR/Combine; drop "Reset to default redraws…"; :71 add "typed ports". docs/screenshots.md:58 caption → "Pipeline Composer, default graph" + re-shoot composer.png. docs/ARCHITECTURE.md:16, docs/storage.md:41,50 accurate. No CHANGELOG.

## Cross-agent flags
Engine: exec/gate payload fields (ordinal, trigger.freshPorts, verdict.hasBlocking, wireId on gate questions, endReached/result), assertRunnableWorkflow + archived_at, registryPortsFn for plugin validation, agent-registry.mjs:14 CHANNEL_IDS import dies with channels.mjs.
