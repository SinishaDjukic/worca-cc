# Agent F1 — Executor, meta v2, prompt parity, mock, fan-out, workspace (adjudication, 2026-08-26)

## 0. Drift since 2026-08-10 (dev authoritative)
`git diff 7c390dc8 HEAD -- agents/` is EMPTY — the 11 .md + .meta.json byte-identical to PR base. Drift is in the harness:
- phases.mjs:427 runOpts injects modelEnv: resolveModelEnv(c.model) (+7 lines) — executor spawning through runOpts honors modelEnv for free.
- claude-runner.mjs: runMock takes permissionMode; MOCK_ROLE==='ask'/dontAsk arm reads SYSTEM prompt only (:1034); argv staging (#380, prompt via stdin over 20k chars); mockEnabled exported. NO MOCK_WRITER_ROLES export exists.
- orchestrator.mjs: 4fcb9855 sibling-failure observer also rejects an open recovery prompt (:2260-2290); 885fb013 decomposed-task pause marks paused (:2363-2372); stderr as ERR_STREAM log attr + _logStepFailure clipMiddle 500 (:2384-2398); _checkCostLimits at step boundary with cost_pipeline/cost_total + readCostCapOverride (:2083, :2582-2612); session capture keyed by attr.stepKey (:3087-3093).
- agent-registry.mjs:29 DEFAULT_AGENTS_DIR exported, fileURLToPath-based (Windows fix); old branch's new URL().pathname reintroduces the bug.
- workflows.mjs:111-175, :413-436 template node.defaults layer (sanitizeNodeDefaults, setWorkflowNodeDefaults, PATCH /api/workflows/:id/defaults) — v2 node.config is its successor.
OLD-BRANCH SPOTS NOW STALE/WRONG (fix in rebuild):
1. old _execCtx builds node: {...node, fanOut, agentPrompt} and never stamps nc.tools → effectiveAllowedTools(base, ctx.node?.tools) (phases.mjs:48) loses frontmatter tools → manualWebUiTesting's Playwright MCP grants vanish.
2. Prompt lines the old executor DROPPED: implementer siblings block (phases.mjs:762-790, pinned by test/phases-implementer-task.test.mjs:33-41); 'Work inside the project directory (your cwd). Commit nothing; just edit files and tests.' (:836); plain implement arm 'Implement the plan using TDD (red-green-refactor). Follow it with NO deviation; deviate slightly only if a step does not work at all.' (:808-810); reviewer's checkpoint-ref diff sentence (:858-866); checklist's detached-workspace per-member diff variant (:1078-1088); planner's INLINE `## Clarifications already answered` + renderAnswers (:670-672); decomposer's tasks-dir + manifest-shape lines (:727-731).
3. Old _questionsPath(executionId, round) renames the questions file vs dev questions-<stepIndex>-<nodeId>-c<cycle>-r<round>.json (:2508) — check orchestrator-questions.test.mjs pins.
4. Old _artifact(kind, path, attr) 3-arg; dev 2-arg (:3881) — extend.
5. Old buildSystemPrompt 5-arg + lastResortPrompt; dev keeps FALLBACK_PROMPTS (:236-289) for v1 — leave 4-arg during coexistence.
6. Old sidecars v2-only — incompatible with coexistence.

## 1. Meta v2 schema + verified 11-row table
Schema = spec §5 + Amendment f (metaVersion:2, await reserved both sides, no start ports, mockRole validated against MOCK_WRITER_ROLES, capability fields wantsRequest/workspaceFanOut/workspaceStrategy/workspaceVariantOf/placeable/sideEffect/verdict, per-port as/directive/loop/expands/required/when/filename/store/artifactKind). Borrow old:agent-registry.mjs:63-330 (readMetaV2) rule-for-rule.
Every row additionally gets synthesized await:any input. Q = asksQuestions/locked/default.
| key (runner) | inputs | outputs → filename [store] | verdict | mockRole · flags |
|---|---|---|---|---|
| clarify (clarifier) | task:md | answers:json → clarify.json [run] kind clarify (:2712) | — | clarify · fanOut · Q y/y/y |
| planner (producer) | task:md · answers:json opt as:answers · revise:md loop, directive=REVISE (:655-660) | plan:md → {base}{vsuffix}.md [project] (:669, allocate :35) | — | planner-plan · fanOut · wsFanOut · ws explore (:667) · Q y/n/n |
| refiner (producer) | plan:md · revise:md loop | plan:md clean · revise:md blocking, same template [project] (:44, :697) | refine-review-cycle{cycle}.json (:53) | refiner · wantsRequest (:454) · fanOut · wsFanOut · ws explore (:695) · hints = calibration line (:706-707) |
| planReviewer (verifier) | plan:md | review:md blocking → {base}-plan-review.md [project] (:67) · pass:void clean | plan-review-cycle{cycle}.json | plan-review · wantsRequest · fanOut · wsFanOut · ws explore (:912) · hints (:908, :919) |
| decomposer (producer) | plan:md | tasks:json → decomposition.json [run] kind decomposition (additive artifact event) | — | decomposer · fanOut · Q y/n/n · no ws strategy |
| implementer (producer, sideEffect:code) | fix:md loop as:fix-review directive (:787-791) · task:json opt expands directive (:796-806) · plan:md directive = implement arm (:808-810) — DECLARED IN THIS ORDER (single-directive rule) | done:void | — | implementer · fanOut · wsFanOut · ws task (:832) · IMPLEMENTER_TOOLS ⇒ MultiEdit (:27) · hints = "Work inside… Commit nothing" (:836) |
| reviewer (verifier) | plan:md · done:void opt as:worktree | review:md blocking → {base}-impl-review.md [project] (:71) · pass:void clean | impl-review-cycle{cycle}.json | reviewer · wantsRequest · fanOut · ws review (:874) · NO wsFanOut |
| workspaceReviewer (verifier, ws-only, variantOf reviewer) | plan:md · done:void opt as:worktree | review:md blocking → {base}-ws-review.md [project] (:69) · pass:void clean | ws-review-cycle{cycle}.json | workspace-reviewer · wsFanOut (:139) · ws review (:956) · no wantsRequest · hints = UNION line (:967-969) |
| manualTestsChecklist (producer) | plan:md | checklist:md → manual-tests-checklist.md [run] | — | manual-tests-checklist · no fanOut · Q y/n/n · hints = changesInstruction with {diffInstruction} token (:1078-1088) |
| manualWebUiTesting (verifier) | checklist:md | review:md blocking → webui-review-cycle{cycle}.md [run] kind webui (:2807) · pass:void clean | webui-review-cycle{cycle}.json | manual-web-ui-testing · no fanOut · frontmatter Playwright tools MUST reach ctx.node.tools · hints (:1118, :1126) |
| workspaceScanner (producer, ws-only, placeable:false) | task:md | workspace:md → workspace-description.md [run] | — | workspace-scan (:1045) · off-pipeline, runWorkspaceScan :992 untouched |
Verdict-kind for reviews table: reviewKindOf(filename minus '-cycle{cycle}.json') (artifacts.mjs:225-230) → impl|plan|refine|ws|webui for builtins, <stem> for customs — identical to channels.mjs:47-53.

## 2. Registry coexistence — VERDICT (a) dual-shape, normalizeMeta extended
Sidecars carry metaVersion:2 + inputs/outputs + capability fields AND keep consumes/optionalConsumes/produces/connectsTo/loopSource until kill-list. (b) rejected (ports richer than channels); (c) rejected (plugin-manifest.mjs:402-424 enforces the pair contract).
Why "ignores unknown keys" is NOT enough: normalizeMeta (agent-registry.mjs:189-249) returns a FIXED key set, and agent-store.updateAgent round-trips {...existing, ...rawMeta} through it (agent-store.mjs:86-110) → user-layer v2 sidecar would lose ports on save. Therefore:
- normalizeMeta(raw): if raw.metaVersion === 2 run readMetaV2 (ported) and MERGE onto today's v1 object (inputs, outputs, verdict?, sideEffect?, mockRole?, wantsRequest?, workspaceFanOut?, workspaceStrategy?, workspaceVariantOf?, placeable?, portSummary, metaVersion). v2 validation errors ⇒ warn + skip whole sidecar. No metaVersion ⇒ v1 path, ports absent. Existing string `version` field ('1'/'2', test/agent-registry-schema-v2.test.mjs:66-77) unrelated — do not overload.
- validateMetaV2(raw) → {errors} exported pure for agent-store 400 path (agent-store.mjs:54-110).
- DEFAULT_SPEC, channelList, LEGACY_LABELS, registryToSteps untouched until kill-list; MOCK_WRITER_ROLES import into registry creates edge agent-registry → claude-runner (acceptable; verify no cycle via preflight.mjs).
- v2 resolveGraph refuses a template whose key has no inputs ("agent X has no v2 ports — port its sidecar to metaVersion 2").
Blast radius: agent-registry{,-decomposer,-workspace,-layered,-desc-fallback} deepEqual only v1 fields ⇒ green. test/agent-registry-schema-v2.test.mjs:90-99 asserts promptHints === '' for all 11 builtins ⇒ MUST be rewritten (five builtins gain hints). agents-meta.test.mjs green; add "all 11 validate as v2". plugin-manifest.mjs:402-424 checks key/stem/sibling only ⇒ v2 plugin sidecars pass; plugin-agent-registry.test.mjs:28, plugin-workflows.test.mjs:46 fixtures are v1 sidecars and stay valid. ui/server.mjs:3890 collectChannelIds and agent-gen.mjs:127 _metaSchemaBlock keep v1 fields until their plans.

## 3. Executor design and interfaces
Selection = node.kind → flow executor; kind:'agent' → meta.runnerType (clarifier → runClarifierExecution, else runAgentExecution). Injected opts.runners[runnerType] stays the test seam (dev :306).
Scheduler ⇄ executor (old:scheduler.mjs:315-326, :34-37):
```js
execute(args) // args = { node, executionId, ordinal, bindings:{portId: {seq,type,path,value,meta,forced}},
              //          trigger:{ wireIds:[], freshPorts:[] }, signal,
              //          taskArtifact?  (kind:'task' only),
              //          composite?: 'expand'|'phase'|'finish', expandsPort?, phase?, phaseStatus?, phases?,
              //          kind?: 'task', slice?: { id, title, phase, path, index, siblings:[{id,title,file}] } }
// → { outputs:{portId:{path?,type?,value?}}, verdict?: review|null, summary?, sessionId? }
// composite 'expand' → { phases:[{ordinal, tasks:[{id,title,file,path,nodeId}]}] } ; 'phase' → {} ; 'finish' → { outputs:{} }
```
Add slice.siblings (phase-mates) — scheduler's runSlice (old:scheduler.mjs:463-495) knows ph.tasks; v1's decomposedTaskNode (:190-210) needs it for the siblings block.
Orchestrator adapter _execute(args) (generalizes _runNode :2403 / _runDecomposedTask :2352): ONE ctx = phases ctx (_nodeCtx :2971-3021 fields: projectDir, runRoot, mcpConfigPath, mcpServerGrants, repos, pipelineDir, pipelineId, taskPrompt, toolInstruction, agentPrompts, checkpointRef, workspace, signal, extras, onEvent, claudeOpts{bin,permissionMode,model,effort,permissionRules,envScrub,envAllowlist,mock}) ∪ graph fields (node:{...tplNode, key, fanOut, agentPrompt, tools:nc.tools, promptHints}, nodeId, executionId, ordinal, cycle:ordinal, slice, bindings, trigger, template, portsFn, ports, meta, outputs, verdict, runCtx, resumeSessionId, ask). Order: _checkCostLimits() at every AGENT launch → _primeQuestions (:2486, disabled for clarifier/slices/auto) → _runNodeAttempts (:2437 verbatim, re-keyed by executionId) → _runOnce (:2467, vanished-session fallback) → _questionsLoop (:2517 verbatim; ask ids questions-<executionId>-r<n>) → _afterExecution: writeReview(pipelineId, verdictKind, ordinal, verdict) (was :2207), one _artifact(kind, path, {nodeId, executionId, port}) per DISTINCT path, _stageWorkingTree() (:3625) iff meta.sideEffect==='code' && !slice. Signal for slices = AbortSignal.any([abort, pauseAbort, args.signal]) (v1 :2354). On a slice's genuine error the adapter must ALSO reject an open recovery prompt as noteFailure does (:2265-2273). Session capture: _onAgentEvent (:3087) keys on attr.stepKey — set stepKey = executionId. Composite: expand = readDecomposition + writeDecomposition with nodeId = sliceExecutionId(...) + path = join(pipelineDir, t.file) (replaces _persistDecomposition :2232); phase = updatePhaseStatus + audit; finish = stage once after last phase (:2326) + empty outputs (done:void fires once). Pause inside a slice ⇒ {outputs:{}} + step paused, slice row stays running (885fb013).
Executor module API (src/core/graph/executor.mjs): allocateOutputs({node, ports, executionId, ordinal, runCtx}), allocateVerdict(...), portIoBlock({node, ports, bindings, outputs, verdict, ctx}), selectMode({ports, bindings, freshPorts}), buildAgentPrompt(ctx), runAgentExecution(ctx), runClarifierExecution(ctx), runTaskExecution/runAndExecution/runOrExecution/runEndExecution/runCombineExecution, readVerdict, resolveMockRole, normalizeDecomposition/readDecomposition, expandsOutputPort, taskSourcedPorts. runCtx = { pipelineDir, projectDir, baseName, datePrefix, workspaceKey, duplicateKey, slice, planVersion: () => ++this._planVersion } ({vsuffix} ticks once per DISTINCT template per execution; A2 planStoreSeed consumes 1).
Prompt assembly (pure buildAgentPrompt, reusing taskHeader :449, fanOutDirective :122, workspaceFanOutDirective :183, renderAnswers :1270, siblingsBlock :762, mockMarkers :312 — export the last two):
```
taskHeader(headerCtx, meta.displayName)          // isEntry = task-wired binding || meta.wantsRequest; extras only when task-wired
'\n## What to do\n\n' + baseInstruction(runnerType) + hints(with {pipelineDir},{cycle},{diffInstruction} substituted)
+ modeBlock(ONE winning directive)
+ fanOutDirective(ctxFanOut(ctx), {omitProjectAgents: detachedWs})
+ workspaceFanOutDirective(meta.workspaceStrategy, ctx.workspace, {relative: detachedWs})
+ (slice ? siblingsBlock(slice.siblings) : '')
+ portIoBlock(...)                               // '## Ports (this run)' / '### Inputs' / '### Outputs'
+ (verdict ? VERDICT_CONTRACT : '') + mockMarkers(...)
```
(i) SINGLE-DIRECTIVE RULE — selectMode renders only the first FRESH directive port in DECLARED order (implementer declares fix, task, plan) → fix beats task beats implement (v1 channels.mjs:212, _runStep :2181-2188); latched loop tokens never select (A3). (ii) as:'answers' renderer inlines `## Clarifications already answered\n\n` + renderAnswers(json.answers) (read in prepare()) plus the path. (iii) as:'worktree' renderer = v1 diffInstruction bytes (:858-866) keyed on ctx.checkpointRef, or workspaceDiffInstruction(ctx) on detached workspace; {diffInstruction} in hints resolves to the same helper (export diffInstruction(ctx) from phases). (iv) System prompt = buildSystemPrompt(toolInstruction, resolveAgentBody(ctx, key), key, ctx.workspace) unchanged. (v) allowedTools = meta.sideEffect==='code' ? IMPLEMENTER_TOOLS : READ_WRITE_TOOLS, unioned with ctx.node.tools + fan-out by effectiveAllowedTools (:48). (vi) Outputs sharing one path render one line `- Write **plan** (also **revise**) to: <path>`.
Clarifier (old:executor.mjs:430-475): spawn → read first json output through normalizeClarify (protocol.mjs:143) → if questions: writeStepQuestions + writeClarify → ctx.ask({id:'clarify-<nodeId>-<ordinal>', kind:'clarify', nodeId, agent, questions}) → normalizeClarifyAnswer → rewrite file {questions, answers} → publish. Replaces _runClarifyNode :2704 (ask id was clarify-<cycle>, :2714; check clarify-node.test.mjs).

## 4. Mock v2
Dev writer switch (claude-runner.mjs:1065-1112) = 14 labels: clarify, planner-plan, refiner, decomposer, implementer, reviewer, plan-review, workspace-scan, agent-gen, workspace-reviewer, manual-tests-checklist, manual-web-ui-testing, generic-producer, generic-verifier (ask arm :1034 stays outside). EXPORT MOCK_WRITER_ROLES = new Set([...14]), MOCK_ROLE_CLARIFY='clarify', MOCK_ROLE_DECOMPOSER='decomposer'; switch the two case labels to the constants so old structural audit (old:test/mock-graph.test.mjs:44-70) ports. Unclaimed = agent-gen, generic-producer, generic-verifier.
Chain resolveMockRole (old:executor.mjs:326-333): (1) meta.mockRole; (2) clarifier ⇒ clarify with MOCK_PRIOR = prior-answer count (mock returns questions:[] when >0, :1149-1152); (3) output wired into an expands input ⇒ decomposer + MOCK_TASKS_DIR = <pipelineDir>/tasks (:1311-1330); (4) verdict ⇒ generic-verifier; (5) generic-producer. MOCK_CYCLE = ordinal: every verifier mock gates on cycle <= 1 (:1272, :1404, :1455, :1592, :1664) ⇒ blocking at ordinal 1, clean at 2 ⇒ every loop closes at ordinal 2 incl. through the OR valve. MOCK_ASK via questionsPromptBlock/runOpts (:364); workspaceWriteTargets (:392); MOCK_FANOUT_ROLES (:849) untouched. parseMarkers reads prompt then system prompt (:808-822). Flow nodes never call runClaude. Offline completion of all-custom graph holds. MOCK_STRATEGY additive — keep.

## 5. Prompt-parity pins
Mechanism: inline literal assert.ok(prompt.includes(LITERAL)) lists in test/graph-prompt-parity.test.mjs, literals pasted from dev phases.mjs, driven by REAL agents/*.meta.json through buildAgentPrompt (pure) + one runAgentExecution round-trip under claude:{mock:true} asserting r.prompt === buildAgentPrompt(ctx). No golden files. Keep dev's phases-*.test.mjs until kill-list.
Shared pins: `# Task: <displayName>`; `Project directory (your cwd): <dir>` (:471); `Pipeline directory (shared artifacts): <dir>` (:494); legacy skills sentence (:481-483); `## Original request` (:457) for task-wired (clarify, planner, workspaceScanner) + wantsRequest (refiner, reviewer, planReviewer), attachments block (channels.mjs:279-286) task-wired only, `## Upstream input\n\nYour input is the output of the preceding step(s); the file paths to read are named below.` (:458) for the rest — matrix identical to taskHeader :452-455; `## Fan-out ENABLED — parallelize your research` … `Sub-agents are strictly READ-ONLY investigators: YOU write every artifact.` (:132-144) iff fanOut; RESUME_HEADER (:330-335); `## Asking the user (enabled)` + `MOCK_ASK:` (:366-378); verdict contract 3 lines (:877-879) iff verdict; MOCK_ROLE/MOCK_CYCLE/MOCK_OUT/MOCK_JSON/MOCK_IN per table; every allocated output on an absolute `- Write **<port>** to:` line.
Per builtin:
- clarify :502-524 — `Identify the decisions you cannot safely resolve from the task text or the real codebase`, `2 to 4 options`, `up to 8 questions`, `write { "questions": [] } to that same path`, `MOCK_PRIOR: 0`; dropped: round-2 `## Already answered — DO NOT ask these again` (:505-511).
- planner :651-676 — `Write a complete, build-ready implementation plan. It MUST contain concrete code snippets for the features and MUST end with a "## Clarifications (Q&A)" section`, REVISE block :655-660 as revise directive, `## Clarifications already answered` + `_No clarifying questions were asked._` (:1270) when unbound, `MOCK_ROLE: planner-plan`, `MOCK_BASE`.
- refiner :684-712 — `Read the current plan, critically review it INCLUDING its code snippets, then write an improved version and a machine-readable review.`, `Mark a finding critical/major only if it must be fixed before implementation.` (:706-707).
- decomposer :712-745 — `tracer-bullet vertical slices grouped into ordered phases. Within a phase, tasks must be parallel-safe and edit DISJOINT files`, `Write each task file under: <pipelineDir>/tasks/ (name them p<phase>-t<n>-<kebab-title>.md)`, `The manifest shape is { "phases": [ { "ordinal", "tasks": [ { "id", "title", "file" } ] } ] }. Use id "p<ordinal>t<n>" and a pipeline-dir-relative "file" path.`, `MOCK_TASKS_DIR`.
- implementer :787-810, :822-850 — fix arm `Address EVERY critical and major issue in the review below, then re-run the tests. Follow the plan; deviate only if something does not work at all.`; slice arm `Implement the task below using TDD (red-green-refactor). The TASK file is a self-contained vertical slice and is AUTHORITATIVE`; implement arm (:808-810) otherwise; exactly ONE arm; `Work inside the project directory (your cwd). Commit nothing; just edit files and tests.` (:836); `## Parallel siblings — shared working tree` + rules 1–4 for slices with phase-mates (:762-790).
- reviewer :852-899 — `Review the git diff of what was implemented against the plan. Write a human-readable review markdown AND a machine-readable review JSON.`, ref variant `Inspect the diff with \`git diff <ref>\` (the orchestrator's pre-implementation checkpoint) and \`git status\` in your cwd. New/untracked files are intent-to-added` (:859-862) and no-ref variant (:863-866).
- planReviewer :901-940 — `Review the implementation PLAN against the original request and the real codebase. Do NOT rewrite the plan.`, `only critical/major block (the planner then revises).`
- workspaceReviewer :944-990 — `Review what was implemented across the member projects against the plan. Write a SINGLE human-readable review markdown AND a SINGLE machine-readable review JSON.`, `## Workspace fan-out — one reviewer per touched project` (:216), UNION line (:967-969). Additive deviation: sidecar fanOut:true also renders `## Fan-out ENABLED` which runWorkspaceReviewer never did — tolerate or flip sidecar to fanOut:false + workspaceFanOut:true.
- manualTestsChecklist :1063-1100 — single-project sentence :1085-1087 and detached variant `your cwd is the worca-cc run root, not a repository, so inspect each member on its own` (:1079-1080) + `- **<name>**: \`git -C repos/<key> diff <ref>\`` (:540-547).
- manualWebUiTesting :1108-1150 — `Execute the manual test checklist against the running web UI using the Playwright tools. Write a human-readable result markdown AND a machine-readable review JSON.`, `(a failing manual case is at least major)`; allowedTools contains every mcp__plugin_playwright_playwright__browser_* from frontmatter.
- workspaceScanner — no v2 prompt; runWorkspaceScan :992-1060 untouched, `MOCK_ROLE: workspace-scan`.
- generic :1160-1250 — `(none — work from the request above)`, `(none — report your findings as your final message)`, base sentences :1207-1209 / :1240-1242 (keyed by runnerType).
Legitimate changes: `## Inputs`/`## Outputs` → `## Ports (this run)` + `### Inputs`/`### Outputs` with `- **<port>** (<type>) -> <path>` rows; titles = displayName; `Plan: <path>` lines become port rows; new `Mode: <port>` line; MOCK_STRATEGY; clarify round-2 block gone; refiner `Write the refined plan to:` collapses into shared-path write line.

## 6. Workspace parity
- FANOUT_ELIGIBLE (:139, applied :504-511) → _adoptResolvedGraph forcing fanOut=true where meta.workspaceFanOut (old:orchestrator.mjs:1738-1748); sidecar set {planner, refiner, implementer, planReviewer, workspaceReviewer} equals v1 set.
- Substitution workflows.mjs:399 → generic workspaceVariants(reg) (old:workflows.mjs:263-345): variant must be scope:'workspace-only', signature (id,type,required,loop,expands per input; id,type,when per output; verdict presence — NOT as/filename/store) deep-equals target, asserted on EVERY resolve; winner builtin>user>plugin; legacy per-role config keyed by AUTHORED key.
- Scanner: placeable:false (V4, palette filter, resolveGraph throw old:workflows.mjs:451) replaces literal throw at :402; runWorkspaceScan :992 + workspace-scan.mjs untouched.
- Prompt blocks: buildSystemPrompt(..., ctx.workspace) → `## Workspace Context` (:161-166); taskHeader → `## Workspace projects` + run-root cwd lines (:462-470, :498-530); workspaceFanOutDirective(meta.workspaceStrategy, ctx.workspace, {relative}) with relative = !!ctx.runRoot && workspace.projects.length>0 (:82-92) — pinned by test/phases-workspace.test.mjs:109-220.
- as:'worktree' on reviewer.done/workspaceReviewer.done: §3(iii); on detached workspace renders per-member `git -C repos/<key> diff <ref>` lines (:540-547) — additive for workspaceReviewer.
- Mock: workspaceWriteTargets (:392, :1338-1345) via runOpts unchanged.

## 7. Risks
1. Coexistence test churn — agent-registry-schema-v2.test.mjs:90-99 must change; add "ports ↔ channels consistency" test for the 11 builtins until kill-list.
2. Prompt-byte regressions the old branch shipped (§0 item 2) — pin list is the contract; single-directive rule + three token substitutions ({pipelineDir}, {cycle}, {diffInstruction}) are the minimal generic machinery.
3. Tools regression (§0 item 1) — pin allowedTools for manualWebUiTesting.
4. MOCK_WRITER_ROLES must be exported before any sidecar pins mockRole; keep structural switch-audit test.
5. Recovery-prompt rejection on slice failure (4fcb9855) is orchestrator-side.
6. Ask ids (clarify-<nodeId>-<ordinal>, questions-<executionId>-r<n>) + questions filename rename — check clarify-node, orchestrator-questions, workflows-questions pins.
7. node.defaults PATCH route vs node.config — resolver must fill nodeCtx model/effort/fanOut/askQuestions from config (old:workflows.mjs:462-476).
8. Verdict-kind for custom verifiers = <stem> — same as dev.
9. _checkCostLimits per launch (not per step) stricter than dev — intended, document.
