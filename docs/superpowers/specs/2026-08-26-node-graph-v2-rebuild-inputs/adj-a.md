# Agent A — Engine coexistence & cut-over (adjudication, 2026-08-26)

## 1. Orchestrator split (src/core/orchestrator.mjs @ dev e6968e15)
HARNESS (engine-agnostic): constructor :234 (except this._runners :306 + 'wf_default' default :291), getState :400, answer :411, stop :423, pause :446, _checkPause :464, run() shell :474–808, resume() shell :810–1076, _resolveSingleBranches :1078, _setupRunRoot :1097, _resolveGuardrails :1194, _assembleContext :1228, _recordCapabilities :1313, _resolveMemberBranches :1352, _buildWorktreeGraph[All] :1378/:1419, _teardownWorktree[All] :1458/:1509, _teardownRunRoot :1575, _recordRunWarning :1706, _snapshotRetained :1722, _recordCommitFailure :1744, _commitWork :1814, _checkCostLimits :2582, _pauseForCost :2610, _recover :2624, _enqueueRecoveryPrompt :2650, _enqueueAsk :2663, _backoff :2672, _recoveryNonce :2690, _workspaceChannel :2752, _reposCtx :2773, _ask :2834, _collectExtras :2952, _ensureGitCheckpoint[For|All] :3412/:3566/:3595, _buildResults :3464, _reportToSource :3551, _stageWorkingTree :3625, _excludePathspecs :3655, _git :3667, _loadAgentPrompts :3700, _writeClarifyAnswers :3717, _deriveBaseName :3732, _checkAbort :3738, clocks :3819–3855, _setStatus :3855, _log :3865, _artifact :3881, _emit :3903, _kickoffTitleGeneration :3916, _persist :3947, heartbeat :3957/:3968, _completePaused :3974, _recordStep :3757 (bookends).
V1-ONLY (kill list): FANOUT_ELIGIBLE :139, decomposedTaskNode :190, _dispatch :1961, _buildResumePoint :2141, _runStep :2176, _persistDecomposition :2228, _runDecomposedImplement :2249, _runDecomposedTask :2349, _runClarifyNode :2704, _bindNodeIo :2726, _publishNodeIo :2787, _loopFired :2807, _reviewOf :2819, _gate :2894, _phaseCtx :2903, _stepKeyFor :2944, _phase :3746 (except bookends).
SHARED-BUT-SHAPE-CHANGES (stepKey → executionId): _preflightAgentKeys :1923, _logStepFailure :2390, _runNode :2403 → _execute, _runNodeAttempts :2437, _runOnce :2467, _primeQuestions :2489, _questionsPath :2502, _questionsLoop :2517, _pauseForLimit :2572, _nodeCtx :2971 → _execCtx, _nodeStep :3027 → _execStep, _onAgentEvent :3074 + sub-agent reducers :3231–3412 (keyed attr.stepKey; sub_agents.step_key/step_index/cycle db.mjs:352), _recordCost :3796.
Ten v1 seams inside harness shells: resolveWorkflow :492–497, FANOUT forcing :505–511, _preflightAgentKeys(plan) :514, buildStepperManifest :515, collectRequiredSkills(registry, plan) :676 (skills.mjs:111–113 walks plan.steps), _dispatch(plan) :706, pre-dispatch pause literal {version:1,kind:'boundary'} :721–733, rp.version !== 1 :820, rp.bus?.code?.baseRef ≈:890–900 + rp.bus?.workspace?.projects ≈:913, plan re-resolve + _dispatch(plan,{resume:rp}) ≈:1052–1064.

VERDICT: base class `src/core/run-harness.mjs` `class RunHarness extends EventEmitter` = HARNESS set moved verbatim; six hooks:
1. _resolveTopology(registry) → { manifest, agentKeys:Set } (replaces :492–515; base stamps state.stepper, calls _preflightAgentKeys(agentKeys) + collectRequiredSkills(registry, agentKeys) — one-line change skills.mjs:113).
2. _engineRun({ resume }) → 'done'|'paused' (replaces :706 and ≈:1064).
3. _enginePrePausePoint() (replaces :721–733).
4. _engineRehydrate(rp) → { checkpointRef, memberWorktrees:[{projectKey,worktreeDir,graphInstruction}], plan? } (replaces :820 + three rp.bus reads).
5. _bookend(name, status) — base emits preflight/done phase + _recordStep for both engines (:519, ≈:714).
6. constructor hook for this._runners (v1 only).
`orchestrator.mjs` keeps createOrchestrator = class Orchestrator extends RunHarness (V1-ONLY + v1 SHARED-shape). NEW `src/core/graph/orchestrator.mjs` = class GraphOrchestrator extends RunHarness, createGraphOrchestrator, copy-adapted SHARED-shape (~250 lines, named so P7 deletes the v1 twins).
Why: old branch rewrote in place (3809 lines) → unmergeable; full copy = two 2000-line harness twins diverging on every harness fix (492387c1 touched run() stopped/error branches). Oracle for the P1 move: 53 test files import orchestrator.mjs; 16 orchestrator-* suites + pause-resume-e2e + server-pause-resume + dispatcher (calls _dispatch directly, as does orchestrator-questions).
KEY: state.steps[] STAYS the execution ledger in v2 (key = executionId, phase = agentKey, cycle = ordinal, + kind/ordinal/taskId/trigger/result) as old:orchestrator.mjs:1904 did → _recordCost, clocks, _ask's _runningStepKey, writeState→pipeline_steps (artifacts.mjs:1011–1020), sumStepCosts, stats.mjs:39, readPipelineForResume :1268, _buildResults unchanged/shared. NO pipeline_steps.execution_id column: `key` IS the executionId.

## 2. Dispatch by template version
ONE module `src/core/engine-select.mjs`: selectEngine({ templateVersion, resumePointVersion }) → 'v1'|'graph'; createOrchestratorFor(opts) reads opts.resume?.resumePoint.version FIRST (authoritative for resumes), else readWorkflow(opts.workflowId).version. Sites: POST /api/run (row read at ui/server.mjs:1062 — reuse; factories ≈:1143 workspace / ≈:1210 single), resumeRun ≈:1560, CLI run :1526 (currently throws mid-run via workflows.mjs:373), CLI cmdResume :809. Orchestrator.resume keeps rp.version !== 1 :820; GraphOrchestrator.resume throws on !== 2.
wf_default during coexistence: DEFAULT_WORKFLOW (workflows.mjs:91) stays v1 until the break. src/core/graph/builtin-workflows.mjs exports GRAPH_DEFAULT_WORKFLOW with FINAL id wf_default, but readWorkflow :277 serves it only under alias id `wf_default_v2` until the break; listed in GET /api/workflows :3116 as "Default (graph)" from P4. No env flag. The 21 wf_default literals in app.js, server :1061/:3203/:3647, CLI :213 untouched until the break.
Per-consumer with a v2 row ({id,name,version:2,domain,origin,nodes,wires,canvas?,createdAt,updatedAt}, flat, from new workflows.graph column):
- rowToTpl :206 / readRaw :225 version-aware. writeWorkflow :239 stays v1; writeGraphWorkflow in graph/workflows.mjs.
- resolveWorkflow :371 throws "template is a graph — runs on the graph engine" for v2; resolveGraph reads config_workflow_nodes by nodeId (unchanged) and REUSES config_workflow_feedbacks with fb_id = wireId (db.mjs:221–227 PK = overlay key; setFeedbackCycles :674 / resolveRunConfig :747 unchanged) → NO config_workflow_wires table; re-seed overlay migration = UPDATE … SET fb_id = ? via FB_WIRE_MAP.
- setWorkflowNodeDefaults :303 / PATCH /api/workflows/:id/defaults :3177 reject v2 rows ("edit in Composer").
- POST /api/workflows :3136 branches on body.version === 2 → validateGraph + writeGraphWorkflow. DELETE :3200 neutral.
- loadWorkflowsInto :3135 fine; renderWorkflowConfig :3228 → buildNodeConfigRows :2669 / buildFeedbackRows :2813 guard Array.isArray(workflow.steps) → zero rows until P4 adds v2 branch; composerRenderList :2561 hides v2 rows (P3), v1 composer dies P4.
- PATCH /api/config :2751 neutral. GET/POST /api/config legacy per-role untouched; after break resolveGraph applies stepsCfg[node.key] for wf_default like resolveWorkflow :380/:405.
- Ask: shapeWorkflow catalog.mjs:18 derives steps (one group per condensation-topo rank, agent nodes) + feedbacks (loop wires {id,from,to}); proposal.mjs:104–106/:160 unchanged; follow.mjs:60 → shim.
- agent-store.mjs:133 delete-guard + plugin-workflows.mjs:160–178 referencedPluginAgents walk nodes[].key too. removePluginWorkflows :105 neutral.
- importPluginWorkflows :44 + plugin-manifest.mjs:436–447: coexistence accepts both; at the break v1 files `skipped` with "built for the v1 pipeline format — rewrite as a graph (version: 2)" in Plugins view. WORCA_PLUGIN_API STAYS 2 (bump to 3 would auto-drop every installed plugin).
- command-router.mjs:184 reads state.phase/steps → shim.

## 3. Events/state v2 + shim
exec: { nodeId, executionId, kind:'cycle'|'task', ordinal, status:'start'|'done'|'error'|'paused'|'skipped', agentKey:string|null, trigger:{ wireIds[], freshPorts[] }, phase, taskId?, parentExecutionId?, costUsd, result? }; executionId = x:<nodeId>:<ordinal>[:p<P>t<T>]; flow nodes agentKey:null, kind:'cycle'; End done carries result:{type,path?,value?}.
token: { seq, from:{node,port}, to:[{node,port,wireId}], type, path:null|string, forced, firedAt, sourceExecutionId }.
question: + wireId?, executionId?. log/artifact/subagent/stepskills/stepgraphify: + {nodeId, executionId}; stepskills/stepgraphify keep cycle (= ordinal) (app.js:1795/:1805 key nodeId|cycle).
state: + engine:2, active:[{nodeId,executionId}], result, endReached; steps[] = ledger; NO separate executions[]/graph copies — resolved graph lives in state.stepper (manifest v2, persisted pipelines.stepper db.mjs:252). phase/cycle scalars STAY populated (shim).
Manifest v2: { version:2, graph:{ nodes:[{id,kind,key,label,color,sub,model,effort,uiPhase,ports:{inputs:[{id,type,loop,required}],outputs:[{id,type,when}]},x,y,config}], wires:[{id,from,to,loop,maxCycles?}] }, bookends:{preflight:true,done:true}, steps:[derived cells], feedbacks:[derived loop wires] }.
Consumers: server EVENT_NAMES :251 += exec, token (keeps phase); wireRun ≈:541 sets entry.status='running' on exec; summarizeRuns :455 passes stepper; results.mjs:60,88 reviews.cycle → writeReview(id, kind, ordinal); log-filter.mjs:16–78 facets cycle/steps(stepIndex) — v2 log attrs carry cycle:ordinal, no stepIndex; follow.mjs:60 shim → ported P7; command-router.mjs:184 shim → P7; CLI attachAndDrive :375 + phaseLabel :245 shim → P5; app.js case 'phase' :683 → onPhase :1307 → advanceRun :927 → locateInManifest ≈:893 nodeId-first; manifestFor :876 needs stepper.steps (else CLIENT_DEFAULT_STEPPER :859); buildRunGraph :1071 reads steps[].nodes[].{id,label,color,uiPhase,cycles,model,effort} + feedbacks[].{from,to}; onState :1711–1727; stepStatusByKey :1585; phaseKey :4261/:13953/:14000; history histNodeCycle :10606 / histReachedCell :10653.
SHIM rules (GraphOrchestrator, order exec → phase → state): phase = manifestNode.uiPhase (UI_PHASE[key] || key; map workflows.mjs:385–390 moves into manifest builder; flow nodes uiPhase = kind); cycle = ordinal (task sub-execs: parent ordinal, nodeId = parent; no per-task cells); status 1:1 except skipped → no phase event; state.phase/cycle = last-started execution's (mirrors _nodeStep :3055–3056); derived cells = condensation-topo ranks; feedbacks = loop wires {id, from:node, to:node, maxCycles}; bookends unchanged. Pinned by test/graph-phase-shim.test.mjs; dies in P7 (bookends become exec {nodeId:'preflight'|'done'...}; phase leaves EVENT_NAMES). Cost ≈40 + ≈60 lines.

## 4. Resume/pause/stop/heartbeat
Snapshot (scheduler after every publish + at finish, old:scheduler.mjs:660–680,:757): { version:2, seq, graph, tokens, outputs, consumed, ordinals, wires:{[wireId]:{deliveries,allowance}}, ended, execs:[{executionId,nodeId,kind,ordinal,status,sessionId,taskId?,expandsPort?}], gates:[{wireId,nodeId,executionId,token,issues}], asks, gate, ask }. Resume point: { version:2, snapshot, nodes:[{nodeId,executionId,sessionId,completed}], planVersion, stepModels, workflowId, guardrailsId, checkpointRef, checkpointRefs, workspace:{projects:[...]}|null, pauseReason, toolInstruction, pipelineDir, pausedAt }.
Written on every onSnapshot; keep last CLEAN snapshot (drop once pauseRequested, old:1811–1815); state.resumePoint = _buildResumePoint(snap); _persist().
One-shot re-attach: readPipelineForResume :1268 returns steps with sessionId (pipeline_steps :1683) → executionId→sessionId for status==='paused'; scheduler reattach() (old:696–720) re-invokes execute per non-terminal exec with RECORDED args; _execute sets ctx.resumeSessionId iff present; _runOnce vanished-session re-run :2467–2486 reused. Composite task slices re-run whole (old:704–707).
Heartbeat/reconcile base-harness free: _startHeartbeat :3957, _stopHeartbeat :3968, claimPipelineOwnership, HEARTBEAT_INTERVAL_MS artifacts.mjs:1101, reconcileStaleRunning :1229. Parametrize orchestrator-heartbeat (2), -partial-diff (10), -pause (4), -resume (2), -session-capture, -guardrails over both factories in P3.
Pause = cancel in-flight (v1 parity): pause() :446 sets pauseRequested, aborts pauseAbort, rejects open ask (:456–460); scheduler pause() stops launching; ctx.signal = AbortSignal.any([abort, pauseAbort]) (:3006); _execute marks 'paused'; scheduler finishes 'paused' when running.size===0. Held gates persist + re-asked on resume; answered questions re-injected by _primeQuestions :2489.
Stop → partial diff free: stop() :423 → abort → scheduler abort() → finish('error') → _runGraph throws → base catch :753–773 (_buildResults({stage:true}) + _reportToSource, 492387c1).
Startup sweep of paused v1 rows: NOT needed. At P7: ResumeError(409, {code:'ENGINE_RETIRED', error:'paused before the graph engine rework — not resumable'}) in engine-select + resumeRun :1497 + CLI cmdResume :749; History keeps honest "paused" row, Resume disabled with reason, discard route (resolveRunScope :1725).

## 5. Plan boundaries (7 plans)
P1 Harness split: NEW src/core/run-harness.mjs, src/core/engine-select.mjs (v1 for everything); orchestrator.mjs shrinks to v1 class; skills.mjs:113 accepts key set. Tests: existing unchanged (oracle) + run-harness-hooks, engine-select.
P2 Pure graph core, no callers: NEW src/core/graph/{ports,validate,scheduler,fixtures,builtin-workflows,seed-templates,workflows}.mjs; DB V23 = ALTER TABLE workflows ADD COLUMN graph TEXT + INCREMENTAL_COLUMNS db.mjs:732 (no re-seed). Meta v2: 11 builtin sidecars GAIN metaVersion:2 + inputs/outputs + capability fields while KEEPING consumes/produces/connectsTo/uiPhase/loopSource (both engines read same files until P7; normalizeMeta :189 passes both; v1-skip rule deferred to P7). Tests: graph-ports/validate/scheduler, seed drift guard (8 graphs 0/0 + pin counts), workflows-db v2 round-trip, agent-registry-v2-meta.
P3 GraphOrchestrator + executor + dispatch + shim: NEW graph/{executor,orchestrator}.mjs; engine-select routes v2; /api/run + resumeRun + CLI use createOrchestratorFor; EVENT_NAMES += exec/token; wireRun status; wf_default_v2 alias; POST /api/workflows accepts version:2; v1 composer list hides v2 rows. Tests: orchestrator-graph (mock e2e alias + 7 seeds), saved-pipeline-parity (LIVE v1 vs v2 on identical mocks — dual-engine), harness suites parametrized, graph-phase-shim, mock-graph.
P4 Composer v2 + run-setup (before the break): NEW ui/public/graph/*; index.html:1062–1115 replaced; v1 composer DIES here (read-only "legacy · runnable until the graph cut-over" item in saved list); run-setup buildNodeConfigRows/buildFeedbackRows v2 branch, thumbnail, picker lists "Default (graph)". Deleted composer-core.mjs; 8 ui-composer*/composer-ui suites retired, ~14 ui-graph-* added.
P5 Run monitor v2 + Agents/agent-gen v2 + plugins + CLI parity: run-decor (Running Detailed card, #running/<id>, #history/…), log-filter execution dimension, v1-row chip strip; Agents port editor + agent-gen v2; plugin v2 import + manifest check + Plugins-view message; CLI exec rendering when engine===2.
P6 THE BREAK: DEFAULT_WORKFLOW → v2 graph id wf_default (v1 constant kept as LEGACY_DEFAULT_WORKFLOW for audit); DB V24: re-seed 7 in place (version=2, graph, steps='[]', feedbacks='[]'), config_workflow_nodes.node_id via NODE_ID_MAP, config_workflow_feedbacks.fb_id via FB_WIRE_MAP (incl. wf_default's), ARCHIVE remaining v1 rows (archived_at, archived_reason on workflows; listWorkflows :286 filters; readWorkflow null → 400 "archived"; audit line), plugin v1 files skipped; dispatch default v2 (server :1061, CLI :213); POST /api/workflows rejects v1; setWorkflowNodeDefaults v2 → node.config. Paused v1 runs still resume on retained v1 class. Tests: db-migrate-v24, api-workflows v2 default, e2e mock wf_default, smoke.
P7 Kill list: delete v1 class + spec §9 + f additions (channels.mjs, runners.mjs, nine run* builders, workflow-validator.mjs, rewriteStepperForDecomposition, v1 buildStepperManifest, FANOUT_ELIGIBLE, DEFAULT_SPEC/LEGACY_LABELS/UI_PHASE, v1 sidecar fields, EMBEDDED_AGENTS v1, CLIENT_DEFAULT_STEPPER, normalizePhase, phaseKey switches, CLI phaseLabel, follow.mjs phase handler, command-router.mjs:184, the shim, phase out of EVENT_NAMES, state.phase/cycle scalars); graph/orchestrator.mjs → orchestrator.mjs; engine-select collapses to ENGINE_RETIRED refusal. Tests retired: dispatcher, orchestrator-decompose, stepper-rewrite, channels*, runners*, bespoke phases-*, ui-stepper, ui-run-graph*, ui-phase-label; added v1-remnants-removed grep guard.
No feature flag anywhere.

## Product questions raised
1. "Default (graph)" in run-setup picker for everyone from P4 (chosen) vs id-only until P6.
2. P7 paused v1 runs: refuse 409 + discard (chosen) vs auto-flip at boot.
3. Plugin API stays 2 with per-file v1 rejection (chosen) vs bump to 3.
