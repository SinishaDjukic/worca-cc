# Node-Graph v2 REBUILD — handoff to the planning session

Written 2026-08-26 at the end of the design session (context-heavy; planning continues in a fresh session). Everything below is untracked under `docs/superpowers/` — never commit it.

## State
- **Spec (authoritative):** `docs/superpowers/specs/2026-08-26-node-graph-v2-rebuild-design.md` — 16 sections; §1 user decisions D1–D8; §2 the 8-plan series; §16 plan-writing conventions. Status: written, fresh-eyes-reviewed (findings applied — see the review log), **APPROVED by the user 2026-08-26**. Plan count decided: 8 documents, with a/b halves for P2, P5, P6, P8.
- **Base concept spec:** `docs/superpowers/specs/2026-08-10-node-graph-pipelines-design.md` (Amendment f at the end overrides earlier text). Rebuild spec wins on conflicts.
- **Inputs dir:** `docs/superpowers/specs/2026-08-26-node-graph-v2-rebuild-inputs/` — `decisions.md`, `dev-map.md` (dev structure with anchors), `pr359-postmortem.md`, `adj-a.md` (coexistence/cut-over), `adj-b.md` (shared core), `adj-c.md` (canvas engine), `adj-d.md` (run monitor), `adj-e.md` (migration/seeds), `adj-f1.md` (executor/meta/parity), `adj-f2.md` (plugins/agent-gen/agents-view/CLI), `plan-writer-brief.md` (common brief), `composer-proto/proto.html` (reference canvas prototype, 12/12 CDP checks green) + `composer-proto/measure/measure.mjs` (the CDP harness incl. the forced-frame recipe).
- **Old branch (borrowable):** `origin/worca-cc/v2-orchestrator-bfb6a0ed` is fetched; `git show origin/worca-cc/v2-orchestrator-bfb6a0ed:<path>`.
- **Memory:** `node-graph-v2-rebuild.md` (index line in MEMORY.md) summarizes the decisions.

## Next steps (writing-plans phase)
1. Confirm the user approved the spec (or apply their change requests first).
2. Invoke `superpowers:writing-plans`; produce the eight plans in parallel with writer agents (Opus; the key decisions are already adjudicated), each given `plan-writer-brief.md` + its per-plan section below. Output: `docs/superpowers/plans/2026-08-26-node-graph-v2-P<n>-<slug>.md`.
3. Refinement per the house recipe (`plan-refinement-fable-agents` memory): per plan an Opus anchor fact-check + an executed TDD dry-run in a clone (P1 and P2 run directly on dev; P3+ apply their predecessors' dry-run clones first, or defer the executed dry-run to just before that plan's execution when the predecessors are on dev) with a mutation audit, a Fable adversarial/spec-conformance critique, then a Fable fresh-eyes pass on the written v2 and re-execution of revised snippets. Recommended depth (ask the user): full chained dry-runs are expensive; default = dry-run P1+P2 now, fact-check + fresh-eyes for P3–P8 now, re-refine each later plan right before its run.
4. Execution: one worca orchestrate pipeline run per plan, in order; merge to dev between runs (each plan leaves dev green).

## Model split (user's standing rule — applies to every step above)
- **Fable 5 (max effort) for key decisions and critical thinking:** any design/contract decision that surfaces while writing or refining a plan (a conflict between two plans' interfaces, a spec gap, an ordering dependency, a semantics question) goes to a Fable ADJUDICATOR agent that decides and explains — never silently decided by an Opus writer, never bounced to the user unless it is a product preference. Also Fable: the cross-plan contract-consistency pass after the 8 writers finish, the adversarial/spec-conformance critique of each plan, the cold fresh-eyes review of each v2 plan, and the main-loop synthesis (edit manifests).
- **Opus for the mechanical bulk:** plan writers, anchor fact-checks, executed TDD dry-runs + mutation audits, seed/spec cross-checks, applying edit manifests.
- Ask the user only for product-level choices (UX preference, scope, data-loss trade-offs); engineering calls are adjudicated by agents and recorded in the plan's `## Clarifications (Q&A)` as "agent adjudication".

## Session split (decided with the user 2026-08-26)
- **Session A (fresh):** write ALL eight plans (parallel writers, one cross-plan contract-consistency pass), then fully refine P1 and P2 (both dry-run against bare dev). Plans P3–P8 are v1 drafts at this point — contract-complete, anchors valid for dev @ e6968e15 only.
- **Sessions B…I (one per plan, in order):** re-anchor + refine P<n> against the CURRENT dev (predecessors landed ⇒ executed dry-run is real; `app.js`/`orchestrator.mjs`/`workflows.mjs` anchors will have moved), run the pipeline, review, merge to dev. Never execute a later plan's original draft unrefined.

## Per-plan writer sections (paste into each writer's prompt with the common brief)
- **P1 Harness split + foundations** — spec §2 row P1, §3 (mount, purity guard, `constants.mjs`, `verdict.mjs`), §5.1 (HARNESS/V1-ONLY/SHARED sets, 6 hooks, oracle), §5.2 (`engine-select` v1-only), §10.2 seeds paragraph (copy `old:src/core/graph/seed-templates.mjs` + `builtin-workflows.mjs` verbatim; structural tests; `FB_WIRE_MAP` vs dynamic resolver pin). Inputs: `adj-a.md` §1, `adj-b.md` (mount + guard code), `adj-e.md` §3. Sentinel for P2: `export class RunHarness` in `src/core/run-harness.mjs`, `export const SEED_TEMPLATES`.
- **P2 Shared graph core + sidecars v2 + schema + store** (halves P2a / P2b) — P2a: §3 module table (all exports), §7.4 geometry constants/formulas, base spec §2 (V1–V21 as amended by f — embed the full rule list), §6 (meta v2 schema, dual-shape sidecars for the 11 builtins with the verified port table, `normalizeMeta` v2 merge, `validateMetaV2`, `registry-ports.mjs`, `MOCK_WRITER_ROLES` export, the `agent-registry-schema-v2.test.mjs:90-99` rewrite), seed drift guard against the real sidecars. P2b: §4 (storage, `rowToTpl`, `writeGraphWorkflow` id rule, `listWorkflows`, `readWorkflow`, `assertRunnableWorkflow`, overlays, `setWireCycles`, API deltas), §10.1 V23. Inputs: `adj-b.md`, `adj-f1.md` §1–§2 (table + registry coexistence), `adj-e.md` §1/§5, `old:src/core/graph/{ports,validate}.mjs`, `old:agents/*.meta.json` (port bodies), `old:agent-registry.mjs:63-330` (`readMetaV2` rules only), `old:ui/public/graph/{graph-geometry,graph-layout,thumbnail}.mjs`. Sentinel for P3: `export function validateGraph` in `src/shared/graph/validate.mjs`, `SCHEMA_VERSION === 23`, `MOCK_WRITER_ROLES` exported from `claude-runner.mjs`.
- **P3 Engine, no callers** — §5.3 scheduler (incl. composite `phase` calls, `freshPorts`, gate ask ids), §5.4 executor + prompt assembly + adapter contract, §5.5 mock, §6 parity pins (embed the literal list from `adj-f1.md` §5 and the drift list §0), hand-written v1 fixture graphs `test/fixtures/workflows-v1/<seedId>.json`. Inputs: `adj-f1.md`, base spec §3/§5, `old:src/core/graph/{scheduler,executor,fixtures}.mjs` (mind the §16 never-borrow gaps), `old:test/graph-{scheduler,executor,prompt-parity,decompose}.test.mjs`, `old:test/mock-graph.test.mjs`. Sentinel for P4: `export function createScheduler`, `export function runAgentExecution`.
- **P4 GraphOrchestrator + dispatch** — §5.2 (routing), §5.6, §5.7 (events + shim rules), §5.8 manifest builder, §5.9 ledger write path, `wf_default_v2` alias, `POST /api/run`/`/api/resume`/CLI via `createOrchestratorFor`, `EVENT_NAMES`, dual-engine parity suite, harness suites parametrized. Inputs: `adj-a.md` §2–§5, `adj-d.md` §1–§2, `old:src/core/graph/orchestrator.mjs` (if present) / `old:src/core/orchestrator.mjs`. Sentinel for P5: `export function createGraphOrchestrator`, `wf_default_v2` served by `readWorkflow`.
- **P5 Composer v2** — §7 in full (layout, pointer pipeline, geometry, ghost, wheel/fit, view model, modes, palette, inspector, undo/save, run-setup, verification incl. §12.3 caveat), §3 import convention. Inputs: `adj-c.md`, `composer-proto/proto.html` (reference implementation — the plan may lift its pipeline code), `measure.mjs` (CDP harness to adapt into `scripts/verify-composer-cdp.mjs`), `old:ui/public/graph/{composer-editor,graph-view,inspector,save-dialog}.mjs` (borrow selectively; avoid the postmortem's defects). Sentinel for P6: `ui/public/graph/view.mjs` exports `createGraphView`; `index.html` composer section rebuilt.
- **P6 Run monitor v2 + CLI** — §8 in full, §5.7 consumers, §7.8 modes static/monitor. Inputs: `adj-d.md`, `old:ui/public/graph/run-decor.mjs`, dev `app.js` anchors listed in `adj-d.md` §9, `adj-f2.md` §4 (CLI). Sentinel for P7: `ui/public/graph/run-decor.mjs` exports `decorFromState`; `src/cli/render.mjs` exists.
- **P7 Agents view + agent-gen v2 + plugin API 3** — §9 in full (+ `adj-f2.md` §1–§3 for the message texts, schema block, port-editor mockup/classes, fixture conversion). Sentinel for P8: `WORCA_PLUGIN_API === 3`; `agentFormRender` in `app.js`.
- **P8 The break + kill list + docs** — §10.2 V24 (backup, seeds, archive ALL v1 rows, overlay maps, active reset, sweep, audit, tests), `DEFAULT_WORKFLOW` flip + alias removal, dispatch default, `POST /api/workflows` v1 rejection, §11 kill list + docs, `v1-remnants-removed` guard. Inputs: `adj-e.md` §2/§4, `adj-a.md` §5 P6/P7 rows, `adj-f2.md` §5 docs list. Internal split point: commit the break tasks before any deletion task.

## Review log
- 2026-08-26 Fable fresh-eyes review of the spec: 44 findings, ALL applied (1 critical: P2's seed drift guard needed the v2 sidecars → sidecars/registry moved into P2a; majors: `wf_default_v2` alias remap at V24 + resume never re-reads the row, sweep covers `interrupted` v1 runs, shim cells' exact `buildStepperManifest` shape, `execution_id` write/read rule, v1 fixture graphs for the parity suite, Ask catalog lists the alias row, plan sizing → halves P2/P5/P6/P8; plus ~30 minors: anchor corrections, questions-file name, `exec.phase` meaning, `freshPorts`, `stopped` stamping, `/api/agents` timing + `?all=1`, `?archived=1`, `writeGraphWorkflow` id rule, gate ask ids, `known/ported`, auto-mode clarify answer `[]`, artifact route `{rel,text}`, fixture helper names, test file names, never-borrow list). The reviewer's verdict after fixes: ready to plan from.
- Plan count: user chose 8 documents with a/b halves (2026-08-26).
- Prototype measurement: 12/12 PASS (spec §12.3); CDP harness caveat recorded in memory `cdp-layout-verification`.
