# Adversarial + spec-conformance critique (Fable 5, max effort) — plan <PLAN> (node-graph v2 <N>), worca

You are a hostile senior reviewer. Read IN FULL: the plan `<PLAN>`; the rebuild spec `/Users/denislavprinov/Develop/worca-cc/docs/superpowers/specs/2026-08-26-node-graph-v2-rebuild-design.md` (authoritative; your plan's sections: <SECTIONS>); the base spec `/Users/denislavprinov/Develop/worca-cc/docs/superpowers/specs/2026-08-10-node-graph-pipelines-design.md` (Amendment f at the end wins); the adjudications <ADJ> in `/Users/denislavprinov/Develop/worca-cc/docs/superpowers/specs/2026-08-26-node-graph-v2-rebuild-inputs/`; the cross-plan contract sheet `<SCRATCH>/reports/xplan-manifest.md` §A (already applied to the plans); the neighbour plans this plan hands off to: <NEIGHBOURS>. Repo `/Users/denislavprinov/Develop/worca-cc` (dev @ e6968e15) is READ-ONLY — open real files whenever a claim hinges on them; you may clone into `<SCRATCH>/critique-<N>/` to probe (timeout-wrapped; never `npm test` in the repo; never edit repo files; never git add/commit/push/stash/clean).

Attack the plan on:
1. SPEC CONFORMANCE, section by section: every requirement in the listed spec sections → the task that implements it, or a GAP. Every deviation from a locked decision (D1–D8), from Amendment f semantics, from the §16 never-borrow list, from §3's export table. Quote the spec line and the plan line.
2. CONTRACT BREAKS with consumers: what P<N+1>… will import/read/expect (exports, signatures, event/state fields, message strings, sentinels) — does the plan ship exactly that? (Use the neighbour plans + contract sheet.)
3. CORRECTNESS of the embedded code and tests: logic errors, wrong semantics (e.g. V-rule exemptions, OR resolution, loop classification, readiness), off-by-one geometry, async/await misuse, ESM import cycles (`src/shared` purity; registry ↔ claude-runner edge), DB transaction/idempotency mistakes, security (static mount traversal, 404 tail, SAFE_WORKFLOW_ID), byte-identity of the harness move (P1) / prompt bytes (P3).
4. TEST QUALITY: vacuous or tautological assertions, tests that cannot go red, missing mutation-proofing for a rule, tests depending on task-ordering (importing a module created later), jsdom traps, live-network/live-claude leaks, fixture bugs (store keys, missing columns).
5. EXECUTABILITY by a zero-context implementer in a fresh worktree: placeholders, "see spec", ambiguous steps, missing imports, code that references symbols no task defines, Task 0/sentinel correctness, self-containedness, commit hygiene, the split point (if any).
6. v1 SAFETY: any behavior change to the live v1 engine/UI that the plan does not intend (P1: the oracle suites; P2: `normalizeMeta` round-trip via agent-store, the registry key set, `GET /api/agents` payload consumers, `POST /api/workflows` v1 path, schema pin sweep).

Output `<SCRATCH>/reports/<N>-critique.md` (Bash heredoc, chunks ≤ 6k): findings numbered `F<k>`, each with severity (CRITICAL = plan cannot execute or ships a wrong contract/semantics; MAJOR = will bite in execution or breaks a neighbour; MINOR), location (task/step), the problem, the evidence (spec/file quotes), and the CONCRETE FIX as plan-ready text (exact replacement code/test/wording). End with a verdict line: executable-after-fixes YES/NO and the list of CRITICALs. Final message = the CRITICAL+MAJOR list (one line each) + the path.
