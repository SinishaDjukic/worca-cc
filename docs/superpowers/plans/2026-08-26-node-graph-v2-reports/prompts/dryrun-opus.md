# Executed TDD dry-run + mutation audit (Opus) — plan <PLAN> (node-graph v2 <N>), worca

Work ONLY inside the clone `<CLONE>` (already `npm ci`'d, checked out at dev e6968e15) with `WORCA_HOME=<HOME>`. The repo `/Users/denislavprinov/Develop/worca-cc` is READ-ONLY (never edit, never git anything there; never `npm test` there). In the clone you may commit per task (never push). Wrap EVERY test/node command in `timeout 900`. No servers/browsers/screenshots/`open`; no non-exiting processes. Old branch is available in the clone as `origin/worca-cc/v2-orchestrator-bfb6a0ed` (run `git -C <CLONE> fetch origin worca-cc/v2-orchestrator-bfb6a0ed` if `git rev-parse --verify` fails).

<PREREQ>

Then EXECUTE the plan `<PLAN>` task by task EXACTLY as written (the tests, the implementation code, the commands, the commit messages) — you are the zero-context implementer. For every step record: what the plan predicted (`Expected:`) vs what actually happened (paste the decisive output lines). When a step cannot work as written, make the MINIMAL change that makes it work, and record it as a PLACEHOLDER RESOLUTION / DEVIATION in plan-ready text (the exact replacement the plan should carry). Run the full suite (`npm test`) only where the plan says (Task 0 baseline, designated checkpoints, the last task) and the named test files elsewhere. Record the baseline count and the final count.

After the last task: MUTATION AUDIT — for every NEW or MODIFIED test in the plan, deliberately break the thing it pins, ONE change at a time (delete the rule / flip the condition / drop the branch / remove the export / change the message text), re-run that test file, and record whether it went RED. Any test that stays GREEN under a mutation is a SURVIVOR — list it with the mutation and a proposed assertion that would catch it. Also audit the plan's guard tests for vacuity (a probe that fails at lookup before reaching the guard; `assert.doesNotThrow` around jsdom dispatch; helper defaults injecting the claim under test). Revert each mutation before the next.

Leave the clone IN PLACE at the plan's end state (do not reset; wave 2 reuses it). Output `<SCRATCH>/reports/<N>-dryrun.md` (Bash heredoc, chunks ≤ 6k): §1 per-task execution log (predicted vs actual, decisive output), §2 deviations/placeholder resolutions as plan-ready text, §3 mutation table (test · mutation · RED/GREEN · fix), §4 counts (baseline, final, tests added), §5 the clone's final `git log --oneline` and `git status --short`. Final message = §2 + §3 survivors + counts + the report path.
