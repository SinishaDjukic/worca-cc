# P1 refinement — EDIT MANIFEST (mechanical part; from P1-anchors.md + P1-dryrun.md). Applier: copy the CONTRACT-ALIGNED v1 file to `…-P1-harness-split-foundations-v2.md` and apply there.
File F = /Users/denislavprinov/Develop/worca-cc/docs/superpowers/plans/2026-08-26-node-graph-v2-P1-harness-split-foundations-v2.md (line numbers below are from the v1 file at draft time; locate by the quoted text).

## Reporter text (Node ≥ 22 `spec` reporter prints `ℹ tests N` / `ℹ pass N` / `ℹ fail 0`, never `# pass`)
- P1-M1 (L118): old `all three files green (\`# fail 0\`)` → new `all three files green (\`ℹ fail 0\`)`.
- P1-M2 (L919, whole line): old starts `` `Expected: # fail 0` (measured: 126 + 43 passing `` → new: `` `Expected: ℹ fail 0` (measured 2026-08-27 on a clone of dev `e6968e15` with the split applied: 156 tests, 0 fail). The real importer list is `{ grep -lE "from '[^']*orchestrator\.mjs'" test/*.mjs; grep -lE "import\(['\"][^'\"]*orchestrator\.mjs" test/*.mjs; } | sort -u` — 53 files (a plain `grep -l "orchestrator.mjs" test/*.mjs` prints 60: seven files mention the path only in comments); the full suite in Step 7 covers them all. ``
- P1-M3 (L1111): `# pass 7`, `# fail 0` → `ℹ pass 7`, `ℹ fail 0`.
- P1-M4 (L1232): `# pass 5`, `# fail 0` → `ℹ pass 5`, `ℹ fail 0`.
- P1-M5 (L1564): `` `Expected: # fail 0` — 22 tests `` → `` `Expected: ℹ fail 0` — 22 tests ``.
- P1-M6 (L1641): `# pass 3`, `# fail 0` → `ℹ pass 3`, `ℹ fail 0`.
- P1-M7 (L1644): old `` the purity test FAILS with `…/src/shared/graph/mutant.mjs: node: builtin`; after `rm`, re-run → `# pass 3`. `` → new `` the purity test FAILS with `…/src/shared/graph/mutant.mjs: non-relative import "node:path"` (the relative-import assertion runs before the `node:`-builtin regex; both pin the same rule); after `rm`, re-run → `ℹ pass 3`. ``
- P1-M8 (L1779): `# pass 4`, `# fail 0` → `ℹ pass 4`, `ℹ fail 0`.
- P1-M9 (L2403): `# pass 10`, `# fail 0` → `ℹ pass 10`, `ℹ fail 0`.
- P1-M10 (Global Constraints): append a bullet: `- Test output: Node ≥ 22's `node --test` uses the `spec` reporter even when piped — the summary lines are `ℹ tests N` / `ℹ pass N` / `ℹ fail 0`; every `Expected:` below quotes those. macOS ships no `timeout`; to cap a command use `perl -e 'alarm 900; exec @ARGV' <cmd…>`.`
- P1-M11 (Task 11 last step / any remaining `# pass`/`# fail` in the file): grep `'# pass\|# fail'` after applying — must be zero hits.

## Anchors
- P1-M12 (L1119, Task 4 Files): old `` `POST /api/run` (`ui/server.mjs:1062`, factories ≈`:1143`/`≈:1210`), `resumeRun ≈:1560`, CLI run `worca-cc.mjs:1526` and `cmdResume :809` keep calling `createOrchestrator` directly. `` → new `` `POST /api/run` (`ui/server.mjs:972`; the workflow row is read at `:1062`; factories `:1150` workspace / `:1203` single), `resumeRun` (declared `:1497`, its `createOrchestrator` call `:1560`), CLI run `src/cli/worca-cc.mjs:1526` and `cmdResume` (declared `:724`, its call `:809`) keep calling `createOrchestrator` directly. ``
- P1-M13 (L125): old `~60 class members and ~2000 of` → new `63 class members (of 96) and ≈2,590 of`.
- P1-M14 (L295): old `**The ten seams** (all inside the moved `run()`/`resume()` shells; `→` = the replacement text). These are the ONLY non-moved lines the review in Step 5 may show.` → new `**The twelve seams** (`→` = the replacement text): S1–S10 sit inside the moved `run()`/`resume()` shells; S11 is the constructor runner hook (the `this._runners` line, `:306`) and S12 the `_preflightAgentKeys` signature (`:1923`) — both applied by the script's blocks of the same name below. Together with the two import blocks, the two class lines, `BASE_HOOKS`/`V1_HOOKS`, `planAgentKeys` and the export lines, they are the ONLY non-moved lines the review in Step 5 may show.` — and ADD two table rows after S10 (derive their "from"/"to" cells from the script's `// ── S11:` and `// ── S12:` blocks, quoting the replaced signature lines): `| S11 | `:306` | `this._runners = { …defaultRunners… }` (the runner table) → `this._initRunners(opts);` (base constructor) |` and `| S12 | `:1923` | `_preflightAgentKeys(plan) {` + the `plan.steps` walk → `_preflightAgentKeys(agentKeys) {` over the iterable |` (exact text per the script).
- P1-M15 (L308, S10 row): old `` `:944`–`:945`, `:976`–`:994` `` → new `` `:945` (channelDefs) + `:952` (audit), `:981`–`:994` ``.
- P1-M16 (S1 row): old `the **8 lines** in the S1 block below` → new `the **9 lines** in the S1 block below`.
- P1-M17 (`_phase` deviation note near L142): `(**13** calls)` → `(**14** calls)`; `(**11** calls)` → `(**10** calls)` (keep bold markers as in the file).
- P1-M18 (Task 2 Step 1 prose): `asserts every expected name is found exactly once` → `asserts the member count (96) and that every expected name is present`; `(each asserted to match exactly once)` → `(each asserted to match exactly once; S6 and S9 exactly twice — see their `all(…, 2)` calls)`.
- P1-M19 (Task 1 Files L46): `:101-110` → `:100-110`.
- P1-M20 (Task 1 Step 1 Expected L86): old `with `actual: []` for the Set case` → new `followed by the `+ actual - expected` diff showing `+ []` (an empty actual array) for the Set case`.
- P1-M21 (Task 9 Step 1, L1796): old `` then `git show origin/worca-cc/v2-orchestrator-bfb6a0ed:src/core/graph/seed-templates.mjs | diff - src/core/graph/seed-templates.mjs && echo IDENTICAL` `` → new `` then write BOTH files from the code blocks embedded below (Steps 2–3) — that is what a no-network execution types — and verify the PLAN's copies against the branch (the check that can actually fail): `git show origin/worca-cc/v2-orchestrator-bfb6a0ed:src/core/graph/seed-templates.mjs | diff - src/core/graph/seed-templates.mjs && git show origin/worca-cc/v2-orchestrator-bfb6a0ed:src/core/graph/builtin-workflows.mjs | diff - src/core/graph/builtin-workflows.mjs && echo IDENTICAL` (run AFTER writing the files from the plan, never after a `git show > file`, which is a tautology). Measured 2026-08-27: the embedded blocks are byte-identical to both branch files. `` — and delete the two `git show … > src/core/graph/…` lines at L1794-1795 (or keep them as an OPTIONAL shortcut clearly labelled "only if the branch is present; the plan's blocks are authoritative").

## Review-as-move (BLOCKING fix)
- P1-M22 (Step 5, L913–916 — from `- [ ] Step 5: Review the move as a move:` through the end of the paragraph ending `and `BASE_HOOKS`.`): replace with:
```
- [ ] Step 5: Review the move as a move. `run-harness.mjs` is untracked, so git has nothing to pair the moved blocks with — stage its existence first (content stays in the working tree) and diff BOTH paths:
`git add -N src/core/run-harness.mjs`
`git diff --color-moved=zebra --color-moved-ws=allow-indentation-change -- src/core/orchestrator.mjs src/core/run-harness.mjs`
Expect ≈2600 moved-away / ≈2600 moved-to lines and ≈250 non-moved. Everything must render as moved except: (orchestrator) the new import block, `class Orchestrator extends RunHarness {`, the `V1_HOOKS` block, `planAgentKeys`, `export { isAbort, errStreamAttr };`, the nine helper signature lines that gained an `export ` prefix, and the twelve seam ORIGINALS; (harness) the file header, the import block, the twelve seam replacements S1–S12 and `BASE_HOOKS`. One-line comment banners, lone `}` and blank lines also show as non-moved — git's move detection needs ≥ 3 lines per block — that is noise, not an edit. (Measured 2026-08-27: 2612 removed / 2609 added render as moved.)
**Stronger cross-check (do this too — `--color-moved` is a heuristic):** compare `git show HEAD:src/core/orchestrator.mjs` against `cat src/core/orchestrator.mjs src/core/run-harness.mjs` as a MULTISET of non-blank lines, e.g. `diff <(git show HEAD:src/core/orchestrator.mjs | grep -v '^\s*$' | sort) <(cat src/core/orchestrator.mjs src/core/run-harness.mjs | grep -v '^\s*$' | sort) | grep '^[<>]' | wc -l`. Expect ~102 lost / ~188 gained, ALL accounted for by the two import blocks, the two class lines, the nine `export `-prefixed helpers, the twelve seams, `planAgentKeys`, the `export {…}` line, the harness header, `BASE_HOOKS` and `V1_HOOKS`. Any other line is a bug in the script — stop and re-derive.
```
- P1-M23 (Q&A P1-a, L2441): `` `git diff --color-moved` must show only the twel… `` — keep, but add `(after `git add -N` of the new file, both paths)`.

## Prose ↔ script sync (only the script's text lands)
- P1-M24: the prose `BASE_HOOKS` block and `V1_HOOKS` block shown before the seams table (the "Orchestrator's implementations (appended at the end of its class body …)" region, ~L197–290) differ from the script's template strings (L557–594 `const BASE_HOOKS = \`…\`` and L595–… `const V1_HOOKS = \`…\``): `registry` vs `_registry` param names, three dropped JSDoc lines, the shortened `_initRunners` comment, the `// (orchestrator.mjs:945 on dev)` comment. Replace the prose blocks' contents with the script's template text VERBATIM (without the backtick wrapper), and change the sentence "same text … if you change one, change both" to: "The blocks above are the script's `BASE_HOOKS`/`V1_HOOKS` strings verbatim — the script is the source of truth; edit it, not this prose."

## Survivor fixes (dry-run mutation audit — each validated green-unmutated / red-mutated)
- P1-M25 (Task 3 test code, the `run()` test): add `getDb` to the `db.mjs` import line of `test/run-harness-hooks.test.mjs` (the line that imports `_resetForTests`), and append at the end of the `run()` test body (before its closing `});`):
```js
  // The workflow field of the topology bag is what the audit line renders (P1-c).
  const audit = getDb().prepare('SELECT text FROM pipeline_events WHERE pipeline_id = ? ORDER BY id')
    .all(orch.pipeline.id).map((r) => r.text).join('\n');
  assert.match(audit, /Workflow: \*\*Stub\*\* \(wf_stub\)\./, 'the audit line comes from topology.workflow');
```
  (the stub engine in that test must return `workflow: { id: 'wf_stub', name: 'Stub' }` from `_resolveTopology` — if the plan's stub uses other values, use those in the regex.) Add a mutation line to the task's mutation list: "print `topology.workflow.id` where the name goes → RED".
- P1-M26 (Task 4, `src/core/engine-select.mjs` `createOrchestratorFor`): replace the bare `selectEngine(...)` call with
```js
  const engine = selectEngine({ templateVersion, resumePointVersion });
  // P4 routes 'graph' to createGraphOrchestrator(opts); until then every run is v1.
  const orch = createOrchestrator(opts);
  orch.engine = engine; // the decision the data made, observable by callers and tests
  return orch;
```
  and in `test/engine-select.test.mjs` add one assertion to each of the two factory tests: `assert.equal(orch.engine, 'v1', 'the factory records the selector\'s answer for the row it read');` (v1 row case) and `assert.equal(orch.engine, 'graph', 'the factory consulted the resume point, not just the row');` (resume-point v2 case). Update the task's Interfaces block: `createOrchestratorFor` → orchestrator with `.engine` = `'v1'|'graph'`. Mutation line: "gut the factory (no readWorkflow, no selectEngine) → RED".
- P1-M27 (Task 1, `test/skills-resolve.test.mjs` new Set test): append `assert.deepEqual(collectRequiredSkills({ ...registry, p: { requiresSkills: ['perChar'] } }, 'planner'), []); // a bare string is iterable but is NOT a key list` and a mutation line "remove the `typeof planOrKeys !== 'string'` guard → RED".
- P1-M28 (Task 8, `test/api-shared-static.test.mjs` test 3 traversal loop): add `assert.equal(res.status, 404, p);` beside the existing `doesNotMatch(body, /node:sqlite/)` so a 500 or an HTML fallthrough cannot pass. Mutation line: "widen the mount to `/src` → RED".
- P1-M29 (Task 7 prose): add one sentence after the test: "Test 3's escape-branch assertions have nothing to check at P1 (no `ui/public` specifier leaves the static root until P2's `model.mjs`); they are proven live by the injected-import mutations above and become load-bearing from P2."

## Not applied (recorded)
- ORCH_IMPORTS `resolve`/`basename` are unused after the split (dead imports) — left as is; removing them risks a ReferenceError for no behavioral gain. Optional cleanup for P8.
- Task 11 Step 4 (manual browser check) stays manual; a pipeline implementer records it as "skipped — verified by test/api-shared-static.test.mjs; Chrome execution checked by hand".

## Cross-plan overlap (xplan-manifest §B P1 = P1-E1…E8, applied IN PLACE before this manifest runs)
- P1-E1…E5 (A27): `BOOKEND_EXECUTION_IDS = Object.freeze(['x:preflight:1', 'x:done:1'])` in `constants.mjs` + its test import/pin + Interfaces line + Task 5 expected count (+1 ⇒ Task 5 suite = 10; final = BASELINE + 46). My P1-M* edits do not touch these lines; wave 2 re-measures totals (D7).
- P1-E6…E8 (A28): the `wf_clarify-implement` fb↔wire pairing is PINNED (`fb_0` = review loop `w9`, `fb_1` = refine self-loop `w5`) in the seed test + Q&A P1-q; the "V24 resolves dynamically" sentence is gone. The dry-run's documented survivor S-c therefore becomes RED after the xplan edit — wave 2 must confirm (add to the re-exec prompt).
- P1-E8 also records (Q&A P1-r) that P4 owns the telemetry hoist (A18) — nothing moves in P1.
- A17 confirms P1's hook shapes as executed — no P1 change.
- Critique findings (P1-critique.md) still to merge: CRITICAL/MAJOR items become P1-C<k> edits in the final manifest.

## Critique fixes (from P1-critique.md — take the EXACT code/text from that file's F-sections and from the probe files `scratchpad/critique-p1/run-harness-hooks-extra.test.mjs` and `scratchpad/critique-p1/graph-seed-templates-extra.test.mjs`, which ran green on the clone)
- P1-C1 (F1, CRITICAL) — APPLY all four parts: (1) script `BASE_HOOKS` JSDoc: `_resolveTopology` returns `{manifest, agentKeys, workflow:{id,name}}` ALL REQUIRED + the sentence; `_engineRehydrate` returns `{checkpointRef, memberWorktrees, plan?, audit}` with `audit` REQUIRED + the sentence. (2) S1 replacement gains the `engine hook contract: _resolveTopology …` guard line right after `const topology = await this._resolveTopology(registry);`; S8 gains the `engine hook contract: _engineRehydrate …` guard after `const rehydrated = await this._engineRehydrate(rp);` (see P1-C4 for the `await`). Add both guard lines to Step 5's allowed non-moved list (P1-M22 text). (3) Task 3: import `getDb`; add the `auditOf` helper; append the two `assert.match` lines to tests 4 and 6; add the contract test (stub returning `{manifest, agentKeys}` ⇒ `run()` resolves `{status:'error', error:/engine hook contract: _resolveTopology/}` and `calls.engineRun.length === 0`); mutations (e) and (f). (4) Task 3 count 7 → 8 (then 9 with P1-C2). P1-M25 (the dry-run's M40 audit assertion) is SUBSUMED by this — apply F1's version once, not both.
- P1-C2 (F2, MAJOR) — APPLY: the stub engine's `_engineRun` gains `this._checkPause();` as its first line (with the one-line comment); add the pre-pause test verbatim from F2; mutation (g) "S7 → `= null` must turn this test red". Task 3 count → 9. Do NOT add the optional v1 `orchestrator-pause` test (out of P1's byte-identical mandate).
- P1-C3 (F3, MAJOR) — APPLY: move the S12 block (`S12_FROM`/`S12_TO` + `body = once(body, S12_FROM, S12_TO);`) physically BEFORE the "The helpers v1 still needs become named exports" block so document order == execution order; keep the order note; add the two post-condition `die` guards immediately before the two `writeFileSync` calls (exact lines in F3).
- P1-C4 (F4, MAJOR) — APPLY: the `_engineRehydrate` JSDoc paragraph from F4 (pre-rehydration, outside the try, pure, may be async; restoration belongs in `_engineRun`); the S8 call becomes `const rehydrated = await this._engineRehydrate(rp);` (base throws synchronously, so Task 3's `assert.throws` on the base stays valid — say so in the task). Add to the Q&A: `- **P1-s** — When does `_engineRehydrate` run? → **At dev's `:820` version-gate position: before any state is rehydrated, outside the try; pure; async allowed. Engine restoration goes in `_engineRun({resume, rehydrated})` (agent adjudication, Fable critique F4 / xplan seam S3).**`
- P1-C5 (F5, MAJOR) — APPLY counts: Task 3 = 9 tests; Task 5 = 10 (xplan E4, already); Task 10 = 11 (P1-C8); Task 6 Step 5 gate → `BASELINE + 31` (list the per-task numbers: Task 1: 2, Task 3: 9, Task 4: 5, Task 5: 10, Task 6: 5); Task 11 Step 1 → `BASELINE + 49` (reference 3809 on a 3760 baseline) and the per-file list updated (`Task 3 → 9`, `Task 5 → 10`, `Task 10 → 11`); every such gate says "re-measured in the wave-2 execution; write the printed number".
- P1-C6 (F6, MAJOR cross-plan) — APPLY to Q&A P1-r: append `The move must treat SKILLS_MAX, skillLabel and mergeSkills as "used by both sides" (orchestrator.mjs:4369 `_testing`, imported by test/skill-capture.test.mjs): export them from run-harness.mjs and import them back, or move `_testing` with them.` (P4-side fix recorded in later-plan-notes.md.)
- P1-C7 (F7, MINOR) — APPLY: S1 comment → `hard-fail BEFORE the stepper is STAMPED / createPipeline / worktree (the manifest is built inside the hook, which tolerates unknown keys)`; add the `buildStepperManifest`-before-gate reorder to the plan's list of disclosed reorderings next to the `collectChannelDefs` relocation.
- P1-C8 (F8, MINOR) — APPLY: Task 10 test 11 = the two pins from `graph-seed-templates-extra.test.mjs` (`wf_default` maps derived from the REAL v1 `DEFAULT_WORKFLOW` via `import { DEFAULT_WORKFLOW } from '../src/core/workflows.mjs'`; `EXPECTED_FB` by loop role with `wf_clarify-implement: { fb_0: 'review', fb_1: 'refine' }` marked as the A28 convention). Task 10 count 10 → 11; Expected `ℹ pass 11`.
- P1-C9 (F9, MINOR) — APPLY: `test/api-shared-static.test.mjs` walk filters `/\.mjs$/` (mirror the purity test).
- P1-C10 (F10, MINOR) — APPLY: purity guard scans a comment-stripped copy for the token rules (`const code = src.replace(/\/\*[\s\S]*?\*\//g, '').replace(/(^|[^:'"])\/\/.*$/gm, '$1');`), raw source for specifiers; drop the "also never as a bare word in a comment" sentence from Global Constraints; keep mutation M28 but make it a bare `document` in CODE (not a comment).
- P1-C11 (F11, MINOR) — NOTE ONLY: add to the disclosed-deviations list: `the `rp.bus?.workspace?.projects` `.map` now runs outside the try (corrupt-point-only difference: a non-array `projects` rejects `resume()` instead of landing as status error)`. No code change (byte-identical mandate).
- P1-C12 (F12 a/b, MINOR) — APPLY: Task 2 Step 4 → "exactly two entries under `src/`" (the main checkout carries other untracked files); Task 11 Step 3 → `git diff e6968e15 -- ui/public/` (drop the root `index.html` pathspec). (c) branch-name case: SKIP (brief's lowercase stands).
- P1-C13 (F13 = my P1-M24) — APPLY ONCE: keep the script's `BASE_HOOKS`/`V1_HOOKS` as the only copy; upgrade the script's `BASE_HOOKS` JSDoc with the prose's richer lines (`manifest -> state.stepper (the UI snapshot); agentKeys -> the §9.4 preflight gate + the skills gate; workflow -> the run's audit line`, the "P8 turns these into `exec` rows" note, the 3-line `_initRunners` comment) PLUS the F1/F4 sentences; replace the prose blocks with "the hook text is the script's `BASE_HOOKS`/`V1_HOOKS` — do not retype".
- P1-C14 (F14, MINOR) — APPLY: add `builtin-workflows.mjs` header's "`fixtures.mjs` re-exports it as FIXTURE_DEFAULT" to the P8 comment-correction list in Q&A P1-p.
- P1-C15 (F15, MINOR) — APPLY: `createOrchestratorFor` uses `resumePointVersion == null` (not `=== undefined`) before reading the row.
- F16 — SKIP (A30 accepted `w_?`).

## Provenance line (add under the title, before the "For agentic workers" line)
`> **v2 (refined 2026-08-27, Session A):** v1 + cross-plan contract alignment (xplan A17/A18/A27/A28/A30) + Opus anchor fact-check (61 OK/15 DRIFT/7 WRONG) + executed dry-run in a clone (3760 → 3805, 40 mutations, 3 survivors fixed) + Fable critique (F1–F15 applied). Wave-2 re-execution + cold fresh-eyes follow; counts marked "re-measured" are written by that run. Anchors valid for dev @ e6968e15.`
