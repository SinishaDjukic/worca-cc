# OPEN CONTRACT POINTS from the 8 writers (verbatim from their final messages)

## P4 — GraphOrchestrator + dispatch (12 tasks, 2505 lines) — file 2026-08-26-node-graph-v2-P4-graph-orchestrator-dispatch.md
Sentinels consumed (Task 0 Step 4): `export function createScheduler` + `export function sliceExecutionId` in scheduler.mjs; `export function runAgentExecution` in executor.mjs; P1: `class RunHarness extends EventEmitter` in run-harness.mjs, `createOrchestratorFor` in engine-select.mjs, `GRAPH_DEFAULT_WORKFLOW`; P2: `resolveGraph` + `assertRunnableWorkflow` in workflows.mjs, `buildGraphManifest` + `kind: 'preflight'` shim cells in src/shared/graph/manifest.mjs, `classifyLoops`, `registryPortsFn`, `execution_id` in db.mjs; P3 fixtures test/fixtures/workflows-v1/wf_full.json.
Sentinels produced: `export function createGraphOrchestrator` (+ `export class GraphOrchestrator`, `export function resolvedFromManifest`) in src/core/graph/orchestrator.mjs; `readWorkflow('wf_default_v2')` → `{...GRAPH_DEFAULT_WORKFLOW, id:'wf_default_v2', name:'Default (graph)'}` via new exports `GRAPH_DEFAULT_ALIAS_ID` / `graphDefaultAliasTemplate()` in workflows.mjs.

Assumed neighbour exports / shapes:
1. `resolveGraph` return shape (P2b) — assumed `{ template, ports, nodeCtx }` with `nodeCtx[id]` = `{nodeId, kind, key, templateKey, meta, runnerType, agentFile, agentPrompt, promptHints, tools, config, model, effort, fanOut, askQuestions, awaitAll, duplicateKey}` (agents) / `{nodeId, kind, key:null, config}` (flow), per old:workflows.mjs:422-524. P4 assumes resolveGraph owns overlays + `workspaceVariants(registry)` substitution + the port-signature assertion + the `placeable:false` throw + the per-loop-wire `maxCycles` merge, and does NOT return `loops` (P4 computes `classifyLoops` itself).
2. `classifyLoops` field name — P4 uses `loops.loopWireIds` (spec §3) and `loops.launchOrder`; the old branch called it `loopWires`.
3. `buildGraphManifest(tpl, agentsByKey, {overlays})` — signature per spec §3, not the old branch's `buildGraphManifest(resolved)`. P4 assumes `agentsByKey` = the raw `loadAgentRegistry()` map, that the builder emits `graph.nodes[].uiPhase` (UI_PHASE moved out of workflows.mjs:385-390), `ports.await` as a boolean, and BOTH the v1-shaped `steps` shim cells and `feedbacks`. Task 5 Step 2 hard-verifies this and STOPs if missing. `overlays` semantics chosen by P4: `{nodes:{[nodeId]:{model,effort,fanOut,askQuestions,awaitAll}}, wires:{[wireId]:{maxCycles}}}` carrying EFFECTIVE (post-merge) values, not raw DB overlays. If P2 defined `overlays` as raw rows, one must change.
4. `buildGraphManifest` does not set `manifest.template` — P4 stamps `{id, name}` itself after the call (uses the REQUESTED id, i.e. `wf_default_v2`, which V24 remaps).
5. `manifestTemplate(manifest)` / `manifestPortsFn(manifest)` — assumed to return a v2 template (`{id,name,version:2,nodes,wires}`) and a `portsFn(node)`. Used only by `resolvedFromManifest`.
6. `createScheduler` options — P4 passes exactly `{template, portsFn, loops, execute, onEvent, onSnapshot, onGate, onAsk, maxParallel, log}` and calls `reattach(snapshot)` BEFORE `run()` (no `snapshot` ctor option, no `taskArtifact`; P4 supplies `taskArtifact` itself in `_runFlow`).
7. `onGate` / `onAsk` signatures — planner defaults: `onGate({wireId, nodeId, executionId, deliveryNo, issues}) → Promise<'another'|'continue'>`; `onAsk(payload) → Promise<answer>`. Gate ask id `gate-<wireId>-<deliveryNo>`.
8. `sched.run()` resolves `'done'|'paused'|'error'`; `sched.getState()` exposes `{active:[{nodeId,executionId}], wires:{[id]:{deliveries,allowance}}, tokens:{'<n>.<p>':{seq,type,path,firedAt}}, gate, ended}`. `_syncSchedulerState` degrades on partial shape.
9. `execute` args carry `parentExecutionId` for `kind:'task'` slices; also `args.trigger = {wireIds, freshPorts}`, `args.phases` on `composite:'finish'`, `args.phaseStatus` on `composite:'phase'`, `args.expandsPort` on `'expand'`, `args.signal` on slices.
10. `allocateOutputs({node, ports, executionId, ordinal, runCtx})` / `allocateVerdict({node, ports, ordinal, runCtx})` return `{[portId]:{path,...}}` / `{path,...}`; `runCtx.planVersion` is a FUNCTION the executor calls to tick `{vsuffix}`.
11. `RunHarness` is a NAMED export; P1 replaced the literal `rp.version !== 1` check with hook 4 (Task 6 Step 1 greps run-harness.mjs for `rp.version` and must find nothing).
12. `_preflightAgentKeys(agentKeys: Set)` lives on the base with the Set signature (spec hook 1), reused unchanged.
13. `assertRunnableWorkflow(id)` throws `{code:'NOT_FOUND'|'ARCHIVED', message}`; `readWorkflow(id, {includeArchived})` is P2b's signature.
14. `writeGraphWorkflow({id?, name, domain, origin?, nodes, wires, canvas?})` used by P4 tests to seed the 7 graphs.
15. P3 v1 fixtures named `test/fixtures/workflows-v1/<seedId>.json`, complete v1 templates (`{id,name,version:1,domain,steps,feedbacks}`); `seed-templates.mjs` exports `SEED_TEMPLATES` + `FB_WIRE_MAP` keyed `FB_WIRE_MAP[wfId][fbId] = wireId`.

Names/shapes P4 chose (in its Q&A):
16. **P1-boundary deviation (biggest):** Task 1 MOVES `_onAgentEvent` + `_recordSubAgentSpawns/_recordSubAgentFinishes/_recordSubAgentTelemetry/_recordSkills/_recordGraphify/_upsertSubAgent/_subAgentTransition/_recordCost` and 13 module helpers from orchestrator.mjs UP TO `RunHarness`, instead of copy-adapting ~340 lines into the v2 class as spec §5.1 "SHARED-BUT-SHAPE-CHANGES" implies. Rationale: they are `attr`-only and key on `state.steps[].key`; the sole change is `attr.stepKey = executionId`. Oracle: the ~25 sub-agent suites. Guarded by a Step-1 grep so it no-ops if P1 already did it. ADJUDICATE against §11 kill list (does not name these methods) and against P1's HARNESS set.
17. Task 1 also EXPORTS the pure helpers `isAbort, pauseErr, isPause, jsonClone, safeParse, firstLine, numOr, roundUsd, sumStepCosts, sumStepActive, clip, clipMiddle, normalizeClarifyAnswer` from run-harness.mjs (v2 needs clipMiddle, normalizeClarifyAnswer, sumStepActive, firstLine, jsonClone).
18. `export` added to `loadAgentFile` (workflows.mjs:47) so `_engineRehydrate` can hydrate prompt bodies/frontmatter tools on resume.
19. `_artifact(kind, path, attr)` emits `{kind, path}` plus only the present keys of `{nodeId, executionId, port}` — 2-arg v1 calls stay byte-identical.
20. `_log` gains `executionId` (placed between `nodeId` and `stepIndex`).
21. `nodeIdSafe` = `String(nodeId).replace(/[^A-Za-z0-9_-]/g,'_')`; file `questions-x-<nodeIdSafe>-c<ordinal>-r<round>.json`.
22. `QUIESCENCE_WARNING` text: `finished at quiescence — the End card was never reached, so this run bound no result` (P6 owns the UI banner copy — spec §8 banner says "finished at quiescence — End not reached").
23. Resume-point extra field `manifest` treated as REQUIRED (`_engineRehydrate` throws without it).
24. `resolvedFromManifest(manifest, registry, agentsDir)` — new exported helper; port IDENTITY from the snapshot, per-port RENDERING fields (as/directive/filename/store/artifactKind) merged in from the live registry; overlays NOT refreshed.
25. `state.resumePoint` refreshed on every clean `onSnapshot` (not just on pause) so a crash-reconciled `interrupted` v2 run is resumable; persisted by the next `_execStep`. Diverges from v1's boundary-only cadence.
26. `pipelines.outcome` JSON = `{endReached, result, warnings, wireDeliveries, tokens}`; `exec_meta` JSON = `{taskId, parentExecutionId, title, phaseOrdinal}`; `stepRowToStep` maps `cycle → ordinal` as an alias.
27. `_verdictKind` = `reviewKindOf(basename(verdict.path).replace('-cycle<ordinal>.json',''))`.
28. Shim order pinned: every `phase` immediately preceded by an `exec` of the same status with no `state` between (`skipped` emits none).
29. `createOrchestratorFor` gains optional `opts.template` routing hint (the already-read row at ui/server.mjs:1062), stripped before construction.
30. `GraphOrchestrator` constructor default `workflowId = 'wf_default_v2'`; `this._runners = {...opts.runners}` (no `defaultRunners`, no bound clarifier).
31. `POST /api/run` 400 wording: NOT_FOUND keeps `unknown workflowId "<id>"`; ARCHIVED returns P2's message verbatim.
32. Parity normalization (Task 10 table) is P4's definition; v2 flow-card rows and the `result` artifact kind are the only sanctioned asymmetries.
33. New helper files `test/helpers/git-dir.mjs` and `test/helpers/engines.mjs` (`ENGINES` + `adaptRunner`, the v1→v2 stub-runner ABI adapter).
34. New test files beyond the spec's names: `test/run-harness-attr.test.mjs`, `test/api-workflows-graph-alias.test.mjs`, `test/artifacts-exec-ledger.test.mjs`, `test/orchestrator-graph-resume.test.mjs`, `test/api-run-engine-dispatch.test.mjs`.
35. `state.gate.askId` derived as `gate-<wireId>-<deliveryNo>` when the scheduler's gate record does not carry one.
Risks (P4 writer): item 16 hoist touches live v1 (pure move, 25-suite oracle); depends on P3 signatures landing per §5.3/§5.4; resume-from-manifest merges rendering fields from a possibly newer registry; assumes P2's buildGraphManifest emits shim cells (Task 5 Step 2 STOP-and-fix); test count `BASELINE + 28 + D`; `orchestrator-session-capture` hard-codes v1 step keys (Task 9 Step 3).

## P7 — Agents view + agent-gen v2 + plugin API 3 (Task 0 + 13 tasks, 2581 lines) — file 2026-08-26-node-graph-v2-P7-agents-view-agent-gen-plugin-api-3.md
Sentinels consumed: `decorFromState` in ui/public/graph/run-decor.mjs + src/cli/render.mjs; hard-grepped P2 exports `validateMetaV2`/`normalizeAgentMeta`/`indexByKey` (src/shared/graph/agent-meta.mjs), `validateGraph`, `portsFnFor`, `registryPortsFn` (src/core/graph/registry-ports.mjs), `MOCK_WRITER_ROLES` (claude-runner.mjs), `writeGraphWorkflow` (workflows.mjs).
Sentinels produced: `WORCA_PLUGIN_API = 3` / `WORCA_PLUGIN_APIS = [1, 2, 3]` in src/core/plugin-api.mjs; `function agentFormRender` in ui/public/app.js (with `agentFormRead`/`bindAgentForm` on `window.__agents`).

Assumed from P2:
1. `validateMetaV2(raw, { mockWriterRoles } = {}) → { errors: string[] }` — P7 calls it with NO options from plugin-manifest.mjs and agent-store.mjs, assuming `mockWriterRoles` only affects a WARNING (unknown mockRole dropped, never a 400 per adj-f2 §3). If P2 makes mockRole an error without the option, both call sites need the set injected — and src/shared cannot import claude-runner.mjs.
2. `validateGraph` issues are `{ code, message }` (spec §3). P7 formats `${e.code}: ${e.message}`; old branch used `msg`. `formatIssue` output unspecified — not used.
3. `portsFnFor(agentsByKey)` assumed to answer FLOW kinds (task/end/and/or/combine) from `flowPorts` internally, so plugin-manifest and plugin-workflows need only supply agent metas.
4. `normalizeAgentMeta(raw) → {meta, errors}` used in plugin-manifest to build ports from raw plugin sidecars; assumes `.meta` carries `inputs`/`outputs` with defaults applied (`required`, `when`, `store`).
5. `GET /api/agents` v2 fields assumed present on every builtin after P2: `metaVersion, inputs, outputs, portSummary, placeable`.

Names/shapes/messages P7 chose:
6. `agentFormRender(host, meta, { markdown, mockWriterRoles, registryKeys })` — `markdown` in the options bag.
7. Ignored-sidecar log string: `[agent-registry] <origin>/<file>: built for <plugin API N | an older plugin API> — worca requires plugin API 3 (a metaVersion 2 sidecar with typed ports) — ignored`.
8. **`scanLayer` gate is PLUGIN-LAYER ONLY** (`scanLayer(dir, origin, {requireMetaV2, builtFor})`, opt-in from the plugin merge loop). adj-f2 §1.3 says "all layers"; rebuild spec §6 keeps the v1 path for builtin/user sidecars during coexistence — spec wins per P7; gating all layers would break every existing custom agent on the still-live v1 engine. NEEDS ADJUDICATION.
9. `pluginAgentLayers()` gains `builtFor` (breaks the deepEqual pin at test/plugin-agent-registry.test.mjs:57; plan updates it) and now reads each plugin's manifest (new agent-registry.mjs → plugin-manifest.mjs import; acyclic today).
10. `apiMismatch(range, issues) → {builtFor, host, agents, workflows}` content-gated (null when the plugin ships no v1 data), `builtFor: declaredApi(range) || null`.
11. `dataContractIssues(absDir)` returns BASENAMES (`agentsV1: ['x.meta.json']`, `workflowsV1: ['y.json']`); callers prefix `agents/`/`workflows/`.
12. TWO message functions: `apiMismatchMessage` (browser, plugins-view.mjs, adj-f2 §1.7 verbatim) and new `apiMismatchDetail` (server, plugin-manifest.mjs, short) used by `dirChecks('agents-api')` and `worca plugin list`. adj-f2 §1.5 implies the same text in both. NEEDS ADJUDICATION.
13. `agentFormRead` omission set extended: `domain`, `icon`, `promptHints`, `requiresSkills` omitted when empty; `scope` written only for `workspace-only`; `placeable` only as `false`.
14. `AS_TYPE = { file: 'md', answers: 'json', 'fix-review': 'md', worktree: 'void' }` client-side `as` hint (`file` also accepts json) — a guess at P2's rule; hint only.
15. New CSS class `.row-3`; port-editor CSS adapted from old:ui/public/style.css:1057-1071; control classes per adj-f2 with `.field.pf-f-<name>` wrappers.
16. Wizard Step 3 (`#agw-step-3`) and the card pane both mount a bare `<div class="agent-form">`; `#agw-msg`, `#agw-save`, `.agent-edit-msg` and the actions rows stay in HTML.
17. agent-gen gains one seam, `async _runClaude(metaOnly)`, so the read-back gate is testable offline.
18. plugin-workflows keeps its own `INSERT … ON CONFLICT` (needs `origin` + `wfp_` ids) instead of calling `writeGraphWorkflow`; `graph` stores `{nodes, wires, canvas?}` only (P2b's rowToTpl must agree).
19. Test file `test/ui-agent-port-editor.test.mjs` (new) not in spec §12.3 list but named in the P7 handoff.
Risks (P7 writer): agent-store 400 gate makes every v1-shaped fixture that creates a user agent fail — plan converts agent-store, agents-api, agents-questions-form, api-plugins, plugin-agent-registry fixtures (grep step included); plugin-layer registry gate is a behavior change for plugin agents on v1 before P8 (intended); assumes P2 signatures; test count BASELINE + 33 (agents-api channels-union test replaced 1:1).

## P6 — Run monitor v2 + CLI (P6a Tasks 0–10, split marker, P6b Tasks 11–17; 2394 lines) — file 2026-08-26-node-graph-v2-P6-run-monitor-v2-cli.md
Sentinels consumed (P5): `export function createGraphView` in view.mjs + `class="gv-head"` in index.html; Task 0 also greps P2's `buildGraphManifest`/`manifestPortsFn` (manifest.mjs), `nodeSize` (geometry.mjs), P4's `'exec'` in ui/server.mjs.
Sentinels produced: `export function decorFromState` (and `applyDecor`) in run-decor.mjs; src/cli/render.mjs exports `formatExecLine`, `fmtDur`. P6b entry (Task 11) greps `decorFromState` + `applyDecor` + `function paintGraphFor` in app.js.
New files: ui/public/graph/run-decor.mjs, ui/public/graph/run-hosts.mjs, src/cli/render.mjs, test/ui-run-decor.test.mjs, test/ui-run-hosts.test.mjs, test/cli-exec-render.test.mjs. Expected: BASELINE + 36 (P6a), BASELINE + 49 (P6b).

Decor + view seam:
1. Decor bag shape (planner default, Task 1): `{version, live, resolved, runStatus, status{}, footers{}, totals{}, colors{}, liveWireIds[], loopBadges{}, gate|null, endResult|null, progress{done,total}, activeNodes[], warnings[], quiescent, executions, loopDeliveries, nodeIds[], wireIds[], expanded}`. `FooterRow = {executionId, nodeId, kind:'cycle'|'task', ordinal, label, led, dur, cost, durMs, costUsd, flow}`. `decorFromState(st,{live,now,subsOf})` pure; host shallow-adds `run`, `runId`, `mode`, `histKey`.
2. **`view.setFooter(nodeId, bands)` vocabulary REDEFINED by P6** (biggest cross-plan risk with P5): rows become 26/22px BANDS `{kind:'fan'|'strip'|'exec'|'result', …}` with `footerRows = bands.length`. If P5 shipped a different row shape, P6 Task 3 replaces `setFooter`'s body. NEEDS ADJUDICATION (P5 owns view.mjs).
3. Two NEW view fast paths invented: `setNodeChrome(nodeId, {color, gate, totals})` and `setWireBadge(wireId, badge)`. `setNodeChrome` also stamps `run-node` + `data-id` on the card so app.js:15475's 1s `.run-node[data-id] .dur` tick works on v2 cards. NEEDS ADJUDICATION (P5 must ship or P6 adds).
4. View internals assumed: cards carry `data-node-id`; view keeps `nodeEls`/`wireEls`/`badgeEls` Maps + `world`; wire paths carry `data-wire-id`; badges `.wbadge[data-wire-id]`; `view.destroy()`; `view.setTransform({x, y, zoom})`; world inline style `translate(x,y) scale(z)`.
5. `view.mjs` node-lookup helpers `nodeOf(id)` / `portsOfNode(node)` referenced by name in `setFooter` — "use whatever P5 calls them".
6. `wheelPan:'engaged'` assumed honored by P5's nav controller; Task 5 Step 4 gives the policy to add to view.mjs if not.
App-side:
7. `paintGraphFor(host, stepper, decor)` 3-arg; v1 arm reads `decor.run`. Mounts memoised in `WeakMap<hostEl, {m, ctx}>`; host `innerHTML=''`-cleared on first v2 mount.
8. `activeNodes(r)` returns `[{nodeId, executionId, label, color, model, effort}]`, newest-started first. `activeCopy(r)` (new) pill: ≥2 → "2 agents running", 1 → node label, 0 while live → "Running".
9. New module `ui/public/graph/run-hosts.mjs` with `mountRunGraph(hostEl, opts) → {update(runId, stepper, decor), fit, destroy, view}` (spec names no file). Accordion Map<runId,nodeId> inside the mount instance.
10. Detail host sizing two-pass (width → zoom → `clamp(360, h·z+48, 600)` → fit both axes) — spec formula circular as written.
11. `.rc-meta` progress segment new markup (`.rc-seg.rc-prog`) in index.html:397.
12. CSS anchor drift: density rules at style.css:2376-2377 (spec said 2358-2373; adj-d's 2376-2390 right). New card CSS scoped `.run-flow .gv-world`; added `.is-error`, `.is-skipped`, `.ngate`, footer/`xresult`/`wfired`, `.run-warn`, `.rg-hint`, host heights. `paintRdTerminal` at app.js:13639 (spec :13646); `openHistDetail` at :10729, graph lines :10874-10876.
Server:
13. Routes (planner default): `GET /api/history/:key/:id/artifact?rel=` and `GET /api/workspaces/:id/runs/:runId/artifact?rel=` → `{rel, text}`; new `export async function resolveIndexedArtifact(key, id, rel)` in src/core/artifacts.mjs. `rel` selects among `listArtifacts(pipelineId)` rows by exact `relPath` OR basename (End result is an absolute engine path; artifact rows dir- or store-root-relative); STORED path read — run dir first, then store root.
Log filter / sub-agents:
14. Filter field names `{source, level, step, node, execution, cycle, search}`; node select = existing `.log-f-step` with `data-axis="node"|"step"`; chip `.log-f-exec[data-execution-id]` with `.lfe-text`/`.lfe-x`. `panel.__setLogSource` KEPT (pinned by test/ui-history-graph-log-link.test.mjs) + `__setLogFilter` added; `r.__setLogFilter({node, execution})` run-model setter (no-op-safe in P6a Task 8, completed P6b Task 13).
15. Group keys `execKey(nodeId, executionId, cycle)` = `${nodeId}|${executionId || (cycle ?? 0)}` applied to subsByNodeCycleArrays, subsGroupsForRender, stepStatusByKey, stepSkillsFromSteps, stepGraphifyFromSteps, onStepSkills, onStepGraphify. REQUIRES `stepskills`/`stepgraphify`/`subagent` events to carry `executionId` (spec §5.7; Task 14 Step 1 verifies P4).
16. `cycleAwareLabel` gains 4th arg `steps` (defaulted); labels v2 groups `Implementer #1 · Add schema`.
CLI:
17. `formatExecLine(ev, manifest, {now, color})` with injected `color(name, text)` so render.mjs stays pure. `fmtDur` CLI shape `12s`/`1m03s`/`1h01m`, deliberately different from run-decor's `1m 3s` — TWO fmtDur's exist. NEEDS ADJUDICATION (one or two).
18. `ev.taskIndex` / `ev.taskTotal` ASSUMED on `kind:'task'` exec events for `task 3/7`; spec §5.7 does NOT list them. Degrades to `  ▶ task · <title>`. NEEDS P4 DECISION.
19. Paused/skipped exec lines unspecified: `⏸ <Label> #<n>  paused` and `''` (planner default).
20. `formatGateHeader` reads `payload.wireId` + `payload.deliveryNo`; `deliveryNo` not in spec gate payload → falls back to `maxCycles`. (P4 item 35 derives askId with deliveryNo — align.)
21. ARCHIVED message: spec §4 text used; adj-f2 §4 variant superseded.
22. `--workflow` guard ownership: Task 16 Step 1 verifies whether P2b already wired `assertRunnableWorkflow` into `cmdRun`; adds if absent (idempotent).
23. `phase` double-print: CLI keeps `phaseLabel`/`statusMark` and gates the `phase` handler on `state.stepper.version === 2` because P4's shim emits BOTH streams.
Risks (P6 writer): P5 seam (setFooter vocabulary, two fast paths, data attributes, setTransform/destroy); P4 event surface (executionId on subagent/stepskills/stepgraphify, verdict.hasBlocking on verifier done, wireId on gate questions, slice index/total); wide app.js coexistence surface guarded by `isGraphRun(r)`/`isGraphManifest(stepper)`; oracle suites re-run per task.

## P8 — The break + kill list + docs (P8a Tasks 0–10, split at line 963, P8b Tasks 11–21; 1361 lines) — file 2026-08-26-node-graph-v2-P8-break-kill-list-docs.md
Sentinels consumed (P7): `WORCA_PLUGIN_API = 3`; `agentFormRender` in app.js; plus P2b `SCHEMA_VERSION = 23`, P4 `wf_default_v2` in workflows.mjs, P1 `SEED_TEMPLATES`/`NODE_ID_MAP`/`FB_WIRE_MAP`/`GRAPH_DEFAULT_WORKFLOW`, src/core/{run-harness,engine-select}.mjs, src/core/graph/orchestrator.mjs.
Produced — P8a: `SCHEMA_VERSION = 24` + `reconcileV1Workflows(db,{seed})` + `sweepV1Runs(db=getDb())` + `V1_RUN_RETIRED` (db.mjs), `GRAPH_DEFAULT_WORKFLOW`/`LEGACY_DEFAULT_WORKFLOW` (workflows.mjs), `paintLegacyStrip` (app.js), `state.resumable` (artifacts.mjs). P8b: test/v1-remnants-removed.test.mjs green, `createOrchestrator` from src/core/orchestrator.mjs (moved), `EngineRetiredError` (engine-select.mjs), `BOOKEND_EXECUTION_IDS` (run-decor.mjs).

1. B2 db path — `migrate(db)` gets a handle, so the backup path comes from `PRAGMA database_list` (`main.file`), not `dbPath()`; returns the RESOLVED path (`/private/var/…` vs `/var/…` on macOS); `existsSync(dbPath()+'.pre-v24.bak')` still finds it. `:memory:` → no backup.
2. B3 report JSON — `store_meta('migration:v24','migration', JSON)` with `{at, archived[], seeded[], seedsSkipped[], overlayNodes, overlayWires, aliasRemapped, activeReset[], sweptRuns[]}`, `INSERT OR IGNORE`. `store_meta(key PK, kind NOT NULL, data NOT NULL)` verified; precedent `__fs_migration__`.
3. B4 sweep predicate — spec `json_extract(rp,'$.version') != 2`; P8 adds `json_valid(resume_point) = 0 OR …` so a corrupt blob is swept instead of throwing in the tx. `!= 2` does NOT match a missing `version` (NULL); dev's v1 points always carry `version: 1`.
4. B5 fs-import hole — `maybeMigrateFromFs` runs AFTER `migrate()` (db.mjs:70), so imported v1 rows would stay LIVE post-break. Added `reconcileAfterFsImport(db)` in `getDb()` (archive-only, `{seed:false}`, cheap probe first). Spec §10.2 silent; adj-e §2 prescribed it. Forces `migrate-fs-to-db.test.mjs:198-215` edits (exact replacement given). NEEDS ADJUDICATION (accept).
5. B6 `DEFAULT_WORKFLOW` — deleted as a name; v1 literal renamed `LEGACY_DEFAULT_WORKFLOW` (P8a) for dispatcher/workflow-validator suites only, deleted in P8b. Chosen over adj-a §5's "kept for audit". 7 test files re-pointed. NOTE: spec §4 says P8 `GET /api/workflows` → `GRAPH_DEFAULT_WORKFLOW` only; Ask catalog/proposal reference `DEFAULT_WORKFLOW` (dev-map §6) — all importers must be listed.
6. B7 v1 rejection message — 400 `{error:'v1 pipeline templates are no longer accepted — save a graph (version 2)'}`.
7. B8 `writeWorkflow` NOT deleted (4 suites call it); guard pins that no src/ui file outside workflows.mjs references it. NEEDS ADJUDICATION (delete or keep).
8. B9 `paintLegacyStrip` in app.js beside P6's `paintGraphFor` branch, self-contained `legacyChipRows`; assumes `fmtDur`/`fmtUsd`/`COMPOSER_COLORS` exist in app.js (executor greps).
9. B10 `phases.mjs` survivors — 17 exports + P3's `siblingsBlock`/`mockMarkers`/`diffInstruction` + re-homed `renderPromptArtifact`/`renderAttachmentsBlock`; 12 `run*` builders + `FALLBACK_PROMPTS` die. `runWorkspaceScan` survives (§5.10). Assumes P3's executor imports only from that survivor set. CROSS-CHECK vs P3.
10. B11 bookends — `x:preflight:1`/`x:done:1` exec rows; `_bookend` uses `_recordStep(executionId, 0, …)` (cycle 0 ⇒ key is executionId verbatim) then stamps exec columns. `BOOKEND_EXECUTION_IDS` export (planner default) touches P6a's `decorFromState` and P6b's `formatExecLine`. CROSS-CHECK vs P1's `_bookend` signature and P6.
11. B12 CSS re-home — keyframes + `.nstat`/`.fan` de-scoped out of `.run-flow` (P6 hosts v2 graph in `.rd-graph`/`.hd-graph`). Depends on P6 not renaming those classes.
12. B13 `registryToSteps`/`DEFAULT_SPEC`/`LEGACY_LABELS` KEPT (feed `config.mjs#agentSteps` → legacy per-role editor that `resolveGraph` honours for wf_default); only their v1 wiring fields deleted. Deviates from §11 "v1 arms". NEEDS ADJUDICATION.
13. Sidecars — no `uiPhase` key exists in the 11 builtins (only the code map `UI_PHASE`); deletions are `consumes`/`optionalConsumes`(4 files)/`produces`/`connectsTo`/`loopSource`.
14. `phases-*` suites — all five survive and are KEPT (contradicting §11 "bespoke phases-*"); only workspace-runners/runner-decomposer/runners*/channels*/workspace-channel go. `saved-pipeline-parity` + test/fixtures/workflows-v1/ retired in P8b. NEEDS ADJUDICATION (P3's prompt-parity suite + phases-* both pin prompts; fine to keep both?).
15. `GRAPH_DEFAULT_WORKFLOW` lacks `createdAt`/`updatedAt` on the old branch — P8a adds `'1970-01-01T00:00:00.000Z'` if P1 copied verbatim. CROSS-CHECK vs P1 (verbatim copy mandate) and P4 (alias row shape).
Risks (P8 writer): all V24 SQL executed against node:sqlite (`.changes`, INSERT OR IGNORE … SELECT, UPDATE OR IGNORE PK collision, json_set, VACUUM INTO preserving user_version); fixture technique verified; node:sqlite null-prototype rows vs strict deepEqual; anchor drift by P2–P7 (symbol+grep pairing); P8b totals DROP (≈20 suites retired); assumes P2b's resolveGraph carries the wf_default legacy per-role layer (adds if not).

## P5 — Composer v2 (P5a Tasks 0–6, split at line 1240, P5b Tasks 7–19; 3998 lines, 103 steps) — file 2026-08-26-node-graph-v2-P5-composer-v2.md
Sentinels consumed (P4): `export function createGraphOrchestrator` + `wf_default_v2` in workflows.mjs; `ls` of the eight src/shared/graph/*.mjs + ui/public/graph/model.mjs.
Sentinels produced: `export function createGraphView` in ui/public/graph/view.mjs (also `mountStaticGraph`, `thumbnailFor`, `LEGEND_TEXT`, `MODE_ZOOM`), `class="gv-head"` in index.html. P5b entry greps `createGraphView` + `mountStaticGraph` + `v2 node-graph canvas` in style.css.

1. `createGraphView(host, opts)` options — spec `{mode, portsFn, agents, raf, viewport, zoomMin, zoomMax, wheelPan}`; P5 added `doc` (jsdom) and defaults `zoomMin/zoomMax` per mode via exported `MODE_ZOOM` (`edit .4–1.6`, `monitor .3–1.6`, `static .3–1`).
2. **The view OWNS the stage** — `createGraphView` creates `.gv-stage` and PREPENDS it into `host` (never `host.replaceChildren()`), so `.gv-chip`/`.gv-ins-rail` survive as siblings. P6 must pass an empty host div. CROSS-CHECK vs P6 (P6 `innerHTML=''`-clears the host on first v2 mount, memoises in WeakMap).
3. `decor` bag accepted by `render(tpl, {selection, report, decor})`: `{status:{nodeId:status}, footers:{nodeId:rows}, wireLive:[wireId]}` applied via fast paths. P6 owns the real decor contract. CROSS-CHECK vs P6 item 1–3 (P6 bag is much richer; P6 uses `applyDecor(view, decor)` with fast paths — decide whether `render({decor})` accepts P6's bag or P6 always calls applyDecor).
4. `createComposer(hostEls, {doc, api, raf, viewport, storage, portsFn})` — 18 host elements (`canvas, chip, head, name, dirty, errors, newBtn, autoBtn, saveBtn, insRail, insBody, insToggle, palette, filter, savedList, savedCount, archived, dialogHost`) and 8 injected `api` methods (`agents, agentsAll, config, listWorkflows, listArchived, readWorkflow, saveWorkflow, deleteWorkflow`).
5. Element ids/classes (new): `gv-head, gv-name, gv-dirty, gv-errors, gv-new, gv-autolayout, gv-save, gv-canvas, gv-chip, gv-ins-rail, gv-ins-body, gv-ins-toggle, gv-agents, gv-agent-filter, gv-palette, gv-legend, gv-saved-list, gv-saved-count, gv-archived, gv-dialog-host`; classes `.gv-stage/.gv-world/.gv-wires/.node/.nhead/.nbody/.prow/.psep/.dot/.dia/.gdot/.npip/.wbadge/.gv-empty/.gv-drag-ghost/.ap/.pal-chip/.pal-group/.pl-legacy/.pl-thumb`; state classes `is-pending|active|done|paused|stopped|error|skipped` + `wire-live`, `bad`, `drop-ok`, `drop-bad`. (P6 adds `.is-error`/`.is-skipped`/`.ngate` CSS — check overlap: P5 already lists is-error/is-skipped state classes.)
6. Storage key `worca.composer.inspector`.
7. `wheelPan` placement — a `createGraphView` option seeding the default, overridable per host via `view.createNav({wheelPan})`. Composer never calls `createNav` (binds its own pipeline), behaves as `'always'`. CROSS-CHECK vs P6 item 6 (P6 assumes nav controller honours `wheelPan:'engaged'` — must call `view.createNav`).
8. `graphBounds`/`fitBounds` deliberately NOT used — the view computes bounds locally from `nodeSize` (needs per-node footerRows + inset + ≤1× cap). Flag: P2 ships those exports (dead code?).
9. Assumed P2 signatures: `portsOf(portsFn,node)→{inputs,outputs,known,ported}` with synthetic `await` LAST; `nodeSize(node,ports,{footerRows})`; `portAnchor(node,ports,portId,dir)`; `bezierPath(a,b,{mirror,loop})`; `bezierMid(a,b,{loop})`; `classifyLoops(tpl,portsFn)→{loopWireIds,launchOrder,…}`; `resolveOrOutType(tpl,portsFn,orId,seen)`; `canWire({tpl,portsFn,from,to})→{ok,code,reason}`; `newNode(kind,key,x,y)`; `newWire(from,to)`; `autoLayout(tpl,portsFn)→{nodes,…}`; `thumbnailSvg(tpl,portsFn,{width,height})`; `injectGeometry(el)` writing ≥10 `--gv-*`; `GEOMETRY_CSS_VARS` one entry per var. `hitNode/hitPort/hitWire` NOT used — composer hit-tests locally (ports/nodes from `view.anchor`/`view.size`; wires by sampling cached `d`). NOTE spec §7.2 says hit-testing via `hitPort → hitNode → hitWire` from geometry.mjs — deviation; ADJUDICATE (one source of truth).
10. style.css blocks: P5a APPENDS a v2 tail block scoped under `.gv-world`. P5b deletes `:1032-1093` and `:1123-1167`, KEEPS `:1168-1185` (`.fanout-toggle`/`.questions-toggle` are New-Pipeline), RE-SCOPES `:1094-1121` to `.run-flow .node …`. `:1258-1381` untouched (P6 extends, P8 kills).
11. `app.js:1935-2648` not deleted wholesale — real range `1935-2636` (`2638-2647` = `modelById`/`option`, keep); `COMPOSER_COLORS/TINTS/SEQ` (`:1950-1952`) + `composerPaintWires` (`:2353-2434`) are SHARED with the v1 run graph (`:1205`, `__np` `:2919`, test/ui-run-graph-paint.test.mjs:109) — re-homed, not removed. (P8 B9 assumes `COMPOSER_COLORS` exists in app.js — consistent.)
12. `EMBEDDED_AGENTS` replacement: `agentMetaCache` + `ensureAgentMeta()` called from `buildRunGraph(host, manifest)` (app.js:1071), invalidated by `invalidateAgentCaches()` (:6590).
13. Extra retired/edited tests: test/ui-workspace-selectors.test.mjs:67-69 (composer-core import drift guard — rewritten); one test block in test/ui-agents-view.test.mjs:131-159 (drives `#composer-save`) deleted.
14. Archived footer shipped now, rendered only when `GET /api/workflows?archived=1` returns ≥1 row.
15. Run-setup `PATCH` body `{ projectDir, workflowId, wires: { [wireId]: { maxCycles } } }` via new `saveWire()` mirroring `saveFeedback` (app.js:3646). CROSS-CHECK vs P2b PATCH /api/config wires shape.
16. `composerExit()` = `composer.suspend()` (not `destroy()`), adds `resume()/suspend()` to the composer surface.
17. CDP probe seam: `window.__gv()` in app.js (returns `{c, v}`).
18. Test counts: P5a = BASELINE + 20; P5b = BASELINE-B2 + 31 (BASELINE-B2 printed after retirements).
Risks (P5 writer): P2 signature surface (portsOf, nodeSize, portAnchor, bezierPath, bezierMid, classifyLoops fields, canWire reasons, newNode) load-bearing; d-string/geometry assertions arithmetic-verified vs prototype (`191.5/110.5`, `M 280 199 C 334 199, 346 136, 400 136`, `t=(120,104.25)`, loop bow `M 620 537 C 719 668.4, 301 291.4, 400 160`) assuming bezierPath doesn't round; v1 composer retirement couplings; CDP script unverified (first run = debugging session); P5a is 7 tasks.

## P3 — Engine, no callers (13 tasks, 4626 lines, +85 tests) — file 2026-08-26-node-graph-v2-P3-engine-no-callers.md
Sentinels consumed: `export function validateGraph`; `SCHEMA_VERSION = 23`; `export const MOCK_WRITER_ROLES`; Task 0 also greps `registryPortsFn`, `classifyLoops`, `portsFnFor`, `flowPorts`, `AWAIT_PORT`, `blockingIssues`, `SEED_TEMPLATES`, `NODE_ID_MAP`, `FB_WIRE_MAP`, `GRAPH_DEFAULT_WORKFLOW`, 11 v2 sidecars.
Sentinels produced: `export function createScheduler` + `export function sliceExecutionId` (scheduler.mjs); `export function runAgentExecution` + `export function runExecution` + `export function publishable` (executor.mjs); test/fixtures/workflows-v1/*.json (7); test/helpers/graph-run.mjs.

1. **`onGate` vs `onAsk` split** (planner default): `onAsk(ask) → Promise<answer>` is the ask channel (default answers gates `continue`, clarify `[]`); `onGate({wireId, fromNode, toNode, askId} | null)` is the NOTIFIER feeding §5.7 `state.gate`; `onEvent('gate', {wireId, nodeId, executionId, issues, askId, status:'held'|'another'|'continue'})` is the audit trail. CONFLICT with P4 item 7 (`onGate({wireId, nodeId, executionId, deliveryNo, issues}) → Promise<'another'|'continue'>`). ADJUDICATE.
2. **`reattach(snapshot)`**: takes the snapshot, must be called BEFORE `run()`; restores AND re-invokes `execute` for every non-terminal exec and re-raises held gates (unless `ended`). `run()` restores nothing. (P4 item 6 calls `reattach(snapshot)` before `run()` — consistent.)
3. **`execute` may return `{ paused: true }`** (NEW): marks the ledger row `paused` (non-terminal), publishes nothing, emits `exec status:'paused'`. P4's `_execute` MUST return it on pause. CROSS-CHECK vs P4 (§5.4 says pause inside a slice ⇒ `{outputs:{}}` + step paused).
4. Snapshot `execs[]` carries `bindings` + `trigger` (superset of §5.3's list) — reattach uses RECORDED args.
5. `getState().tokens` = latched OUTPUT tokens keyed `'<node>.<outputPort>'` → `{seq,type,path,firedAt}`; snapshot keeps both `tokens` (delivered, by input port) and `outputs` (latched). Also exposes `wireDeliveries`, `endReached`, `result`, `warnings`, `gate`, `active`, `executions`, `settled`. CROSS-CHECK vs P4 item 8 (`getState()` shape: `{active, wires, tokens, gate, ended}`).
6. **No `skipped` exec rows** — derived by P6 from "no rows on a done run" (adj-d). Contradicts base spec §3 "nodes that never fired report skipped" read literally. (P4 shim: `skipped` emits no phase — consistent with no rows.) ADJUDICATE (P6 statusOf handles never-fired → skipped; fine).
7. `exec` fields the SCHEDULER emits: `nodeId, executionId, kind, ordinal, status, agentKey, trigger` always; `+ phase, taskId, title, parentExecutionId` on `kind:'task'`; `+ result` on End done; `+ verdict:{hasBlocking}` on a done with a verdict; `+ error` on error. `costUsd` is P4's. `trigger` on EVERY status. NOTE: no `taskIndex`/`taskTotal` (P6 item 18 wants them for `task 3/7`). ADJUDICATE.
8. `token` event gains `to:[{node,port,wireId}]`, `sourceExecutionId`, `firedAt`.
9. **`taskArtifact` removed from `createScheduler`** — the Task card's document comes from `ctx.taskArtifact = {path}|{text}` (P4 must stamp it); throws a named error if neither is supplied. (P4 item 6 says "P4 supplies `taskArtifact` itself in `_runFlow`" — check mechanism matches: ctx field vs execute arg.)
10. New executor exports for P4: `runExecution(ctx, {runners})` (single dispatch entry; unknown kind throws `node "<id>": unknown kind "<kind>"`) and `publishable(ports, outputs)` (composite `finish` return).
11. `phases.mjs` export-only additions beyond the spec's: `runOpts`, `READ_WRITE_TOOLS`, `IMPLEMENTER_TOOLS` (plus `siblingsBlock`, `mockMarkers`, `diffInstruction`). CROSS-CHECK vs P8 B10 survivor list.
12. `ports.verdict.filename`: P3 assumes P2's `portsFn` carries `verdict` through; test ctx builders stamp `{...portsFn(node), verdict: meta.verdict}` defensively. ADJUDICATE: does `portsOf()` return `verdict`? (P2 defines.)
13. `firedOutputs(outputs, verdict)` called with the outputs ARRAY (old-branch signature); §3 says `firedOutputs(ports, verdict)` — Task 2 tells the implementer to check P2's signature. ADJUDICATE the canonical signature.
14. `isLoopPort` = classification OR meta `loop:true` — fix vs old branch: `or.out → agent.fix` is always-sourced, so `classifyLoops.loopInputs` alone would deadlock every double-loop seed. Mutation-tested. (Semantics check vs Amendment f readiness "loop inputs (meta loop:true) are excused" — consistent with the base spec; the spec §3 loops.mjs `loopInputs` set is classification-based — clarify which set readiness uses.)
15. Test helper naming: P3 uses `registryPortsFn(loadAgentRegistry(...))` rather than P2's `test/helpers/graph-ports.mjs` (export names unknown); new helper `test/helpers/graph-run.mjs` → `runGraphOffline(...)`. ADJUDICATE (spec §3 says the helper loads the real sidecars — use one).
16. `runCtx` fields (P4 builds): `{pipelineDir, projectDir, baseName, datePrefix, workspaceKey, duplicateKey, slice, planVersion}` with `planVersion: () => n` run-global. (P4 item 10 consistent.)
17. Full `ctx` field list tabulated in Task 8 (identity, graph, bindings, phases ctx, `claudeOpts`, questions fields, `priorAnswers`, `ask`).
18. Questions filename deferred to P4; clarifier ask id `clarify-<nodeId>-<ordinal>` implemented; gate ask id `gate-<wireId>-<deliveryNo>`.
19. New test files not in §12.3: `test/phases-graph-seams.test.mjs`, `test/graph-seed-v1-fixtures.test.mjs`.
20. Task 9 sidecar-repair allowance: if a parity literal is missing, P3 may edit `agents/<key>.meta.json` (a P2 defect) rather than weaken the pin.
Risks (P3 writer): P2 name surface; scheduler semantics re-derived from Amendment f (three never-borrow fixes + meta-loop-input barrier fix, mutation-tested); mock-graph hangs → 5s timeouts; 85 tests counted from plan.

## P1 — Harness split + foundations (12 tasks, 2458 lines) — file 2026-08-26-node-graph-v2-P1-harness-split-foundations.md
EMPIRICALLY EXECUTED by the writer in a clone: extraction script run + iterated to green — run-harness.mjs 2693 lines / orchestrator.mjs 1780 lines, both `node --check` clean; full suite 3803/3803 with every P1 task applied except Task 1's two new tests (dev BASELINE = 3760 ⇒ final = BASELINE + 45). Per-file: hooks 7/7, engine-select 5/5, constants 9/9, verdict 5/5, purity 3/3 (+ mutation proof), api-shared-static 4/4, seeds 10/10, 126-test orchestrator oracle unchanged.
Sentinels consumed: none (Task 0 Step 3: `git merge-base --is-ancestor e6968e15 HEAD`).
Sentinels produced: `export class RunHarness` (run-harness.mjs); `export const SEED_TEMPLATES` (seed-templates.mjs); also `selectEngine`/`createOrchestratorFor` (engine-select.mjs), `TEMPLATE_VERSION`/`gatePorts`/… (constants.mjs), `hasBlocking`/… (verdict.mjs), `GRAPH_DEFAULT_WORKFLOW`/`deepFreeze` (builtin-workflows.mjs), `NODE_ID_MAP`/`FB_WIRE_MAP`.

1. `_resolveTopology` return EXTENDED to `{ manifest, agentKeys:Set, workflow:{id,name} }` (spec §5.1 lists two). Third field feeds the audit line `Workflow: **${name}** (${id}).` at orchestrator.mjs:709. P4's GraphOrchestrator must return it. CROSS-CHECK vs P4 hook impl.
2. `_engineRehydrate` return EXTENDED with `audit?: string` (spec: `{checkpointRef, memberWorktrees, plan?}`). Resume audit line used v1-only `rp.kind`/`rp.stepIndex`; base now writes `rehydrated.audit`. P4 must supply a v2 sentence.
3. `_engineRun({ resume, rehydrated })` — spec says `{resume}`. Extra field carries the frozen plan from rehydrate to the v1 dispatcher. Fresh run: base passes `{ resume: null }`. CROSS-CHECK vs P4.
4. **`_phase` moved to `RunHarness`** contra spec §5.1 (V1-ONLY "except bookends"). Forced: all four production call sites ARE bookends; test/cost-tracking.test.mjs (13 calls) + test/duration-tracking.test.mjs (11 calls) call `orch._phase(...)` directly. orchestrator.mjs ends with zero `_phase`. NOTE for P8: `_phase` deletion then lives in run-harness.mjs; P4's shim emits `phase` — CROSS-CHECK whether P4 reuses `_phase` for the shim.
5. **`_preflightAgentKeys` moved to the harness** with signature `(agentKeys: Iterable<string>)` — spec lists it SHARED-BUT-SHAPE-CHANGES while hook 1 says the base calls it. v1 derives the set via new `planAgentKeys(plan)` helper. (P4 item 12 consistent.)
6. Sixth hook named `_initRunners(opts)`, called from the base constructor at the `:306` position. CROSS-CHECK vs P4 item 30 (`this._runners = {...opts.runners}`).
7. `collectRequiredSkills(registry, planOrKeys)` accepts a plan OR an iterable.
8. `createOrchestratorFor` at P1 always returns `createOrchestrator(opts)`; `selectEngine` coerces with `Number(raw) === 2`.
9. `LIMITS` = `{maxNodes:80, maxWires:200, maxPortsPerSide:8, minArity:2, maxArity:8, maxCycles:20, maxNameLen:80}`. P2's validateGraph may extend but must not rename. CROSS-CHECK vs P2.
10. `gatePorts` types: OR `out` `'any'` until resolved, AND `out` `'void'`, Combine `out` `'md'`; AND/OR ins `'any'`, Combine ins `'md'`; ALL gate inputs `required:true` (affects P2's V5). CROSS-CHECK vs P2.
11. `KINDS`/`FLOW_KINDS`/`PORT_TYPES` are frozen ARRAYS, not Sets. CROSS-CHECK vs P2/P3/P5 usage (`.has` vs `.includes`).
12. `WIRE_ID_RE = /^w_?[a-z0-9]{1,32}$/` — underscore optional or every shipping graph (`w1`…`w17`) fails V2. CROSS-CHECK vs P2's V2 + `mintId('w_')`.
13. protocol.mjs re-exports all five verdict names incl. `BLOCKING`/`normalizeSeverity` (additive).
14. Seed files byte-identical, stale `V17` comments included; P8 corrects them. (P8 item 15: GRAPH_DEFAULT_WORKFLOW lacks createdAt/updatedAt — consistent with verbatim.)
15. `FB_WIRE_MAP`'s fb_N↔wire pairing for `wf_clarify-implement` unverifiable; test does NOT pin it (set-equality + per-wire dynamic resolve only) — P8's V24 must resolve dynamically there. CROSS-CHECK vs P8 (static maps only per spec §10.2). ADJUDICATE.
16. Anchor drift vs spec (dev wins, corrected in plan): `collectRequiredSkills` seam :663 (spec :676), `_dispatch(plan)` :710 (:706), boundary literal :727–736 (:721–733), `rp.bus` reads :899/:905/:917, resume plan block :976–994 (≈:1052–1064), bookends :515/:636/:717/:1000, constructor `'wf_default'` :297.
NOT DONE by P1 (P4 item 16 wants it): hoisting `_onAgentEvent` + sub-agent reducers + `_recordCost` + 13 module helpers into RunHarness. ADJUDICATE owner (P1 has the oracle and already executed; P4 guards with a grep).
Risks (P1 writer): Task 2 script asserts orchestrator.mjs is 4369 lines / 96 members (aborts on any prior edit — re-derive, don't loosen); two byte-faithful reorderings (`collectChannelDefs` into v1 `_engineRun`; `rp.bus` reads into `_engineRehydrate` at the :820 position); workspace-resume `memberWorktrees` branch covered only by orchestrator-workspace oracle.

## P2 — Shared graph core + sidecars v2 + schema + store (P2a Tasks 0–14 = 15; P2b Tasks B0–B10 = 11; split at line 3874; 5226 lines) — file 2026-08-26-node-graph-v2-P2-shared-core-sidecars-schema-store.md
Expected: P2a BASELINE + 97; P2b BASELINE-B + 37 (both halves: BASELINE + 134). Writer ran old-branch validator + classifyLoops over the real seed constants + ported sidecars: all 8 graphs 0 errors / 0 warnings; loop-wire pins measured and equal FB_WIRE_MAP; implementer reorder re-validated.
Sentinels consumed (P1): `export class RunHarness`, `export const SEED_TEMPLATES`, src/shared/graph/constants.mjs, verdict.mjs — plus Task 0 Step 4 name-by-name grep of 14 constants and a `LIMITS.maxNodes/maxWires` probe (fallback instruction if P1's LIMITS lacks them). [P1 item 9: LIMITS has maxNodes/maxWires — OK.]
Sentinels produced: `export function validateGraph`; `SCHEMA_VERSION = 23`; `export const MOCK_WRITER_ROLES`.

Validator:
1. All V1–V21 message texts are P2's (borrowed from old:validate.mjs, `msg` → `message`). V4 texts: `unknown agent '<key>' — no such key in the registry` and `agent '<key>' has no v2 ports — port its sidecar to metaVersion 2`. `resolveGraph`'s equivalent throws use DOUBLE quotes (`agent "<key>" …`) while the validator uses single quotes — deliberate but arbitrary split. ADJUDICATE (spec §6 says resolveGraph refuses with `agent X has no v2 ports — port its sidecar to metaVersion 2`).
2. `RULES = [{code, level:'E'|'W', check(ctx, add)}]` with `add(message, extra)`; `validateGraph(tpl, portsFn, {limits})` → `{ok, errors, warnings}`.
3. Node/wire LIMITS enforced by V1 with two new messages (`template has N nodes — the limit is M`).
4. `Issue` gains `wireIds?: string[]` on V12's or-homogeneity issue only; `portId` populated by V7/V9/V12/V13/V14.
5. `formatIssue(issue)` → `"V7: <message> (wire w9)"` / `(node n1)` / `"?: "` for null. (P7 formats `${e.code}: ${e.message}` itself — could use formatIssue.)
Ports/loops/geometry/template:
6. `portsOf` returns `{known, ported, inputs, outputs, meta}`; `portsFnFor` returns `undefined` (unknown key) or `{...meta, known:true, ported:false}` (v1-only sidecar). `firedOutputs` accepts an array OR a resolved ports object. [P3 item 12/13: verdict via `meta`? P3 assumes `portsFn(node).verdict` — P2's portsOf returns `meta` (which has verdict) — ADJUDICATE the access path; `firedOutputs` dual-signature resolves P3 item 13.]
7. `resolveOrOutType(tpl, portsFn, orId, seen)` (spec order).
8. `classifyLoops` returns `{loopWireIds, loopInputs, sccOf, launchOrder}`; validator re-derives cycle components by grouping `sccOf`.
9. `tarjanSccs(ids, edges)` takes `edges` as `{from, to}` objects.
10. `newNode(kind, key, x, y, taken?)` and `newWire(from, to, config?, taken?)` — optional `taken` arg; `newNode` seeds `config.arity = 2` for and/or/combine. [P5 assumes `newNode(kind,key,x,y)`/`newWire(from,to)` — compatible.]
11. `canWire` reasons/codes: `V0 same node`, `V5 unknown port`, `V7 already connected`, `V8 <out> → <in> type mismatch`, `V12 or inputs must match: <type>` (`V0`/`V5`/`unknown port` are P2's). [spec §3: `same node`, `already connected`, `json → md type mismatch`, OR homogeneity per V8 — P2 assigns V12 code; ADJUDICATE code for OR reason (spec says "homogeneity error = V12, mechanism = V8").]
12. Geometry: `GEOMETRY_CSS_VARS` exact 10-var set; `fitBounds(bounds, {width,height}, {zoomMin, zoomMax=1})` and `graphBounds(tpl, portsFn, {pad, footerRowsOf})` shapes are P2's. **Spec closed form `95.5 + 24·(nIn+nOut)` does NOT hold for a 0-input agent** (110.5 — one fewer separator); plan pins it. [P5 computes bounds locally, not via graphBounds/fitBounds — P5 item 8.]
13. `thumbnailSvg` draws inside one `<g transform>` in world space with `stroke-width = 1/z` (uses real bezierPath).
14. `autoLayout` y-origin 60 snaps to 55 (11px grid); tests pin 55/242.
Meta/registry/sidecars:
15. `normalizeAgentMeta(raw, {mockWriterRoles, warn})` / `validateMetaV2(raw, {mockWriterRoles})` — injected vocabulary opts; with NO vocabulary injected, any `mockRole` string rides through. [P7 item 1 calls with no options — consistent: no 400 for mockRole.]
16. `derivePortSummary(meta)` wording: `Reads a, b; produces c.` / `Produces c.` / `Reads a.` / `''`; all-void side falls back to declared ids.
17. **`manualTestsChecklist.promptHints` carries a `{diffInstruction}` token** inside parentheses; substituting `via \`git diff\` in your cwd` reproduces the single-project sentence byte-for-byte, but the detached-workspace arm cannot come from the same template — P3 may need to adjust for parity. CROSS-CHECK vs P3 Task 9 (sidecar-repair allowance) + adj-f1 §5 checklist pins.
18. `decomposer.tasks` gains `artifactKind: "decomposition"`.
19. **implementer input order `fix, task, plan`** with a `plan` directive added (old branch `plan, fix, task`, no plan directive) — re-validated 0/0.
20. ports ↔ channels mapping table with three deltas: `manualTestsChecklist`/`manualWebUiTesting` miss `code`; `implementer` adds `decomposition`; `optionalConsumes` assertion dropped.
21. `normalizeMeta` merged key list; invalid v2 ⇒ return null (skip whole sidecar).
Manifest:
22. Full manifest cell shapes; `fanOut` added to agent cells; `ports.await` boolean; icon sanitizer = drop when > 2048 chars or matching `<script|iframe|object|embed|foreignobject|javascript:|on…=`.
23. **Shim `steps` ranks = `rankNodes` ranks (loop wires excluded), nodes ordered by `launchOrder` inside a cell** — spec says "condensation-topo rank"; pure condensation would collapse a whole loop into one cell. ADJUDICATE (P4/P6 inherit; Ask `shapeWorkflow` also uses "condensation-topo rank").
24. `manifestPortsFn` synthesizes `verdict: {filename: ''}` for nodes with a conditional output so V13/`firedOutputs` work registry-free.
Storage/API/P2b:
25. `assertRunnableWorkflow` NOT_FOUND text = `unknown workflowId "<id>"`; ARCHIVED verbatim §4.
26. **v2-run refusal at P2**: HTTP 409 `template is a graph — runs on the graph engine (not available yet)`; CLI same via `fail()` (exit 2); Ask pushes into its error list. ARCHIVED id at `POST /api/run` is 409 (unknown stays 400). P4 deletes this branch. [P4 item 31 says ARCHIVED → 400 with P2's message. CONFLICT 409 vs 400. ADJUDICATE.]
27. **`resolveGraph` return contract `{template, nodes, wires, agentsByKey, agentKeys}`** with the template unmutated — P4 must consume and not re-resolve; flow nodes get `nodes` entries with `key: null`. [P4 item 1 assumed `{ template, ports, nodeCtx }`. CONFLICT. ADJUDICATE + define the per-node entry shape precisely.]
28. `resolveRunConfig` returns `{nodes, wires, feedbacks}` (keeps `feedbacks` until P8).
29. `POST /api/workflows` v2 error body `{error: 'invalid graph', errors, warnings}` at 422.
30. `setWorkflowNodeDefaults` v2 arm preserves non-tunable config keys (`awaitAll`/`arity`/`planStoreSeed`).
31. `agent-store.deleteAgent` scans with `includeArchived: true`.
32. `GET /api/agents` needs NO edit (objects pass through at ui/server.mjs:3913).
Risks (P2 writer): P1 constants shapes (`gatePorts`/`LIMITS`/`PORT_TYPES`) — Task 0 Step 4 greps; shim-cell rank definition; prompt-parity of five new promptHints + `{diffInstruction}`; API/DB tests assume neighbouring suites' boot recipes (executor lifts test/api-workflows.test.mjs's before/after block).

