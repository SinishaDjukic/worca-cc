# P2 critique — shared graph core + sidecars v2 + schema + store (Fable 5, max effort, 2026-08-27)

Plan reviewed: the frozen v1 snapshot `scratchpad/v1-snapshots/2026-08-26-node-graph-v2-P2-shared-core-sidecars-schema-store.md` (byte-identical to the repo copy at review time; line numbers below are the snapshot's). Cross-plan sheet `reports/xplan-manifest.md` §A/§B-P2 read AFTER the review; findings already covered by a §B edit are marked `[xplan P2-Ex]` and kept only so the executor can verify the edit landed.

## Method (what was actually run)
- Extracted EVERY embedded pure module + test of P2a Tasks 1–7 and 11–12 from the plan, plus P1's `constants.mjs`/`verdict.mjs` (from the P1 plan), the old-branch seed constants (`seed-templates.mjs`/`builtin-workflows.mjs`, byte-identical to P1's copy), and the 11 sidecars = dev JSON ⊕ the plan's Task 9 fragments, into `scratchpad/critique-p2/tree/` and ran `node --test` there (no repo edits, no npm install needed — the shared core has no deps).
- Result BEFORE any fix: ports 8/8, loops 7/7, template 10/10, geometry 10/10, layout 5/5, thumbnail 2/2, agent-meta 11/11 green; **validate 1/23, manifest 8/9, seed guard 0/3 — every `validateGraph` call dies with `TypeError: KINDS.has is not a function`** (F1).
- With the one-token fix (`KINDS.includes`): validate 23/23, manifest 9/9, single-source 2/2; the Task 13 seed drift guard: **0 errors / 0 warnings on all 8 graphs with the plan's own validator and the plan's own sidecars** (the writer had only run the OLD validator), loop-wire pins exactly `FB_WIRE_MAP`; BUT the guard's third assertion fails on its own bug (F2). Extra probes: manifest round-trip (`manifestTemplate` + `manifestPortsFn`) validates 0/0 for all 8 seeds; `autoLayout` idempotent and `thumbnailSvg` NaN-free on all 8; `serializeTemplate` stable on all 8; P1's purity regexes: clean on all nine modules.
- Mutation audit of `validate.mjs`: removing ANY whole rule V1–V21 is caught by `test/graph-validate.test.mjs` (21/21). Fine-grained mutations (exemptions/guards): 14 caught, **7 survive** (F15).
- Repo facts checked by hand (dev @ e6968e15): `claude-runner.mjs` switch = exactly 14 `case '…':` labels (the `ask` arm is an `if`, so the lockstep regex is sound); `config.mjs` `setStep`/`setNodeModel` VALIDATE model ids against the catalog and `EFFORTS = ['medium','high','xhigh','max']`; `ask/proposal.mjs` readers are INJECTED via `createProposalValidator`; `agent-registry.mjs → claude-runner.mjs → preflight/model-env/recoverable-error` is acyclic; `missingColumns` skips absent tables (the B1 minimal seed is sound); no test pins the `## Inputs (from the task prompt)` heading; no registry test deep-equals a whole builtin meta; v1 runners dispatch by agent KEY (`runners.mjs:54-76`) and only the two GENERIC builders read `promptHints` (`phases.mjs:1202,1237`), so the new sidecar hints leave v1 builtin prompts byte-identical.

## Findings

### CRITICAL

**F1 — `KINDS.has` crashes every `validateGraph` call.** Task 3 Step 2, `RULES` V3 (`if (!KINDS.has(n.kind))`, and `[...KINDS].join` is fine). P1's `constants.mjs` exports `KINDS = Object.freeze(['agent', …])` — a frozen ARRAY (P1 Task 5 test even asserts `KINDS.push` throws `TypeError`; P1 Q&A item 11 flagged it "CROSS-CHECK vs P2"). Empirical: 22 of 23 validate tests, the manifest round-trip test, the seed guard and (transitively) the 422 route, plugin import and the composer all throw. Fix (plan-ready): `if (!KINDS.includes(n.kind)) {` — and in Task 0 Step 4 add a SHAPE probe next to the name grep: `node --input-type=module -e "import { KINDS, PORT_TYPES, FLOW_KINDS } from './src/shared/graph/constants.mjs'; if (!Array.isArray(KINDS)) throw new Error('KINDS is not an array')"`. `[xplan P2-E1]` — verify it is applied; the executor's dry-run will otherwise stop at Task 3.

### MAJOR

**F2 — Task 13 seed guard has a self-inflicted failure.** `test/graph-seed-templates.test.mjs` (Task 13 Step 1): `const budgeted = w.config && w.config.maxCycles !== undefined; assert.equal(budgeted, loopWireIds.has(w.id), …)` — for a wire with no `config` the left side is `undefined`, `assert.equal` is strict, so the very first plain wire (`wf_full.w1`) fails: `+ undefined / - false` (reproduced). Fix: `const budgeted = !!(w.config && w.config.maxCycles !== undefined);`. With that the guard is green on all 8 graphs (measured).

**F3 — "exactly five builtins carry prompt hints" is wrong: the plan's own Task 9 makes it SIX.** Task 10 Step 1 (`assert.deepEqual(Object.values(reg).filter((m) => m.promptHints).map((m) => m.key).sort(), ['manualTestsChecklist','manualWebUiTesting','planReviewer','refiner','workspaceReviewer'])`) and the Task 10 prose ("five builtins now carry hints"). Task 9's `agents/implementer.meta.json` block adds `"promptHints": "Work inside the project directory (your cwd). Commit nothing; just edit files and tests."` — required by spec §6 (implementer row: `hints = "Work inside…" (:836)`) and by P3's parity pins (adj-f1 §5). The test therefore fails on a correct implementation; an executor "fixing" it by deleting the implementer hint would break P3. Fix: expected list `['implementer', 'manualTestsChecklist', 'manualWebUiTesting', 'planReviewer', 'refiner', 'workspaceReviewer']`; prose "Exactly six builtins carry prompt hints; the other five stay empty".

**F4 — B3/B4/B6 fixtures use model ids that are not in the catalog and an effort that does not exist.** `setNodeModel` and `setStep` validate against `listModels()` (`config.mjs:400-410, :617-627`: `throw new Error(\`unknown model "${model}"\`)`), and `EFFORTS = ['medium','high','xhigh','max']` (`model-env.mjs:18`; `sanitizeNodeDefaults` drops an unknown effort). Failing lines: B3 test 2 `setNodeModel(projectDir,'wf_g','n_plan',{ model:'sonnet' })`; B4 test 2 `{ model:'override-model' }` / `{ model:'override-model', effort:'low' }`; B4 test 6 `setStep(projectDir,'planner',{ model:'legacy-model' })`; B4 test 7 `{ n_plan: { model:'m2', effort:'low' } }` → `sanitizeNodeDefaults` drops `effort:'low'` (warns) so the deepEqual fails; B6 test 3 `nodes: { n_plan: { model:'sonnet' } }` (400, silently tolerated). Fix: use catalog ids and efforts — `'claude-sonnet-5'` for the overlay, `'claude-opus-5'` + `'max'` for the override, `'claude-opus-4-8'` for the legacy layer, `{ model:'claude-sonnet-5', effort:'medium' }` for the defaults patch (the same ids `test/api-workflows.test.mjs:176` uses). Template `node.config` values (`'tpl-model'`, `'high'`) may stay — `writeGraphWorkflow` does not validate them (see F12).

**F5 — B5 422 test asserts the wrong first rule.** `test/api-workflows-graph.test.mjs` 'an invalid graph is 422…': `bad` adds `w3: n_task.task → n_plan.task`, byte-identical in `(from,to)` to `w1`. V6 (`duplicate wire n_task.task -> n_plan.task`) runs BEFORE V7, so `r.body.errors[0].code === 'V6'`, not `'V7'` (the deepEqual against the shared validator still holds — the identity part is fine). Fix: make w3 a distinct pair into an already-wired input, e.g. `{ id:'w3', from:{node:'n_task',port:'task'}, to:{node:'n_end',port:'result'} }` (task md → result any is type-legal, `n_end.result` already has w2) → `errors[0].code === 'V7'`; or assert `errors.some((e) => e.code === 'V7')`.

**F6 — B7 wires `assertRunnableWorkflow` into Ask's proposal validator BEHIND the injected-reader seam.** `src/core/ask/proposal.mjs:53-60` builds `validateProposal` from `createProposalValidator({ listProjects, readWorkspace, readWorkflow, readGuardrailSet, isGitRepo, pathExists })`; `test/ask-proposal.test.mjs:27-32` injects a fake `readWorkflow` that knows `wf_default` and `wf_review`. The plan's B7 snippet imports the REAL `assertRunnableWorkflow` "beside the existing readWorkflow import" and calls it inside `validateProposal`, so the fake reader no longer governs: `wf_review` → real `readWorkflow` → DB miss → `unknown workflowId "wf_review"` pushed → the existing suite goes red, and the "readers injected so the MCP child can self-correct" design is broken. Fix: add `assertRunnableWorkflow = realAssertRunnableWorkflow` to the `createProposalValidator` deps, use the injected one, and in `test/ask-proposal.test.mjs` add `assertRunnableWorkflow: async (id) => { const wf = await deps.readWorkflow(id); if (!wf) throw Object.assign(new Error(\`unknown workflowId "${id}"\`), { code: 'NOT_FOUND' }); return wf; }` to `deps` (plus one new case: a `{ version: 2 }` row pushes the interim text). The plan's claim "`node --test test/ask-proposal.test.mjs` stays green" is false as written.

**F7 — `PORT_ID_RE` is defined TWICE with two different regexes.** Task 7's `agent-meta.mjs` declares its own `const PORT_ID_RE = /^[a-z][A-Za-z0-9_-]{0,31}$/` (the old branch's / adj-f2 §3's) while P1's `constants.mjs` exports `PORT_ID_RE = /^[a-z][A-Za-z0-9]{0,31}$/` (no `_`/`-`; P1's test asserts `in_1` and `in-1` do NOT match). Spec §3's module table puts `PORT_ID_RE` in `constants.mjs` precisely so there is ONE source; the plan's Task 0 Step 4 even greps for it and then never imports it. Consequence: a plugin/user sidecar port `fix_review` passes the store's `validateMetaV2` but P1's guard/`graph-seed-templates` regex rejects the same id. Fix: `import { PORT_TYPES, MAX_PORTS_PER_SIDE, PORT_ID_RE } from './constants.mjs';` and delete the local constant; SEAM: pick the canonical regex (recommendation below).

**F8 — V2's message text lies about the rule.** Task 3 V2: `` add(`node id ${JSON.stringify(id)} must match /^[A-Za-z0-9_-]{1,64}$/`, …) `` while the test is `NODE_ID_RE.test(id)` = P1's `/^n_[a-z0-9]{1,32}$/` — a user who names a node `planner1` is told it "must match /^[A-Za-z0-9_-]{1,64}$/", which it does. Fix: `` add(`node id ${JSON.stringify(id)} must match ${NODE_ID_RE}`, …) `` (the rule table row and the V2 test regex `/must match/` need no change). SEAM: P1's regex deviates from base spec V2 (`/^[A-Za-z0-9_-]{1,64}$/`); the plan's Q&A should record the tighter id space as a P1 decision (mint `n_`+8 base36; seeds `n_<word>`).

**F9 — The manifest is not "self-sufficient": `node.config` is dropped except `arity`/`awaitAll`.** Task 11 `buildGraphManifest` emits no `config` on any cell and `manifestTemplate` restores only `config.arity`/`config.awaitAll`. `wf_provided-plan`'s task node is `{ kind:'task', config: { planStoreSeed: true } }` (seed-templates.mjs:121, A2 parity-mandatory); P3's `runTaskExecution` reads `node.config.planStoreSeed` (P3 :2915) and P4's `resolvedFromManifest` (P4 :1836-1884) rebuilds the template from `manifestTemplate(manifest)` with flow-node `config: { arity: mn.arity }` — spec §15 says v2 resume "never re-reads the workflow row", so after a resume the Task card has lost `planStoreSeed` (latent today because the Task card never re-fires after t0, but the manifest is the ONLY persisted copy of the topology and P6/P8 History renders from it). Fix: every manifest cell carries `config: { ...(node.config || {}) }` (authored, unknown keys preserved — V17's "preserved and ignored" promise), `manifestTemplate` restores it verbatim (drop the arity/awaitAll special cases), test 'manifest head and node cells' pins `plan.config` deepEqual the template's, and the round-trip test uses `wf_provided-plan`'s shape (`planStoreSeed`) so the loss is caught. SEAM: P4 `resolvedFromManifest` then reads `mn.config` instead of `{ arity: mn.arity }` (A1 `nodes[id].config`).

### MINOR

**F10 — v1 system-prompt bytes change for 7 builtins in P2a.** Task 9 replaces `## Inputs (from the task prompt)` + its bullets with `## Ports` in 7 `.md` bodies (and inserts the block in 4 more) while the v1 engine is live and `resolveAgentBody` feeds the whole file as the system prompt. Spec §6 mandates exactly this in P2a and nothing pins those bytes (verified: no test/src reference to the heading), so it is conformant — but the commit body should say "v1 system prompts for clarify/planner/refiner/planReviewer/decomposer/implementer/reviewer/workspaceReviewer/manualTestsChecklist/manualWebUiTesting/workspaceScanner gain/replace a `## Ports` section; task prompts unchanged" so a bisect of a behaviour change lands here.

**F11 — `writeGraphWorkflow` UPSERT resets `origin` to NULL on conflict.** `ON CONFLICT(id) DO UPDATE SET … origin = excluded.origin` with `origin` = `tpl.origin ?? null`; v1's `writeWorkflow` never touches `origin` on conflict, and spec §4 says the composer's Save on a loaded row "sends its id" — saving over a plugin-owned `wfp_*` row (P7 item 18 keeps plugin rows on their own INSERT) silently detaches it from `removePluginWorkflows`' guard. Fix: `origin = COALESCE(excluded.origin, workflows.origin)` and a one-line test (`origin` survives a re-save that omits it).

**F12 — v2 `POST /api/workflows` skips the catalog validation the v1 path applies.** The v1 branch runs `nodeDefaultsError(node.defaults, models, …)` per node (`ui/server.mjs:3150-3156`); the v2 branch validates topology only, so a body with `config: { model: 'nope' }` on an agent node is saved and reaches `resolveGraph` → the spawn. Fix: before `validateGraph`, `for (const n of graph.nodes) if (n?.kind === 'agent' && n.config) { const err = nodeDefaultsError(pick(n.config, ['model','effort','fanOut','askQuestions']), models, \`node "${n.id}"\`); if (err) return badRequest(res, err); }` (same 400 texts as PATCH …/defaults).

**F13 — V9 double-reports flow-card inputs.** P1's `gatePorts` inputs and `END_PORTS.result` are `required: true` (A30 "✓"), and V9 iterates every node whose meta resolved — flow cards included — so an unwired `in2` yields BOTH `required input 'n_and.in2' is unwired` (V9) and `and node 'n_and' has unwired input 'in2' — every inK must be wired` (V12); an unwired End yields V9 + V21. Fix: V9 `if (n.kind !== 'agent' || !metaOf(n.id)) continue;` (base spec V9 is the agent-input rule; V12/V21 own the cards). No test change needed (the V12/V21 tests use `.find`/`.some`).

**F14 — V12 has no upper bound on `arity`.** `gatePorts` clamps to `MAX_PORTS_PER_SIDE` (8) and `LIMITS.maxArity = 8`, but V12 only checks `arity < 2`, so `arity: 99` validates, is stored, ships `arity: 99` in the manifest, and only `in1..in8` are ever checked for wiring. Fix: `if (arity !== undefined && (!Number.isInteger(arity) || arity < limits.minArity || arity > limits.maxArity)) add(\`${n.kind} node '${n.id}' needs an integer arity between ${limits.minArity} and ${limits.maxArity} (got ${JSON.stringify(arity)})\`)` — keep the V12 test's regex loose (`/needs an integer arity/`) or update the literal.

**F15 — Seven guard/exemption mutations survive the suites (validate + seed guard + manifest).** Measured by editing one line at a time: (1) V18(c) synthesized-`await` exemption removed → 0 failures (no fixture wires an always-source into `await` on a node that already has one always payload input); (2) V19 `end.result` exemption removed → 0 (no blocking output is wired into End anywhere; seeds use `pass`); (3) V19 `await` exemption removed → 0 (the `o` fixture wires `r2.pass` (clean) into `await`); (4) V15 "unresolved meta is never an entry" removed → 0; (5) V5 `metaOf` guard removed → 0; (6) V11 "unresolved member can start" branch flipped → 0; (7) V7 restricted to `liveWires` (meta-dependent) → 0 — the "counted over ALL wires, no dependency on meta resolution" promise is untested. Plan-ready fixture additions for `test/graph-validate.test.mjs`: (a) `n_task.task → n_impl.await` on top of `n_cl.answers → n_impl.task` (2 always payloads) expecting exactly ONE V18 with count 2; (b) `n_rev.review → n_end.result` (replace w6) expecting no V19; (c) `n_rev.review → n_r2.await` expecting no V19; (d) an unknown-key node `n_x` with `n_x.out → n_plan.answers` — assert `n_plan` still warns unreachable? no: assert V15 lists `n_x` AND does not treat it as an entry (add `n_orphan` downstream only of `n_x` and expect BOTH unreachable); (e) V5: a wire from an UNKNOWN-key node's bogus port must yield only V4 + no V5 port message; (f) V11: cycle with one member unknown-key ⇒ no V11; (g) V7: two wires into `n_ghost.in` where `n_ghost` has an unknown key ⇒ V7 still fires. Also: 'V9 … loop inputs exempt' is vacuous — the fixture's loop inputs are already `required:false`, so removing the exemption changes nothing; keep the exemption (it protects a hand-built portsFn) but drop the misleading assertion message or add a `required:true, loop:true` port to `REG.impl`.

**F16 — Task 9 executability.** (a) The JSON fragments say "add ONLY the keys below" but not WHERE; if the executor appends after `"order": N` (the natural place) the previous last line gains a comma and Step 3's `git diff agents/ | grep '^-'` check ("shows ONLY the seven replaced headings") fails on 11 `-  "order": …` lines. Say: insert the fragment immediately after the `"key": "<key>",` line (every fragment already ends with a trailing comma). (b) `agents/worca-cc-decomposer.md` has an H1 `# Your role` at :8 before its first `##` (:15) — "insert right after the intro paragraph, before the next `##`" is ambiguous; say "after line 7, before `# Your role`" (or "after `# Your role`'s paragraph, before `## Draft vertical slices`") explicitly, and give line anchors for clarify (:9), planner (:9), implementer (:9). (c) Step 2's sanity script contains a no-op placeholder loop (`if (!(k in raw) && f !== 'x') { /* … */ }`) — replace with `if (!(k in raw)) { bad += 1; console.log(f, 'lost v1 field', k); }`.

**F17 — B1 Step 4 contradicts itself.** "`grep -rn -A1 "user_version" test/*.mjs | grep -w 22 | grep -v db-migrate-v23` — Expected: empty output both times" — but the same step says the two `17 -> 22` comments hit BEFORE the edit. Fix: "Expected: BEFORE — exactly the two comment lines (`ask-db-schema.test.mjs:80`, `diff-comments-schema.test.mjs:72`); AFTER — empty."

**F18 — B7's run test uses `WORCA_HOME` as the project dir.** `POST /api/run { projectDir: homeDir, … }` starts a real mock run INSIDE the sandbox home (store + pipeline dirs nest under the project) and the file's `after` removes `homeDir` while the orchestrator may still be writing; the precedent (`test/api-workflows.test.mjs:104-112`) uses a separate `mkdtemp('worca-cc-run-')` project dir and `mock: true` in the body. Fix: same recipe for all three B7 requests.

**F19 — Manifest `uiPhase` ignores `meta.uiPhase`.** `uiPhase: node.kind === 'agent' ? (UI_PHASE[node.key] || node.key) : node.kind` drops the sidecar's v1-schema `uiPhase` that `resolveWorkflow` honours (`workflows.mjs:412` `UI_PHASE[key] || meta.uiPhase || key`), so a custom agent whose sidecar declares `uiPhase: 'review'` loses its stepper bucket in the shim. Fix: `UI_PHASE[node.key] || (typeof meta?.uiPhase === 'string' && meta.uiPhase) || node.key` (the merged meta still carries the v1 field until P8).

**F20 — `deleteAgent` now refuses on ARCHIVED references** (`listWorkflows({ includeArchived: true })`, adjudicated C180 accept). Note only: an agent referenced solely by an archived (unrunnable) template cannot be deleted until the archived row is deleted via `DELETE /api/workflows/:id` — the REFERENCED message should say so (`… — delete or edit those first (archived rows count)`).

**F21 — Spec §7.4's await formula also fails for 0-input agents.** The plan notes the height caveat (`110.5`, not `119.5`) but not the anchor: with `nIn = 0` the zone rule gives `await` at `y + 65 + 24·nOut`, not `y + 74 + 24·(nIn+nOut)`. Pin it (`portAnchor(bare, agentPorts(0,1), 'await', 'in').y === 89`) and list it beside the height caveat so P5's CDP script does not "correct" the code.

**F22 — Doc drift inside the plan.** Task 3's fixture comment says the real-sidecar helper "arrives in `test/helpers/graph-ports.mjs` at Task 9" — it is Task 10 (Step 4). Task 13 tells the executor to "append" `import` lines at the bottom of `test/graph-seed-templates.test.mjs` — legal ESM (declarations hoist) but say "add beside P1's imports at the top".

**F23 — `structuredClone` + frozen inputs (post-xplan E6).** Fine on Node ≥ 17 and for `Object.freeze`d rows (the clone is mutable), but `readWorkflow('wf_default')` returns the v1 constant → `stored.version !== 2` throws BEFORE the clone; the alias row (P4) spreads `GRAPH_DEFAULT_WORKFLOW` — a deep-frozen constant — so the `structuredClone` in E6 is load-bearing, keep it.

**F24 — `agent-meta.mjs` is stricter than v1 `normalizeMeta` on `runnerType`.** A v2 sidecar without `runnerType` is rejected (`runnerType must be one of …`) where v1 defaults to `producer`. Rule-for-rule with the old branch and adj-f2 §3, and P7's editor always writes it — accepted; record in Task 7 as an intentional divergence from v1 leniency.

**F25 — `manifest.mjs` imports `layout.mjs` → `geometry.mjs` into the engine path** (for `rankNodes`). Pure and harmless; note that the engine now loads the geometry constants. No change.

## Spec conformance (section by section)

**§3 module table, export by export.** `constants.mjs`/`verdict.mjs` are P1's (all 14 names present; `KINDS/FLOW_KINDS/PORT_TYPES` arrays → F1). `ports.mjs`: all 9 exports, signatures match the table (`resolveOrOutType(tpl, portsFn, orId, seen)`, `firedOutputs(ports, verdict)` dual-shape = A10) ✓. `loops.mjs`: `tarjanSccs`, `classifyLoops → {loopWireIds, loopInputs, sccOf, launchOrder}` ✓. `validate.mjs`: `RULES` (21, numeric order, V22 absent), `validateGraph → {ok, errors, warnings}`, `Issue` (+`wireIds` on V12 only — C153 accepted), `formatIssue` ✓. `template.mjs`: all 10 ✓ (`canWire` codes = A24). `geometry.mjs`: every §7.4 constant + `GEOMETRY_CSS_VARS` (10 vars) + all 12 functions ✓. `layout.mjs`, `thumbnail.mjs`, `agent-meta.mjs` (+injected `mockWriterRoles`, C164), `manifest.mjs` (+`UI_PHASE` copy) ✓. Engine-side `registry-ports.mjs` = `portsFnFor(indexByKey(registry))` ✓. Browser `model.mjs` = re-exports only, identity-tested ✓. Import convention (three `..`, no absolute specifiers, header depth note) ✓. Purity: clean under P1's regexes (measured) ✓. Fetch policy (no embedded table; `GET /api/agents` passes objects through — verified `ui/server.mjs:3907-3913`) ✓. Duplication guards: 422 = validator issues by construction ✓ (test F5 aside); seeds validated against the REAL sidecars ✓ (Task 13); `test/helpers/graph-ports.mjs` loads the real `agents/*.meta.json` ✓; one `bezierPath` ✓; CSS `--gv-*` set-equality guard is P5's (not in P2 — correct, `style.css` has no `--gv-*` yet). Deviation: `PORT_ID_RE` not taken from `constants.mjs` (F7).

**§4 storage / API / consumers, bullet by bullet.** `graph` holds `{nodes, wires, canvas?}` only ✓ (B2); `archived_at` ✓; v2 rows write `version=2, steps='[]', feedbacks='[]'` ✓; `rowToTpl` version-aware ✓; `writeWorkflow` untouched ✓; `writeGraphWorkflow` UPSERT + un-archive ✓, id rule (`SAFE_WORKFLOW_ID`, not `wf_default`/`wf_default_v2`, else slug) ✓ — but `origin` reset on conflict (F11); `listWorkflows({includeArchived})` ✓; `readWorkflow(id,{includeArchived})` → null when archived ✓; `assertRunnableWorkflow` codes/texts ✓ (A21); `GET /api/workflows/:id` → 404 via the gate ✓; `?archived=1` ONLY archived rows ✓; `deleteWorkflow`/`removePluginWorkflows` untouched ✓. Overlays: `config_workflow_wires` DDL + PK order ✓; `setWireCycles` coerces ≥ 1 like `setFeedbackCycles` ✓ (byte-identical coercion); `resolveRunConfig → {nodes, wires, feedbacks}` (keeps `feedbacks` till P8 — C177) ✓; precedence chain + "effort never inherits across a model change" mirrors `workflows.mjs:427` ✓; per-wire `overlay → wire.config → 3` ✓; `node.defaults` superseded, PATCH …/defaults rewrites `graph.nodes[].config` for the 4 tunables, non-tunables preserved ✓. API deltas: `GET /api/config` emits both keys (no server edit — verified `readRunConfig` is returned whole) ✓; `PATCH /api/config` `wires` needs `workflowId` ✓; `DELETE /api/config/workflow` clears wires via `resetWorkflowConfig` ✓; `GET /api/workflows` order unchanged ✓; `POST /api/workflows` branches on `body.version === 2` → 422 `{error, errors, warnings}` ✓ (catalog validation gap F12). Consumers: `resolveWorkflow` throws the §4 sentence ✓; Ask `shapeWorkflow` derives steps/feedbacks (rank = A19) ✓; `proposal.mjs` uses `assertRunnableWorkflow` — but not through the injected seam (F6); `agent-store` delete guard + `referencedPluginAgents` walk `nodes[].key` ✓; `buildNodeConfigRows`/`buildFeedbackRows` guard ✓; v1 composer list hides v2 rows (an extra the spec does not ask for, harmless) ✓.

**§5.5 `MOCK_WRITER_ROLES`.** 14 roles = dev's 14 `case` labels (checked); `ask` arm is an `if`, correctly outside; `MOCK_ROLE_CLARIFY`/`MOCK_ROLE_DECOMPOSER` used as case labels; structural lockstep test's regex is sound against the current file (no stray `case '…':` elsewhere in `claude-runner.mjs`) ✓.

**§5.8 manifest.** Shape matches the sample (`version, template{id,name}, graph{nodes,wires}, bookends, steps, feedbacks`); `ports.await` boolean, await not under `inputs` ✓; OR `out.type` resolved ✓; `maxCycles` on `loop:true` wires only, overlay-merged ✓; icon sanitized ≤ 2 KB, script-ish dropped whole ✓; model/effort/askQuestions/awaitAll on agent cells ✓ (+`fanOut`, additive); shim cells = v1 `buildStepperManifest` shape byte-for-byte (`{id,key,uiPhase,label,color,sub,cycles,model,effort}`) ✓; `feedbacks` ✓; `template:{id,name}` stamped by the builder ✓ (A2). Deviations: `config` dropped (F9); `meta.uiPhase` ignored (F19). Shim ranks: measured cells for the three OR seeds put `n_or` in the FIRST cell beside `n_task` (both in-wires are loop wires, so the OR has no forward predecessor) — the v1 painters will draw an OR chip in cell 1 with `key:null`; consistent with A19, but P4/P6 should know the OR lands there (not next to the implementer).

**§6 the 11-builtin table, row by row (inputs/outputs/when/filename/store/as/directive/loop/expands/verdict/mockRole/capabilities).** All 11 rows match the table exactly, including: planner `revise` directive = the REVISE block; refiner's two arms on ONE template `{base}{vsuffix}.md` [project] + `refine-review-cycle{cycle}.json`; planReviewer `{base}-plan-review.md` [project] + `pass:void clean`; decomposer `decomposition.json` (artifactKind `decomposition`, C167); implementer `fix (as fix-review + directive) · task (json, expands + directive) · plan (+ implement-arm directive)` in THAT order, `sideEffect:'code'`, `workspaceFanOut`, strategy `task`, hints (F3 counts it); reviewer `done:void as:worktree`, NO `workspaceFanOut`; workspaceReviewer `workspaceVariantOf:'reviewer'`, `workspaceFanOut`, hints = UNION line, no `wantsRequest` (spec: "no wantsRequest") ✓; manualTestsChecklist hints with `{diffInstruction}` (C166); manualWebUiTesting `webui-review-cycle{cycle}.md` [run] artifactKind `webui` + verdict; workspaceScanner `placeable:false`. Dual shape: fragments ADD keys only; v1 `consumes/optionalConsumes/produces/connectsTo/loopSource` untouched ✓ (verified by merging on the dev files). The merged 11 validate under `validateMetaV2` with the vocabulary injected (measured, `realAgentMetas()` throws on none). Every port id is `await`-free ✓. `.md` bodies: 7 replacements + 4 insertions, every block opens with the mandated sentence, verdict/clarify/fan-out/workspace/graph-tooling sections untouched, no body mentions `await` ✓ (anchoring gaps F16).

**§7.4 geometry.** Every constant verified against the frozen list; `ROW0 = 56`; closed forms reproduced numerically: Agent(2,2) = 191.5, Task/End = 110.5, OR₂ = 167.5, AND₂ = 134.5, Agent(nIn,nOut) = 95.5 + 24(nIn+nOut) for nIn ≥ 1; anchors `task(400,136) fix(400,160) plan(620,193) review(620,217) await(400,250) n_task.task(280,199) n_end.result(760,199)` = the CDP measurement; bezier `M 280 199 C 334 199, 346 136, 400 136`, mirrored `C 346 …, 334 …`, loop bow `56 + 0.2|Δy|`, `dx` clamp [48,160]; `fitBounds` → `{z:1, tx:120, ty:104.25}`; `zoomMax` defaults to 1 (§7.6) ✓. The plan correctly documents the 0-input deviation from the spec's closed form (110.5) — the zone rule is the spec's own; the await-anchor twin of that caveat is missing (F21).

**§10.1 V23.** DDL byte-equal; `INCREMENTAL_COLUMNS` gains `workflows.graph/archived_at`, `pipelines.outcome`, the 7 `pipeline_steps` ledger columns; `INCREMENTAL_TABLES.config_workflow_wires`; ladder `if (current < 23) applySchemaV23(db)` = `repairSchemaGaps(db, schemaGaps(db))` — reconcile-safe on the residue (old PK order untouched, `missingColumns` skips already-present columns; `missingColumns` also skips ABSENT tables, so the test's minimal seed is valid); `SCHEMA_VERSION` 22→23; `EXPECTED_TABLES` 18→19; the pin sweep's only hits are the two comments ✓ (wording F17).

**§12 / §16.** Test names are the contracted ones (`test/graph-<module>.test.mjs` ×9 + the allowed extras, A31) ✓; Task 0 = branch check, `npm ci`, old-branch fetch, sentinels, baseline ✓; every task has Files/Interfaces/TDD steps/expected output/commit message ✓; Q&A carries only user decisions + labelled planner defaults (no invented user answers) ✓; product name "worca" in every user-facing string ✓ (the only `worca-cc` strings are paths/env/temp prefixes).

## Semantics of the embedded core (rule by rule, against Amendment f)

- **V1–V21**: V7 universal single-wire incl. `await`/`inK`/`result`, counted over ALL wires, meta-independent ✓ (untested — F15); V8 per-wire equality with `any` accepting everything, OR resolution via `resolveOrOutType` (seen-set, chained ORs), unresolvable ⇒ skip, and or.out's OUTBOUND wires compared against the resolved type ✓; V12 explicit-arity-only (flowPorts defaults 2), every `inK` wired, OR homogeneity with `wireIds` ✓ (no upper bound — F14); V16 counts a wired `await` ✓; V18 agents only, non-void non-loop, exemptions a–d ✓ (c untested — F15); V19 exempt AND/OR `inK`, End `result`, agent `await`; Combine still warns ✓ (end/await untested — F15); V20/V21 exactly one Task/End, Task zero inputs + ≥1 wire, End zero outputs + `result` wired ✓; V4 unknown vs un-ported vs `placeable:false` ✓ (texts → A20); V5 ports checked only where meta resolved ✓; V9 required non-loop, "wired" = both ports resolve ✓ (scope — F13); V10/V11 over nontrivial SCCs incl. self-wired singletons ✓; V13 unknown `when`, conditional without verdict, `maxCycles` int ≥ 1 and loop-wire-only ✓; V14 expands ⇒ json ✓; V15 entries = zero-input RESOLVED nodes ✓; V17 per-kind known keys ✓. All whole-rule removals are caught (measured).
- **`classifyLoops`**: loop wire = both ends in one nontrivial SCC or a self-wire AND source `when:'blocking'` (so `or.out → fix` is plain) ✓; `loopInputs` wiring-independent from meta `loop:true` ✓; `launchOrder` = Kahn over the condensation with min-member-id tiebreak, iterative Tarjan, sorted adjacency — deterministic ✓; dangling endpoints/unknown keys filtered, never thrown ✓. Seed pins reproduced exactly (`FB_WIRE_MAP` = loop wires on all 8).
- **`resolveOrOutType`**: inbound walked by `inK` index then wire id, first resolvable, chained OR recursion with the seen-set → cyclic OR chain returns null ✓.
- **`canWire`**: Map lookup + `findPort` + `inboundWires` + type check — no Tarjan on the pointer path ✓; order `same node → unknown port → already connected → type → or-homogeneity` matches the chip semantics (V7 before V8 so a second wire into a wired input never shows a type chip) ✓.
- **`template.mjs`**: `normalizeTemplate` keeps unknown config keys (V17 promise), drops non-object nodes/wires, `key` only on agents, half-written `canvas` dropped; `serializeTemplate` key order `id,name,version,domain,nodes,wires[,canvas]` / node `id,kind[,key],x,y,config` / wire `id,from,to[,config]` — stable through JSON on all 8 seeds (measured) ✓.
- **`geometry.mjs`**: zone model (inputs → outputs → await → caption; separators only BETWEEN emitted zones) reproduces every §7.4 number (above) and degrades correctly at nIn = 0; `bezierPoint`/`bezierMid` sample the same cubic; `hitWire` samples 48 segments at 6 px ✓.
- **`layout.mjs`**: longest-path ranks over NON-loop edges (self-wires excluded), sorted Kahn frontier, bounded fallback for a residual cycle, 2 barycenter sweeps with stable ties, `x = 60 + rank·300`, `y` stacked from the SNAPPED row so re-running reproduces itself — idempotent and deterministic on all 8 seeds (measured) ✓.
- **`thumbnailSvg`**: one `<g transform>` in world space, wires before rects, numbers only, `fill="none"`, no ids/names, NaN-free on dangling input ✓.
- **`agent-meta.mjs` vs adj-f2 §3 texts**: every listed rule present verbatim; the additional rules (`meta must be an object`, `key is required`, `order must be a number`, `verdict …`, `<side> must be an array`, `each port must be an object`, void-port filename/store on either side) are the old branch's, not inventions; the two warnings (loop ⇒ required:false; unknown mockRole) are warnings, never errors ✓; injected vocabulary: no Set ⇒ the role rides through (P7 calls with no options — consistent) ✓; capability defaults applied at read time, absent fields stay absent (diffable entry) ✓. `PORT_ID_RE` duplication (F7).
- **`normalizeMeta` merge + `agent-store.updateAgent` round-trip**: `{...existing, ...rawMeta}` re-enters `normalizeMeta` → `raw.metaVersion === 2` → `normalizeAgentMeta` over already-normalized ports is idempotent (defaults re-materialize identically, `portSummary` recomputed, computed `origin/agentPath/descriptionDerived` dropped by the fixed key set) ✓; invalid v2 ⇒ `null` ⇒ store 400 `invalid agent metadata` (P7 adds the verbatim texts) ✓. Every v1 field survives (`base` first) ✓; no existing registry test deep-equals a whole builtin meta (checked) ✓.
- **`MOCK_WRITER_ROLES` lockstep**: correct (above).
- **`buildGraphManifest` shim**: ranks = `rankNodes` (loop wires excluded), cells ordered by `launchOrder`, flow nodes `key:null`, `uiPhase = kind`; `cycles` = a WIRED `loop:true` input; `feedbacks` = loop wires with the merged `maxCycles` — what `app.js`'s `manifestFor/buildRunGraph/locateInManifest` paint without a `stepper.version` check ✓ (A19). `manifestPortsFn` re-synthesizes `AWAIT_PORT` from the boolean and a `verdict:{filename:''}` stub from any conditional output (A10) — round-trips 0/0 for all 8 seeds (measured) ✓.
- **`registry-ports.mjs`** ✓; **purity** ✓; **`model.mjs`** identity test ✓.

## P2b (schema + store) — semantics review

- **B1**: gap-heal on the residue is sound (walked `db.mjs:725-870`); `ALTER … DROP COLUMN` in the self-heal test needs SQLite ≥ 3.35 (node:sqlite ships 3.4x) ✓; the ladder runs V23 for DBs stamped 15–21 too — a column-guarded no-op (adj-e §4) ✓.
- **B2**: `readRaw`'s `Array.isArray(tpl.steps)` legacy gate is correctly split per version; `ROW_COLS` selects the new columns AFTER B1 ✓; `resolveWorkflow` throws before any config read ✓; `assert.rejects(…, /^Error: …$/)` matches Node's `String(err)` semantics ✓. `origin` reset (F11).
- **B3**: `readWorkflowsMap` third bucket + `resolveRunConfig` shape + `resetWorkflowConfig` delete ✓; catalog ids in the test (F4).
- **B4** (as adjudicated by A1/E5–E13): substitution on the CLONE before `reg[key]`; `agentsByKey` keyed by resolved key; `classifyLoops(tpl, registryPortsFn(agentsByKey))` now walks resolved keys ✓ (the pre-edit version would have reported the substituted reviewer `known:false` on workspace runs — the xplan fix is correct and necessary); legacy layer keyed by AUTHORED key ✓; `wf_default` legacy branch is dead code until P4's alias (the test covers only "saved rows ignore it") — acceptable; refusal texts (A20) ✓; workspace port-signature check excludes `as/filename/store` ✓; `fanOut` forcing on `workspaceFanOut` ✓ (A36). `setWorkflowNodeDefaults` v2 arm keeps `awaitAll/arity/planStoreSeed` ✓ (C179); `workflowNodeDefaults` v2 arm ✓.
- **B5**: 422 identity test sound (F5 aside); `GET /:id` 404 mapping ✓; `?archived=1` uses `isTruthy` (hoisted function declaration at `ui/server.mjs:4758`) ✓; PATCH …/defaults unchanged ✓. Catalog gap (F12).
- **B6**: `PATCH /api/config` `wires` arm mirrors the `feedbacks` arm ✓; `GET /api/config` untouched ✓.
- **B7**: `POST /api/run` gate (status → A16), CLI `fail()` (exists, exit 2), Ask (seam — F6). SQL correctness: no `assert.deepEqual` on a raw `node:sqlite` row anywhere in P2b (rows are read field-by-field or re-parsed) ✓; transactions via `tx()` ✓; `writeGraphWorkflow` idempotent (UPSERT) ✓; `SCHEMA_VERSION` pin sweep ✓; `EXPECTED_TABLES` 19 ✓.
- **B8/B9**: `shapeWorkflow` v2 arm keeps the LLM-facing keys in order ✓; `referencedPluginAgents` selects + walks `graph` ✓; app.js guards ✓ (regexes match the real function bodies: `buildNodeConfigRows(workflow, …)` starts with `Array.isArray(workflow && workflow.steps)` at `app.js:2670`).

## Test quality summary
- Per-rule mutation-proofing: 21/21 whole-rule removals caught; 7 exemption/guard mutations survive (F15, fixtures given). Fixture graphs exercise their own rule (e.g. V11's deadlock fixture is also a V19 warner but the assertion is on V11; V10's uses a clean-source loop; V13's uses a verdict-less registry) ✓.
- Vacuous assertions: V1's `codes(...).filter(...).length ? ['V1'] : []` construct (works, but write `assert.ok(V(null).errors.some((e) => e.code === 'V1'))`); the V9 "loop inputs exempt" line (F15); Task 9 Step 2's placeholder loop (F16).
- Ordering dependencies: Task 3's test comment names Task 9 for the helper that Task 10 creates (F22) — no import of a later-task module anywhere; Task 13 (seeds) after Task 10 (helper) ✓; B-tasks after B1 (columns) ✓; `api-workflows-graph.test.mjs` tests are order-dependent within the file (rows accumulate; the `?archived=1` assertion expects exactly `['Arch Me']`) — fine under node:test's sequential default, say so in a comment.
- Fixture bugs: F2 (`undefined` vs `false`), F4 (catalog ids/efforts), F5 (V6 before V7), F3 (five vs six). No `freshStore` key-class bug found (`useTempHome` used everywhere a DB is touched; B8 is pure).
- Boot recipe lifting: "copy the before/after block of `test/api-workflows.test.mjs`" is exact (it defines `homeDir`, `base`, `WORCA_MOCK=1`, `useTempHome(after)` outer isolation) ✓; B7's extra requirement for a run is the separate project dir (F18).
- jsdom traps: none (B9 deliberately uses a source-level guard instead of booting `app.js`).

## Executability by a zero-context implementer
- Self-contained: every rule text, port table, formula, DDL and message is embedded; no "see spec" placeholders; no symbol is used before it is defined (checked the import lines of every embedded module against the P1 exports and dev's `workflows.mjs`/`config.mjs` exports: `readConfig`, `resolveRunConfig`, `loadAgentFile`, `DEFAULT_AGENTS_DIR`, `DEFAULT_MAX_CYCLES`, `tx`, `prepare`, `slugify`, `normDomain`, `isSafeWorkflowId`, `sanitizeNodeDefaults` all exist as module-local or imported names).
- Task 0 sentinels match P1's literal exports (`export class RunHarness`, `export const SEED_TEMPLATES`, the two shared files); the 14-name grep is right but does not probe SHAPES (F1 fix adds it). The fallback texts ("if `gatePorts` returns a bare array…", "if `PORT_TYPES` is a Set…") are unnecessary given P1's actual module (both are as the plan's code assumes); the ONE shape that differs (`KINDS` array) has no fallback — F1.
- Split point + B0 entry: clean (B0 re-checks branch, deps, P2a sentinel, baseline); B2's `ROW_COLS` depends on B1 (ordered) ✓.
- Commit hygiene: one commit per task, `worca: Node-graph v2 P2 — …`, nothing under `docs/superpowers/**`; Task 10 asks for a commit-body note on `ui/server.mjs:3907-3913` ✓.
- Size: 26 tasks; Task 3 (validator, ~450 lines code + 280 lines test) and Task 9 (22 files) are the largest but each is one coherent unit with an executable stop (mutation audit / `ALL 11 VALID`); nothing is unsafely large. Task 11 (manifest) borrows nothing and is fully specified.
- Ambiguities: F16 (Task 9 anchors), F17 (B1 sweep wording), F22 (helper task number, import placement). Post-xplan E6 introduces `structuredClone` — Node ≥ 22 ✓.

## v1 safety during coexistence
- `normalizeMeta` merge: every registry consumer/test deep-equals only v1 fields or reads named keys (grep over `test/agent-registry*.test.mjs`, `agents-meta`, `api-agents`, `agent-store`: no whole-object equality on a builtin); the ONLY red test is the `:90-99` block the plan rewrites (and F3 makes the rewrite red again). `agent-gen.mjs:91` normalizes a MOCK/LLM-written v1 meta — unaffected (no `metaVersion`). `GET /api/agents` payload grows by the v2 fields; `app.js`'s Agents view ignores unknown keys ✓.
- Sidecar edits vs `phases.mjs`: v1 builders never read `promptHints` (only the generic builders at `:1202/:1237`, which serve custom keys), so the five/six new hints — including the `{diffInstruction}` token — never reach a v1 prompt; the token is a P3 executor concern only ✓. The `.md` system-prompt edits are a REAL v1 byte change (F10, spec-mandated).
- `workflows.mjs`: `resolveWorkflow` gains one early throw for `version === 2` (unreachable on dev data until a v2 row exists); `readWorkflow(id)`/`listWorkflows()` keep their zero-arg shapes; the archived filter is inert until V24; `writeWorkflow` untouched; `buildStepperManifest` untouched ✓.
- Server routes: `POST /api/workflows` v1 bodies take the old path (`body.version === 2` gate); `GET /api/workflows/:id` 404 text for an unknown id changes from `workflow not found` to `unknown workflowId "<id>"` — `test/api-workflows.test.mjs:57-61` only asserts `.error` is truthy ✓; `POST /api/run` unknown-id text unchanged ✓; `GET /api/config` unchanged ✓.
- `GET /api/workflows` now lists v2 rows to the v1 New-Pipeline picker: `workflowPickerLabel` reads `name/origin` only; `buildNodeConfigRows/buildFeedbackRows` are guarded (B9); the v1 composer's saved-list painter iterates `item.steps.length` (`app.js:2533`) — protected ONLY by B9's `composer.saved` filter, so B9 must land in the same half as B5 (it does) ✓.
- `agent-registry.mjs → claude-runner.mjs` import: acyclic (`claude-runner` imports only `model-env`, `recoverable-error`, `preflight` + builtins); `config.mjs`/`agent-store`/CLI now transitively load `claude-runner` at import — no side effects at module scope ✓.

## Verdict
**Executable after fixes: YES.** CRITICALs: **F1** (`KINDS.has` — already scheduled as xplan P2-E1; confirm applied). MAJORs that will otherwise turn the dry-run red on a CORRECT implementation: F2, F3, F4, F5, F6; design-level MAJORs: F7, F8, F9. Everything in P2a's pure core is otherwise sound — the embedded modules pass their own suites, the seed drift guard is 0/0 on all 8 graphs with the plan's validator + sidecars, whole-rule mutations are all caught, and the geometry reproduces the CDP measurement to the pixel.

## Seam recommendations for the adjudicator (aligned to xplan §A; disagreements carry evidence)
1. **A30 ✓** — P2-E1 is exactly the fix; add the Task 0 shape probe (F1) so the next `.has` cannot recur.
2. **NEW — `PORT_ID_RE` (F7)**: agent-meta.mjs must import it from `constants.mjs`. Canonical regex: keep P1's strict `/^[a-z][A-Za-z0-9]{0,31}$/` (mint/seeds/builtins all comply; P7's editor hint already says "lowerCamel"), and amend adj-f2 §3 / base spec §5's `_-` text in §15's deviation index. If the adjudicator prefers the spec's looser regex, P1 Task 5's test line (`in_1`, `in-1` doNotMatch) must flip instead — one of the two must change.
3. **NEW — `NODE_ID_RE` message (F8)**: message = `${NODE_ID_RE}`; record P1's `n_`-prefixed id space as a deliberate deviation from base spec V2 in the plan's Q&A.
4. **NEW — manifest `config` (F9)**: every manifest cell carries `config: {...node.config}`; `manifestTemplate` restores it verbatim; P4 `resolvedFromManifest` reads `mn.config` (replaces `{ arity: mn.arity }`); A1's `nodes[id].config` then has ONE source on both the fresh and the resume path. Spec §5.8 shape gains `config` (additive).
5. **NEW — Ask proposal seam (F6)**: `assertRunnableWorkflow` becomes an injected dep of `createProposalValidator` (default = the real one); the xplan's "P2 B7 owns the gate" (A32) stands, but through the seam.
6. **A16 ✓ (400)** — agree; dev's route is already 400 and the UI shows `error` verbatim for any 4xx. No 409 anywhere.
7. **A19 ✓ (rankNodes)** — agree, with one operational note for P4/P6: measured shim cells put the OR card in cell 1 beside Task on `wf_full`/`wf_provided-plan`/`wf_full-no-decompose` (both in-wires are loop wires); the v1 painters will draw a `key:null` OR chip there until P6.
8. **A20 ✓** — agree; note E6 also changes `resolveGraph`'s not-found text to `unknown workflowId "<id>"` (consistent with A16) — P4's `_resolveTopology` error mapping should expect it.
9. **A1 ✓** — agree; the xplan edits are correct and necessary (authored-key template vs substituted index would drop the workspace reviewer's loop). Residual: E13's new assertion `g.loops.loopWireIds.size >= 1` is weak — pin `['w5']`.
10. **NEW — V9 scope (F13)**: keep A30's `required:true` on gate/End inputs and restrict V9 to `kind === 'agent'`, so the 422 body carries one line per defect.
11. **NEW — v2 POST catalog validation (F12)**: adopt `nodeDefaultsError` over agent `node.config` tunables in the v2 branch (parity with the v1 branch; P5's composer then surfaces the same 400 texts).
12. **NEW — `writeGraphWorkflow` origin (F11)**: `origin = COALESCE(excluded.origin, workflows.origin)`; P7 item 18 (plugin rows keep their own INSERT) is unaffected.
13. **NEW — promptHints count (F3)**: six, incl. implementer — this also protects P3 Task 9's parity pin for `Work inside the project directory…` (adj-f1 §0 item 2) from a "repair" in the wrong direction.
14. **A31 ✓** — P2's helper is fine; the Task 13 guard needs the `!!` fix (F2) before it can be the drift oracle A31 assumes.
