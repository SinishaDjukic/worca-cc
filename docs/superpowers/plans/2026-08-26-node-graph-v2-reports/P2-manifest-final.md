# P2 refinement — EDIT MANIFEST (mechanical part; from P2-anchors.md). Applier: copy the CONTRACT-ALIGNED v1 file to `…-P2-shared-core-sidecars-schema-store-v2.md` and apply there.
File F = /Users/denislavprinov/Develop/worca-cc/docs/superpowers/plans/2026-08-26-node-graph-v2-P2-shared-core-sidecars-schema-store-v2.md (line numbers from the v1 file at draft time; locate by quoted text).

## Plan-killers
- P2-M1 (Task 3 `validate.mjs` V3, L1021): old `      if (!KINDS.has(n.kind)) {` → new `      if (!KINDS.includes(n.kind)) {` (P1's `KINDS` is a frozen ARRAY; measured: every `validateGraph()` call threw `TypeError: KINDS.has is not a function`, all 8 seeds + 23 validate tests + the manifest round-trip failed). Add to Task 3's mutation list: "`includes` → `has` ⇒ every test RED with the TypeError".
- P2-M2 (Task 13 second test, L3842): old `      const budgeted = w.config && w.config.maxCycles !== undefined;` → new `      const budgeted = Boolean(w.config && w.config.maxCycles !== undefined);` (`assert/strict` `equal` is `strictEqual`; `undefined !== false` throws for every config-less wire).
- P2-M3 (Task B4 `resolveGraph`, around L4638 `const portsFn = registryPortsFn(agentsByKey);`): on a workspace resolve `agentsByKey` is keyed by the SUBSTITUTED key while `tpl.nodes[].key` holds the AUTHORED key, so every substituted node resolves `known:false`, `classifyLoops` finds no loop wires and `wires` comes back `{}` (and the manifest P4 builds from the same map is portless). Fix: build `const metaByAuthored = {}` — for each agent node `metaByAuthored[node.key] = agentsByKey[resolvedKeyFor(node)]` (the variant's meta under the authored key; identical for non-substituted nodes) — use `const portsFn = portsFnFor(metaByAuthored);` (import `portsFnFor` from `../shared/graph/ports.mjs`), and return `metaByAuthored` alongside the existing fields so P4's manifest builder uses the SAME map. Add to B4's test `workspace resolve substitutes the variant…`: `assert.deepEqual(Object.keys(g.wires).sort(), ['w12', 'w15', 'w5'].sort())` for `wf_full` (its loop wires), plus the mutation line "key `portsFn` by the substituted map ⇒ RED". NOTE: the final `resolveGraph` RETURN SHAPE is the cross-plan sheet's decision (open-points P2 #27 vs P4 #1) — apply that sheet's shape; this edit only fixes the keying.
- P2-M4 (Task 0 Step 4, L46): old `` `LIMITS` must expose finite `maxNodes` and `maxWires`. If it does not, ADD them in this task (`maxNodes: 60, maxWires: 120`, keeping every key P1 already put there) and record the deviation in the Q&A. `` → new `` `LIMITS` must expose `maxNodes: 80` and `maxWires: 200` (P1 ships `{maxNodes: 80, maxWires: 200, maxPortsPerSide: 8, minArity: 2, maxArity: 8, maxCycles: 20, maxNameLen: 80}`) — verify, do not add; any other value is a P1 regression to fix in P1, never a P2 deviation. ``
- P2-M5 (B1 Step 4 L4043 and B10 Step 2 L5185, the pin sweep): old command `grep -rn -A1 "user_version" test/*.mjs | grep -w 22 | grep -v db-migrate-v23` (`Expected: empty output`) → new command `grep -rn -A1 "user_version" test/*.mjs | sed -E 's/^[^:]+[:-][0-9]+[:-]//' | grep -w 22` with `Expected:` exactly the two pre-existing COMMENT lines (`test/ask-db-schema.test.mjs:80` and `test/diff-comments-schema.test.mjs:72` mention v22 in prose) and NO assertion line; the `sed` strips the `file:lineno:` prefix so a line NUMBER 22 (four permanent false positives on dev: `db-pause-schema.test.mjs:22`, `subagent-migration-v6/v7/v8.test.mjs:22`) can no longer match. Run it before the bump (must list only those two comments) and after (identical).

## Messages / prose that contradict the code
- P2-M6 (Task 3 rule table row V2, L569 and code L1010): old message `node id <json> must match /^[A-Za-z0-9_-]{1,64}$/` → the code tests P1's `NODE_ID_RE = /^n_[a-z0-9]{1,32}$/`; change BOTH to `` add(`node id ${JSON.stringify(id)} must match ${NODE_ID_RE}`, { nodeId: id }); `` and the table text to `node id <json> must match /^n_[a-z0-9]{1,32}$/`.
- P2-M7 (Task 3 rule table row V3, L570): old `expected one of agent, task, and, or, combine, end` → new `expected one of agent, task, end, and, or, combine` (= `[...KINDS].join(', ')` in P1's order).
- P2-M8 (Task 3 "Consumes" line): drop `PORT_TYPES` (never used in `validate.mjs`).
- P2-M9 (Task 1 `ports.mjs`): either drop the unused `FLOW_KINDS` import or use it — apply: `if (!FLOW_KINDS.includes(kind)) return undefined;` at the top of `flowPorts` (state which in the task's Interfaces line).
- P2-M10 (Task 7 Step 2 note): old `If P1's `PORT_TYPES` is a Set the `TYPES` line works as written; if it is an array, drop the spread.` → new ``PORT_TYPES` is a frozen array in P1; the spread is a defensive copy and stays.`
- P2-M11 (Task 7 impl, local `const PORT_ID_RE = /^[a-z][A-Za-z0-9_-]{0,31}$/`): RECOMMENDED — delete the local copy and `import { PORT_ID_RE } from './constants.mjs'` (P1's strict lowerCamel `/^[a-z][A-Za-z0-9]{0,31}$/`, which matches the product text "Port ids are lowerCamel, ≤32 chars" in adj-f2 §2 and every builtin/flow port id); adjust any Task 7 test that expects `in_1`/`in-1` to be VALID (they must be INVALID, consistent with P1's pinned test). If the cross-plan sheet decides the relaxed regex instead, change P1's constant + test and keep P2 importing it — one source either way.
- P2-M12 (Task 5 prose): old `WIRE_HIT_TOL 6` "frozen constant from the prototype" → reword `from spec §7.4 (the prototype hit-tests ports only)`.
- P2-M13 (Task 12 prose): `has a **second** test that walks every `ui/public` specifier` → `has a **third** test (`ui/public leaves the static root only into src/shared…`) that walks every `ui/public` specifier`.
- P2-M14 (Task 8 Step 2 verify, L2787): old `` `node --test test/mock-runner.test.mjs test/mock-graphify.test.mjs` (every existing `mock*` suite) `` → new `` `node --test test/claude-runner-ask-mock.test.mjs test/skill-mock.test.mjs test/subagent-mock.test.mjs test/workspace-mock.test.mjs` (every existing `*mock*` suite — `ls test | grep mock`) ``.
- P2-M15 (Task 9 header): add one sentence naming the three deliberate deltas vs `old:agents/*.meta.json`: `decomposer.tasks` gains `"artifactKind": "decomposition"`; `implementer` inputs reordered `plan,fix,task` → `fix,task,plan` and the `plan` port gains a `directive`; `manualTestsChecklist.promptHints` templates `via \`git diff\` in your cwd` → `{diffInstruction}`; the other 8 sidecars' port bodies are byte-identical to the old branch (all three per spec §6).
- P2-M16 (Task 3 LIMITS sentence): old `the structural ceilings the validator enforces` → new `the structural ceilings; V1 enforces `maxNodes`/`maxWires` — `minArity`/`maxArity`/`maxCycles`/`maxNameLen` are reserved for the composer (P5) and stay unread here` (or wire `maxArity` into V12 — NOT done: spec lists no such error).
- P2-M17 (B4 refusal text vs B2): note in B4's Interfaces: `resolveGraph` throws `workflow not found: <id>` only when called directly; every run path reaches it through `assertRunnableWorkflow` (`unknown workflowId "<id>"`), so the two texts never race. (Or unify to the B2 text — planner default: unify, use `unknown workflowId "<id>"` in both.)
- P2-M18 (B5 Step 2): add the note `the dev handler is `app.get('/api/workflows', async (_req, res) => {` — the replacement MUST rename `_req` → `req` (it reads `req.query.archived`)`.
- P2-M19 (B9 test comment): `// createAgent({ meta, markdown }) — agent-store.mjs:53` → `:54`.

## Anchor drifts (cite the real lines)
- P2-M20 (Task 10 Files): `agent-registry.mjs:189-249` → `:189-248`; Task 7 Files never-borrow note `agent-registry.mjs:29` → `:28`; Task 10 Why-paragraph `agent-store.mjs:86-110` → `:97-99` and quote the real three lines: `const base = { ...existing }; if (base.descriptionDerived) base.description = ''; const raw = { ...base, ...rawMeta };`.
- P2-M21 (B1 Files L3895): `INCREMENTAL_COLUMNS` `:732-745` → `:732-742`; the ladder `:1051-1073` → `:1049-1073` (the load-bearing `:1072 if (current < 22) applySchemaV22(db);` + `:1073` stamp are exact); B1 prose `db.mjs:725-760` are those maps / `:845` is `reconcileSchema` → `db.mjs:732-766` (maps) / `schemaGaps :814`, `repairSchemaGaps :834`, `reconcileSchema :855`.
- P2-M22 (B1 Step 3): `test/db.test.mjs` `EXPECTED_TABLES :74-92` → `:74-93` (18 entries; the length assertion at `:126` and the title at `:120` are exact).
- P2-M23 (B9 Files): `plugin-workflows.mjs:150-178` → `:150-179` (SQL at `:166` byte-exact).

## Held for the P2 critique / cross-plan sheet (do NOT apply yet)
- H1 implementer `promptHints` (Task 9 L2866 vs Task 10 "Exactly five" L3141): spec §6 row says implementer hints = "Work inside the project directory (your cwd). Commit nothing; just edit files and tests." (`phases.mjs:836`); the old branch's implementer sidecar has none. Decide after the critique reports whether v1's `phases.mjs` renders `meta.promptHints` for builtins (double sentence risk) — if v1 ignores hints for builtins or the implementer's v1 prompt is unaffected: KEEP the hint, change Task 10 to six (`+ 'implementer'`, "Exactly six"); else DROP the hint line and let P3's prompt assembly keep the sentence in `baseInstruction('producer' + sideEffect:'code')` or the implementer's directive.
- H2 `resolveGraph` return shape, `portsOf`/`verdict` access path, `firedOutputs` signature, `canWire` codes, shim rank rule, 409 vs 400, `PORT_ID_RE` — per the cross-plan sheet §A.
- H3 `WIRE_ID_RE` unconsumed by P2 (no V2-style wire-id check; old-branch parity) — note only, unless the sheet adds a rule.

## Cross-plan overlap (xplan-manifest §B P2 = P2-E1…E21, applied IN PLACE before this manifest runs) — SKIP my duplicates
- P2-M1 (`KINDS.has` → `includes`) = P2-E1 — verify applied, then SKIP M1.
- P2-M3 (`resolveGraph` keying by authored vs substituted) = P2-E5…E13 (A1: resolved-key deep-copy template; returns `ports`, `loops`; entries += `authoredKey`, `config`, `duplicateKey`; `wires` for loop wires only) — verify applied; SKIP M3 except: keep the added B4 TEST assertion that a workspace resolve of `wf_full` returns `Object.keys(g.wires)` = `['w12','w15','w5']` (sorted) IF the xplan edits did not add an equivalent assertion (grep the B4 test for `g.wires`).
- P2-M6/M7 (V2/V3 messages) — check P2-E2…E4/E7/E14/E15 (A20 double-quote texts) did not already rewrite those lines; apply M6/M7 only on the remaining text.
- P2-M17 (B4 refusal text) — A20 makes `resolveGraph`'s unknown-key text `unknown agent "<key>" — no such key in the registry`; the UNKNOWN-WORKFLOW text is separate: keep M17's unification to `unknown workflowId "<id>"`.
- 409 → 400 (P2-E16…E18, E20, E21; A16): my draft has no 409 edits — nothing to do; verify B7's Expected lines say 400.
- A19 (shim ranks = `rankNodes` + `launchOrder`, B8 prose) = P2-E19/E21 — no draft overlap.
- H1 (implementer promptHints five-vs-six) — still HELD for the P2 critique verdict (v1 prompt-bytes question). §C C168 accepted the reorder + plan directive; it did not rule on the hint line.
- H2 items are now decided by §A (A1, A10, A16, A19, A20, A24, A30) — apply per the xplan edits; `PORT_ID_RE` single-source (P2-M11) was NOT adjudicated: keep my recommendation (import P1's strict constant) unless the critique objects.

## Critique fixes (from P2-critique.md — take the EXACT code/text from that file's F-sections; the critique ran the plan's own modules in scratch)
- P2-C1 (F1, CRITICAL) — VERIFY xplan P2-E1 applied (`KINDS.includes`); ADD the Task 0 Step 4 shape probe: `node --input-type=module -e "import { KINDS, PORT_TYPES, FLOW_KINDS } from './src/shared/graph/constants.mjs'; if (!Array.isArray(KINDS)) throw new Error('KINDS is not an array')"`. (P2-M1 subsumed.)
- P2-C2 (F2, MAJOR) — APPLY `const budgeted = !!(w.config && w.config.maxCycles !== undefined);` (P2-M2 subsumed — same fix).
- P2-C3 (F3, MAJOR — resolves H1) — APPLY: Task 10 expected list `['implementer', 'manualTestsChecklist', 'manualWebUiTesting', 'planReviewer', 'refiner', 'workspaceReviewer']`; prose "Exactly six builtins carry prompt hints; the other five stay empty". KEEP the implementer hint in Task 9. Q&A line: implementer hint is spec §6 + P3 parity (adj-f1 §5); v1 builders never read `promptHints` (critique v1-safety check) so v1 bytes are unchanged.
- P2-C4 (F4, MAJOR) — APPLY catalog ids/efforts in B3/B4/B6 tests: overlay `'claude-sonnet-5'`; override `'claude-opus-5'` + `'max'`; legacy layer `'claude-opus-4-8'`; defaults patch `{ model:'claude-sonnet-5', effort:'medium' }` (ids as in test/api-workflows.test.mjs:176; EFFORTS = medium/high/xhigh/max). Template `node.config` literals may stay.
- P2-C5 (F5, MAJOR) — APPLY: B5 422 test's `w3` becomes a distinct pair into an already-wired input (`{ id:'w3', from:{node:'n_task',port:'task'}, to:{node:'n_end',port:'result'} }`) so `errors[0].code === 'V7'`; keep the deepEqual-vs-shared-validator assertion.
- P2-C6 (F6, MAJOR) — APPLY: `assertRunnableWorkflow` becomes an injected dep of `createProposalValidator` (default = the real import); `validateProposal` uses the injected one; `test/ask-proposal.test.mjs` `deps` gains the fake `assertRunnableWorkflow` from F6 + one new case (a `{version: 2}` row pushes the interim text). Fix the plan's claim that the suite "stays green" to describe the injection.
- P2-C7 (F7, MAJOR) — APPLY (= P2-M11): `import { PORT_TYPES, MAX_PORTS_PER_SIDE, PORT_ID_RE } from './constants.mjs'` in `agent-meta.mjs`; delete the local `PORT_ID_RE`; flip any Task 7 test expecting `in_1`/`in-1` VALID to INVALID; Q&A: canonical regex = P1's strict `/^[a-z][A-Za-z0-9]{0,31}$/` (lowerCamel, adj-f2 §2 product text; adj-f2 §3/base §5's `_-` text superseded — record as a deviation, spec §15 index untouched (untracked spec)).
- P2-C8 (F8, MAJOR) — APPLY (= P2-M6): `` add(`node id ${JSON.stringify(id)} must match ${NODE_ID_RE}`, { nodeId: id }); `` + rule-table text; Q&A: P1's `n_`-prefixed id space is a deliberate deviation from base spec V2 (`/^[A-Za-z0-9_-]{1,64}$/`) — mint `n_`+8 base36, seeds `n_<word>`.
- P2-C9 (F9, MAJOR) — APPLY: every manifest cell carries `config: { ...(node.config || {}) }`; `manifestTemplate` restores it verbatim (drop the `arity`/`awaitAll` special cases); test 'manifest head and node cells' pins `plan.config` deepEqual the template's; the round-trip test uses `wf_provided-plan`'s shape (`planStoreSeed`). Spec §5.8 shape gains `config` (additive) — Q&A line. (P4: `resolvedFromManifest` reads `mn.config` — later-plan-notes.)
- P2-C10 (F10) — APPLY: Task 9's commit step body gains the sentence "v1 system prompts for clarify/planner/refiner/planReviewer/decomposer/implementer/reviewer/workspaceReviewer/manualTestsChecklist/manualWebUiTesting/workspaceScanner gain/replace a `## Ports` section; task prompts unchanged".
- P2-C11 (F11) — APPLY: `writeGraphWorkflow` UPSERT `origin = COALESCE(excluded.origin, workflows.origin)` + one test (`origin` survives a re-save that omits it).
- P2-C12 (F12) — APPLY: v2 `POST /api/workflows` branch runs `nodeDefaultsError(pick(n.config, ['model','effort','fanOut','askQuestions']), models, \`node "${n.id}"\`)` for every agent node with `config` BEFORE `validateGraph` → 400 with the same texts as PATCH …/defaults; one test.
- P2-C13 (F13) — APPLY: V9 `if (n.kind !== 'agent' || !metaOf(n.id)) continue;` (flow-card inputs are V12/V21's).
- P2-C14 (F14) — APPLY: V12 arity bounds via `limits.minArity`/`limits.maxArity` with the message `<kind> node '<id>' needs an integer arity between 2 and 8 (got …)`; V12 test regex `/needs an integer arity/`; update the rule table row; P2-M16 wording then says V1 + V12 consume LIMITS (minArity/maxArity) and the rest are reserved.
- P2-C15 (F15) — APPLY the seven fixture additions (a)–(g) to `test/graph-validate.test.mjs` verbatim from F15 (they kill the seven surviving mutations: V18(c), V19 End, V19 await, V15 unresolved-not-entry, V5 metaOf guard, V11 unresolved member, V7 over ALL wires); add the corresponding mutation lines to Task 3 Step 3.
- P2-C16 (F16) — APPLY Task 9 executability: (a) "insert the fragment immediately after the `"key": "<key>",` line"; (b) explicit line anchors for the `.md` insertions (decomposer after line 7 before `# Your role`; clarify :9, planner :9, implementer :9 — verify each against dev before writing them); (c) sanity script placeholder loop → `if (!(k in raw)) { bad += 1; console.log(f, 'lost v1 field', k); }`.
- P2-C17 (F17 + P2-M5) — APPLY the merged pin sweep: command `grep -rn -A1 "user_version" test/*.mjs | sed -E 's/^[^:]+[:-][0-9]+[:-]//' | grep -w 22`; Expected BEFORE the bump: exactly the two comment lines (`ask-db-schema.test.mjs:80`, `diff-comments-schema.test.mjs:72`); AFTER: empty IF B1 rewrites those two comments to 23 (check B1's steps — if it does not, say "the same two comment lines, no assertion").
- P2-C18 (F18) — APPLY: B7's three `POST /api/run` requests use a separate `mkdtemp('worca-cc-run-')` project dir + `mock: true` in the body (recipe of test/api-workflows.test.mjs:104-112), never `homeDir`.
- P2-C19 (F19) — APPLY: manifest `uiPhase = UI_PHASE[node.key] || (typeof meta?.uiPhase === 'string' && meta.uiPhase) || node.key` for agents.
- P2-C20 (F20) — APPLY ONLY IF `grep -rn "delete or edit those first" test/` shows no full-text pin: REFERENCED message += " (archived rows count)"; else note in Q&A.
- P2-C21 (F21) — APPLY: geometry test pins `portAnchor(bare, agentPorts(0, 1), 'await', 'in').y === <y + 65 + 24·nOut>` (compute: for `y = 80`, nOut = 1 ⇒ 169; the critique's `89` is for y = 0 — use the fixture's y) and lists the 0-input await caveat beside the height caveat.
- P2-C22 (F22) — APPLY: Task 3 comment "arrives … at Task 10 (Step 4)"; Task 13 "add the import beside P1's imports at the top".
- F23, F25 — NOTE ONLY (keep `structuredClone` in E6; engine loads geometry constants — harmless).
- P2-C23 (F24) — APPLY one sentence in Task 7: v2 sidecars REQUIRE `runnerType` (v1 defaulted to `producer`) — intentional strictness, rule-for-rule with adj-f2 §3; P7's editor always writes it.
- P2-C24 (seam rec 9) — APPLY: xplan E13's `g.loops.loopWireIds.size >= 1` → `assert.deepEqual([...g.loops.loopWireIds].sort(), ['w12','w15','w5'])` for the `wf_full` workspace resolve (or the fixture's actual loop set — the applier computes it from the fixture used in that test).
- Provenance line (under the title): `> **v2 (refined 2026-08-27, Session A):** v1 + cross-plan contract alignment (xplan A1/A10/A16/A19/A20/A30/A31) + Opus anchor fact-check (86 OK/20 DRIFT/7 WRONG) + executed dry-run in a clone (P1 applied first; see counts) + Fable critique (F1–F24 applied). Wave-2 re-execution + cold fresh-eyes follow; counts marked "re-measured" are written by that run. Anchors valid for dev @ e6968e15.`

## Dry-run deviations + survivors (from P2-dryrun.md — executed on dev + P1; counts hit exactly: 3760 → 3805 → 3902 (P2a +97) → 3939 (P2b +37); take the EXACT text from the report's sections)
- P2-D1/D2/D3/D6/D9/D10 = critique F1/F3/F2/F4/F5/F6 — already covered above (apply once). P2-D4 = P2-M14; P2-D5 = P2-C17.
- P2-D7 (BLOCKING) — Task B4 Step 1 test: `readWorkflow` used but not imported → replace the import block with the report's (`writeGraphWorkflow, readWorkflow, resolveGraph, workspaceVariants, workflowNodeDefaults, setWorkflowNodeDefaults` from `../src/core/workflows.mjs`).
- P2-D8a (BLOCKING) — Task B5 Step 1: replace the "Boot preamble: COPY the before/after block …" placeholder at the head of `test/api-workflows-graph.test.mjs` with the resolved preamble from report §D8a VERBATIM (lifted from `test/api-workflows.test.mjs:1-36`, mkdtemp prefix `worca-cc-wfgraphapi-`, imports incl. `registryPortsFn`/`validateGraph`/`loadAgentRegistry`/`getDb, prepare`).
- P2-D8b (BLOCKING) — Task B7 Step 1: same for `test/run-workflow-gate.test.mjs` (report §D8b: the preamble + the missing `api()` helper). Merge with P2-C18 (separate `mkdtemp('worca-cc-run-')` project dir + `mock: true`).
- P2-D11 (BLOCKING, full-suite-only) — Task B3 Step 2.4 changes `resolveRunConfig`'s shape to `{nodes, wires, feedbacks}` and breaks the whole-object deepEqual at `test/run-config.test.mjs:96`, a file B3 never lists: add it to B3's Files and apply the report's exact edit (expected object gains `wires: {}`) BEFORE B3's commit; add `node --test test/run-config.test.mjs` to B3's verification step.
- Survivors (report §3f — add each proposed assertion/test; where a critique fix already covers the same rule, apply ONE version):
  - S1 V18 exemption (a) task-sourced inputs — new fixture case (report §3f S1).
  - S2 V18 exemption (c) synthesized await — = critique F15 (1); apply once.
  - S3 V18 exemption (d) loop inputs — new fixture case (report §3f S3).
  - S4 V19 `end.result` — = F15 (2). S5 V19 `agent.await` — = F15 (3). Apply once each.
  - S6 Ask proposal graph refusal — pin behaviorally in `test/ask-proposal.test.mjs` via the injected `assertRunnableWorkflow` (merge with P2-C6's new `{version: 2}` case — one test that asserts the interim-text error is pushed).
  - S7 CLI `--workflow` gate — add the report's test (an ARCHIVED id and a v2 id each make `worca run --workflow` exit 2 with the §4 / interim text; use the report's child-process recipe with `WORCA_HOME` sandbox and `--mock`).
  - S8 `agent-store` `{ includeArchived: true }` — add the report's test (an agent referenced only by an ARCHIVED row still refuses delete with `REFERENCED`).
  - S9 `plugin-workflows` graph-node walk — add the report's test (`referencedPluginAgents` finds a key used only inside a v2 row's `nodes[]`).
  - S10/S11 B9 `ui/public/app.js` guards — REPLACE the source-regex test with the report's behavioral jsdom tests (call `buildNodeConfigRows`/`buildFeedbackRows` with a v2 workflow object ⇒ zero rows, no throw; the composer saved-list painter given a v2 row renders nothing/does not throw) — the regex test stays only if the behavioral ones cannot reach the functions (report says they can via `window.__np`/exports — follow it).
- Count gates: recount the `test(` blocks per NEW/MODIFIED test file after all edits (critique F11/F12/F15 cases, C6/S6/S7/S8/S9/S10–S11 additions) and rewrite every per-task `Expected: ℹ pass N`, the P2a gate (was BASELINE + 97) and the P2b gate (was BASELINE-B + 37), each marked "re-measured in the wave-2 execution; write the printed number". Reference: the v1 dry-run measured 3805 → 3902 → 3939 with P1 v1 applied; P1 v2 adds +5 tests (3810 baseline for P2).
