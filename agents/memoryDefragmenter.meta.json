{
  "key": "memoryDefragmenter",
  "metaVersion": 2,
  "mockRole": "memory-defrag",
  "sideEffect": "memory",
  "inputs": [
    { "id": "task", "type": "md", "required": true,
      "directive": "Defragment the memory scope named below. Work only inside the memory directory your system prompt names (the `## Worca memory` block) \u2014 it sits outside the project checkout, in the run's own memory directory; the copy under the cwd's `.claude/rules/worca/` is read-only, so never write there. Never touch anything in the project directory." }
  ],
  "outputs": [
    { "id": "report", "type": "md", "filename": "defrag-report.md" },
    { "id": "done", "type": "void" }
  ],
  "domain": "shared",
  "displayName": "Memory defragment",
  "description": "Restructures one memory scope: merges duplicates, splits overgrown topics, drops stale rules, tightens hooks.",
  "color": "violet",
  "icon": "<path d=\"M4 6h16M4 12h16M4 18h10\" stroke-linecap=\"round\"/><path d=\"M17 15l2 2 4-4\" stroke-linecap=\"round\" stroke-linejoin=\"round\"/>",
  "agentFile": "worca-cc-memory-defragmenter.md",
  "runnerType": "producer",
  "fanOut": false,
  "asksQuestions": false,
  "order": 8
}
