# worca-metrics

This branch is written by Worca team metrics. Each file under `.worca-metrics/runs/` is one finished pipeline run (one JSON line). Do not edit it by hand and do not merge it into other branches. To stop recording for the whole team, delete this branch: `git push origin --delete worca-metrics`.
